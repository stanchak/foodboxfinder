---
phase: 14-visual-rebrand-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - REBRAND-TOKENS
  - REBRAND-FONTS

must_haves:
  truths:
    - "Page background is warm off-white (oklch 0.985 0.004 80) instead of pure white"
    - "Primary color is deep teal (hue ~190) instead of green (hue ~150)"
    - "Accent color is warm amber (hue ~48-80) instead of coral"
    - "Headings and body text render in Inter, not Geist Sans"
    - "Source Serif 4 font is loaded and available via font-serif class"
    - "Geist Mono is still available for monospace/price display"
    - "Focus rings use teal primary-600 instead of old green"
    - "Warm-tinted shadows replace rgb-based shadows"
    - "Success green is decoupled from primary (distinct hue 150 vs 190)"
    - "Warm neutral palette has slight brown/amber undertone (hue 40-80)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete design token system (colors, shadows, radii, semantic tokens)"
      contains: "oklch(0.520 0.120 190)"
    - path: "src/app/layout.tsx"
      provides: "Inter + Source Serif 4 + Geist Mono font loading"
      contains: "Source_Serif_4"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-inter maps to Inter font, --font-serif maps to Source Serif 4"
      pattern: "--font-inter"
    - from: "src/app/globals.css"
      to: "All components using primary/accent/neutral Tailwind classes"
      via: "@theme block CSS custom properties"
      pattern: "--color-primary-600"
---

<objective>
Replace the entire design token system and font stack for the FoodBoxFinder visual rebrand.

Purpose: This is the foundation layer. Every component in the site inherits its visual identity from globals.css tokens and layout.tsx fonts. By replacing tokens first, all existing components that reference `primary-*`, `accent-*`, and gray utilities will immediately shift to the new brand palette. Typography swap from Geist Sans to Inter changes the entire site's reading experience in one move.

Output: Updated globals.css with all new oklch color tokens, warm shadows, updated radii, and neutral palette. Updated layout.tsx with Inter, Source Serif 4, and Geist Mono font loading.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BRAND_BRIEF.md
@.planning/phases/14-visual-rebrand-design-system-foundation/14-CONTEXT.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace design tokens in globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current token definitions)
    - .planning/BRAND_BRIEF.md (section 8 — Implementation Specifications, has exact CSS to use)
  </read_first>
  <action>
Rewrite src/app/globals.css completely. Keep the `@import "tailwindcss"` line. Replace all content below it.

**:root block — replace both values:**
```
--background: oklch(0.985 0.004 80);
--foreground: oklch(0.230 0.008 40);
```
The background is warm neutral-50 (not pure white). The foreground is neutral-900.

**@theme inline block — update font references:**
```
--font-sans: var(--font-inter);
--font-serif: var(--font-serif);
--font-mono: var(--font-geist-mono);
```
Note: `--font-sans` changes from `var(--font-geist-sans)` to `var(--font-inter)`. Add `--font-serif`.

**@theme block — replace ALL color tokens with exact oklch values from BRAND_BRIEF.md section 8:**

Primary (Deep Teal) — 11 shades:
- `--color-primary-50: oklch(0.975 0.014 200);`
- `--color-primary-100: oklch(0.940 0.035 200);`
- `--color-primary-200: oklch(0.880 0.065 198);`
- `--color-primary-300: oklch(0.790 0.100 196);`
- `--color-primary-400: oklch(0.700 0.120 194);`
- `--color-primary-500: oklch(0.600 0.130 192);`
- `--color-primary-600: oklch(0.520 0.120 190);`
- `--color-primary-700: oklch(0.450 0.100 190);`
- `--color-primary-800: oklch(0.380 0.080 190);`
- `--color-primary-900: oklch(0.310 0.060 190);`
- `--color-primary-950: oklch(0.220 0.045 190);`

Accent (Warm Amber) — 11 shades:
- `--color-accent-50: oklch(0.985 0.020 80);`
- `--color-accent-100: oklch(0.955 0.050 78);`
- `--color-accent-200: oklch(0.900 0.095 72);`
- `--color-accent-300: oklch(0.840 0.140 65);`
- `--color-accent-400: oklch(0.780 0.170 55);`
- `--color-accent-500: oklch(0.720 0.180 48);`
- `--color-accent-600: oklch(0.640 0.175 42);`
- `--color-accent-700: oklch(0.550 0.155 40);`
- `--color-accent-800: oklch(0.470 0.125 40);`
- `--color-accent-900: oklch(0.400 0.095 40);`
- `--color-accent-950: oklch(0.300 0.070 40);`

Rating Stars:
- `--color-star: oklch(0.780 0.170 55);`
- `--color-star-empty: oklch(0.920 0.008 75);`

Semantic Success (Fresh Green — decoupled from primary):
- `--color-success-50: oklch(0.975 0.020 155);`
- `--color-success-500: oklch(0.650 0.180 150);`
- `--color-success-600: oklch(0.560 0.160 150);`
- `--color-success-700: oklch(0.480 0.130 150);`

Semantic Error (unchanged):
- `--color-error-50: oklch(0.971 0.013 17.38);`
- `--color-error-500: oklch(0.637 0.237 25.331);`
- `--color-error-600: oklch(0.577 0.245 27.325);`
- `--color-error-700: oklch(0.505 0.213 27.518);`

Semantic Warning (unchanged):
- `--color-warning-50: oklch(0.987 0.026 102.212);`
- `--color-warning-500: oklch(0.769 0.188 70.08);`
- `--color-warning-600: oklch(0.666 0.179 58.318);`
- `--color-warning-700: oklch(0.555 0.163 48.998);`

Warm Neutral palette — 11 shades (NEW — these register as Tailwind `neutral-*` utilities):
- `--color-neutral-50: oklch(0.985 0.004 80);`
- `--color-neutral-100: oklch(0.960 0.006 80);`
- `--color-neutral-200: oklch(0.920 0.008 75);`
- `--color-neutral-300: oklch(0.860 0.010 70);`
- `--color-neutral-400: oklch(0.720 0.010 65);`
- `--color-neutral-500: oklch(0.600 0.010 60);`
- `--color-neutral-600: oklch(0.500 0.008 55);`
- `--color-neutral-700: oklch(0.400 0.008 50);`
- `--color-neutral-800: oklch(0.310 0.008 45);`
- `--color-neutral-900: oklch(0.230 0.008 40);`
- `--color-neutral-950: oklch(0.150 0.005 40);`

Shadows (warm-tinted — replace rgb-based shadows):
- `--shadow-card: 0 1px 3px 0 oklch(0.30 0.01 50 / 0.06), 0 1px 2px -1px oklch(0.30 0.01 50 / 0.06);`
- `--shadow-card-hover: 0 4px 12px -2px oklch(0.30 0.01 50 / 0.10), 0 2px 4px -2px oklch(0.30 0.01 50 / 0.06);`
- `--shadow-elevated: 0 8px 24px -4px oklch(0.30 0.01 50 / 0.12), 0 4px 8px -4px oklch(0.30 0.01 50 / 0.06);`
- `--shadow-header: 0 1px 0 0 oklch(0.30 0.01 50 / 0.06);`

Border Radius (updated values):
- `--radius-sm: 0.25rem;`
- `--radius-button: 0.375rem;`
- `--radius-input: 0.375rem;`
- `--radius-card: 0.625rem;`
- `--radius-badge: 9999px;`

**body rule — keep as-is** (references var(--background) and var(--foreground)).

**:focus-visible rule — update to teal primary-600:**
```css
:focus-visible {
  outline: 2px solid oklch(0.520 0.120 190);
  outline-offset: 2px;
}
```

Do NOT add `-webkit-font-smoothing: antialiased` to body — the `antialiased` class is already on the html element in layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c "oklch(0.520 0.120 190)" src/app/globals.css && grep -c "oklch(0.985 0.004 80)" src/app/globals.css && grep -c "shadow-elevated" src/app/globals.css && grep -c "neutral-950" src/app/globals.css && grep "font-sans" src/app/globals.css | grep -c "font-inter"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `oklch(0.520 0.120 190)` (primary-600 teal) at least twice (color def + focus ring)
    - globals.css contains `oklch(0.985 0.004 80)` (warm off-white background + neutral-50)
    - globals.css contains `--shadow-elevated` (new shadow token)
    - globals.css contains `--color-neutral-950` (full warm neutral palette)
    - globals.css `--font-sans` references `var(--font-inter)` not `var(--font-geist-sans)`
    - globals.css contains `--font-serif: var(--font-serif)`
    - globals.css `--radius-card` is `0.625rem` (not old `0.75rem`)
    - globals.css `--radius-button` is `0.375rem` (not old `0.5rem`)
    - globals.css `--color-success-50` uses hue 155 (not matching primary hue 200)
    - No rgb() shadow values remain — all shadows use oklch
    - `--background` in :root is `oklch(0.985 0.004 80)` not `#ffffff`
    - `--foreground` in :root is `oklch(0.230 0.008 40)` not `#111827`
  </acceptance_criteria>
  <done>globals.css has the complete new brand token system: 11 primary teal shades, 11 accent amber shades, 11 warm neutral shades, star colors, 4 success/error/warning semantic sets, 4 warm-tinted shadows, 5 border radii, warm off-white root background, and teal focus ring.</done>
</task>

<task type="auto">
  <name>Task 2: Swap fonts in layout.tsx to Inter + Source Serif 4 + Geist Mono</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current font loading and html/body setup)
    - .planning/BRAND_BRIEF.md (section 4 — Typography Direction and section 8 — Font configuration)
  </read_first>
  <action>
Update src/app/layout.tsx to replace Geist Sans with Inter and add Source Serif 4. Keep Geist Mono.

**Import change:**
Replace `import { Geist, Geist_Mono } from "next/font/google";`
With `import { Inter, Source_Serif_4, Geist_Mono } from "next/font/google";`

**Font declarations — replace the two existing const blocks:**

Remove `geistSans` entirely. Replace with:
```typescript
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const sourceSerif = Source_Serif_4({
  variable: "--font-serif",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});
```

Note: Geist_Mono stays the same but keep it in this import.

**html className — update CSS variable classes:**
Change from: `${geistSans.variable} ${geistMono.variable}`
Change to: `${inter.variable} ${sourceSerif.variable} ${geistMono.variable}`

**body className — update text color:**
Change `text-gray-900` to `text-neutral-800` (warm neutral, slightly softer per brand brief which says body text primary is neutral-700 but using 800 for headings; neutral-800 is a safe default for body since it maintains strong contrast).

Change `bg-white` to `bg-[var(--background)]` so body background respects the warm off-white token. (Or remove `bg-white` entirely since the body rule in globals.css already sets `background: var(--background)`.)

Do NOT remove or change: the skip link, CompareProvider, Header, Footer, CompareBar, metadata, or any other existing structure. Only change the font loading and the html/body classNames.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c "Inter" src/app/layout.tsx && grep -c "Source_Serif_4" src/app/layout.tsx && grep -c "Geist_Mono" src/app/layout.tsx && grep "font-inter" src/app/layout.tsx | grep -c "variable" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports `Inter` and `Source_Serif_4` from `"next/font/google"`
    - layout.tsx does NOT import `Geist` (the sans font)
    - layout.tsx still imports `Geist_Mono`
    - Inter configured with `variable: "--font-inter"` and `display: "swap"`
    - Source_Serif_4 configured with `variable: "--font-serif"` and `display: "swap"`
    - html className includes `inter.variable`, `sourceSerif.variable`, and `geistMono.variable`
    - body className does not contain `bg-white` or `text-gray-900`
    - `next build` completes without errors
  </acceptance_criteria>
  <done>Layout uses Inter as the primary sans font, Source Serif 4 is available via font-serif for editorial content, and Geist Mono remains for price/code display. The build succeeds with the new font stack.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "oklch(0.520 0.120 190)" src/app/globals.css` returns at least 2 matches (primary-600 + focus ring)
2. `grep "Inter" src/app/layout.tsx` confirms Inter font is loaded
3. `grep "Source_Serif_4" src/app/layout.tsx` confirms serif font is loaded
4. `grep "font-geist-sans" src/app/globals.css` returns NO matches (old font reference removed)
5. `grep "rgb(" src/app/globals.css` returns NO matches (all shadows converted to oklch)
6. `grep "#ffffff" src/app/globals.css` returns NO matches (no pure white)
7. `npx next build` succeeds
</verification>

<success_criteria>
The design token foundation is complete when:
- All 33 color shades (11 primary + 11 accent + 11 neutral) use exact oklch values from BRAND_BRIEF.md
- Semantic colors (success, error, warning, star) are defined with correct values
- Shadows use warm oklch tinting instead of rgb black
- Border radii use the updated values (card 0.625rem, button 0.375rem)
- Root background is warm off-white, not pure white
- Focus ring uses teal primary-600
- Inter is the sans font, Source Serif 4 is available as serif, Geist Mono remains
- The build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-visual-rebrand-design-system-foundation/14-01-SUMMARY.md`
</output>
