---
phase: 09-seo-and-collections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/best/[slug]/page.tsx
  - src/app/best/page.tsx
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03

must_haves:
  truths:
    - "Collection pages at /best/[slug] render curated, ranked provider lists with editorial notes"
    - "The collections index at /best shows all published collections with provider counts"
    - "Collection pages have metadata (title, description) and ItemList JSON-LD structured data"
    - "6 seeded collections exist in the database with provider items"
  artifacts:
    - path: "src/app/best/[slug]/page.tsx"
      provides: "Collection detail page with ranked list and editorial notes"
      contains: "ItemListJsonLd"
    - path: "src/app/best/page.tsx"
      provides: "Collections index page"
      contains: "CollectionListJsonLd"
    - path: "prisma/seed-data/collections.ts"
      provides: "6 collection definitions with items"
      contains: "seedCollections"
    - path: "src/lib/queries/content.ts"
      provides: "getPublishedCollections, getCollectionBySlug, getAllCollectionSlugs"
      exports: ["getPublishedCollections", "getCollectionBySlug", "getAllCollectionSlugs"]
  key_links:
    - from: "src/app/best/[slug]/page.tsx"
      to: "src/lib/queries/content.ts"
      via: "getCollectionBySlug"
      pattern: "getCollectionBySlug"
    - from: "src/app/best/page.tsx"
      to: "src/lib/queries/content.ts"
      via: "getPublishedCollections"
      pattern: "getPublishedCollections"
    - from: "src/app/sitemap.ts"
      to: "src/lib/queries/content.ts"
      via: "getAllCollectionSlugs"
      pattern: "getAllCollectionSlugs"
---

<objective>
Verify collection pages work end-to-end and fix any remaining gaps. Collections infrastructure (pages, queries, seed data) was built in prior phases. This plan verifies completeness and fixes minor issues.

Purpose: Confirm that the "Best for X" collection pages satisfy COLL-01, COLL-02, and COLL-03 requirements. The collection detail page, index page, queries, seed data, and sitemap entries all exist. The primary gap is ensuring the collection detail page renders correctly with seeded data and that any edge cases (empty collections, missing providers) are handled.

Output: Verified, working collection pages with all requirements met.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/best/[slug]/page.tsx
@src/app/best/page.tsx
@src/lib/queries/content.ts
@prisma/seed-data/collections.ts
@src/app/sitemap.ts

<interfaces>
From src/lib/queries/content.ts:
```typescript
export const getPublishedCollections = cache(async () => { ... });
export const getCollectionBySlug = cache(async (slug: string) => { ... });
export const getAllCollectionSlugs = cache(async () => { ... });
```

From prisma/schema.prisma:
```prisma
model Collection {
  id            String        @id @default(cuid())
  title         String
  slug          String        @unique
  description   String?       @db.Text
  body          String?       @db.Text
  coverImageUrl String?
  status        ContentStatus @default(DRAFT)
  publishedAt   DateTime?
  metaTitle       String?   @db.VarChar(70)
  metaDescription String?   @db.VarChar(160)
  items         CollectionItem[]
}

model CollectionItem {
  id           String @id @default(cuid())
  collectionId String
  providerId   String
  sortOrder    Int    @default(0)
  note         String? @db.Text
  collection   Collection @relation(...)
  provider     Provider   @relation(...)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify collection pages and fix BreadcrumbList JSON-LD on collection detail</name>
  <files>
    src/app/best/[slug]/page.tsx,
    src/app/best/page.tsx
  </files>
  <read_first>
    src/app/best/[slug]/page.tsx,
    src/app/best/page.tsx,
    src/lib/queries/content.ts,
    prisma/seed-data/collections.ts,
    src/components/Breadcrumbs.tsx,
    src/app/sitemap.ts
  </read_first>
  <action>
    Verify the following are all in place (most already exist):

    1. **Collection detail page** (`src/app/best/[slug]/page.tsx`):
       - Has `generateMetadata()` with title, description, openGraph — ALREADY EXISTS
       - Has `ItemListJsonLd` component with structured data — ALREADY EXISTS
       - Has `Breadcrumbs` component (which provides BreadcrumbList JSON-LD) — ALREADY EXISTS
       - Has `generateStaticParams()` for static generation — ALREADY EXISTS
       - Has `notFound()` call for missing slugs — ALREADY EXISTS
       - Displays ranked provider list with editorial notes ("Why we picked it") — ALREADY EXISTS
       - Displays editorial body text above the list — ALREADY EXISTS

    2. **Collections index page** (`src/app/best/page.tsx`):
       - Has static `metadata` export — ALREADY EXISTS
       - Has `CollectionListJsonLd` — ALREADY EXISTS
       - Has `Breadcrumbs` — ALREADY EXISTS
       - Shows grid of collection cards with provider counts — ALREADY EXISTS

    3. **Verify the collection detail page has a BreadcrumbList JSON-LD via the Breadcrumbs component.** The Breadcrumbs component already renders BreadcrumbList JSON-LD with XSS-safe output. Confirm it is used correctly.

    4. **Verify generateStaticParams pulls from getAllCollectionSlugs**, which queries PUBLISHED collections only. This ensures only published collections get static pages.

    5. **Verify the sitemap includes collection pages.** `src/app/sitemap.ts` already maps `collectionSlugs` to `/best/{slug}` URLs. Confirm this covers the 6 seeded collections.

    If all verifications pass, no code changes are needed for this task. The collection infrastructure is already complete from prior admin/content phases.

    If any gap is found during verification, fix it. Likely gaps:
    - None expected based on code review — the collection pages are fully built.

    Run `npx next build` to confirm the collection pages build correctly with the seeded data structure.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "generateMetadata\|generateStaticParams\|ItemListJsonLd\|Breadcrumbs\|notFound" src/app/best/[slug]/page.tsx` returns matches for all 5 patterns
    - `grep -n "metadata\|CollectionListJsonLd\|Breadcrumbs" src/app/best/page.tsx` returns matches for all 3 patterns
    - `grep "best/" src/app/sitemap.ts` confirms collection routes in sitemap
    - `next build` succeeds
  </acceptance_criteria>
  <done>Collection pages verified: detail page has metadata, ItemList JSON-LD, BreadcrumbList JSON-LD (via Breadcrumbs), ranked list with editorial notes, and static generation. Index page has metadata, CollectionPage JSON-LD, and collection grid. Sitemap includes all collection routes. Build passes.</done>
</task>

</tasks>

<verification>
1. Collection detail page has: generateMetadata, generateStaticParams, ItemListJsonLd, Breadcrumbs, notFound, ranked list, editorial notes
2. Collection index page has: metadata, CollectionListJsonLd, Breadcrumbs, collection cards with counts
3. Sitemap includes `/best/{slug}` routes via getAllCollectionSlugs
4. Build passes without errors
5. 6 collection definitions exist in seed data with provider items
</verification>

<success_criteria>
- Collection detail pages at /best/[slug] render curated, ranked provider lists with editorial notes per COLL-01 and COLL-02
- Collection pages have metadata and JSON-LD per COLL-03
- All collection infrastructure verified end-to-end
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-and-collections/09-02-SUMMARY.md`
</output>
