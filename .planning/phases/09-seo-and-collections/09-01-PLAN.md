---
phase: 09-seo-and-collections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/blog/[slug]/page.tsx
  - src/app/blog/page.tsx
  - src/app/methodology/page.tsx
  - src/app/best/page.tsx
  - src/app/best/[slug]/page.tsx
  - src/app/page.tsx
  - src/app/providers/[slug]/page.tsx
  - src/app/compare/[versus]/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Every JSON-LD script tag on every public page uses XSS-safe rendering with .replace(/</g, '\\u003c')"
    - "Every indexable public page has an alternates.canonical URL in its metadata"
    - "The sitemap covers all public routes (homepage, categories, providers, collections, blog posts)"
  artifacts:
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "XSS-safe JSON-LD and canonical URL"
      contains: "replace(/</g"
    - path: "src/app/blog/page.tsx"
      provides: "XSS-safe JSON-LD and canonical URL"
      contains: "replace(/</g"
    - path: "src/app/methodology/page.tsx"
      provides: "XSS-safe JSON-LD and canonical URL"
      contains: "replace(/</g"
    - path: "src/app/best/page.tsx"
      provides: "XSS-safe JSON-LD and canonical URL"
      contains: "replace(/</g"
    - path: "src/app/best/[slug]/page.tsx"
      provides: "XSS-safe JSON-LD and canonical URL"
      contains: "replace(/</g"
  key_links:
    - from: "All public pages"
      to: "metadata export"
      via: "alternates.canonical"
      pattern: "alternates.*canonical"
---

<objective>
Fix all remaining SEO gaps across the site: apply XSS-safe JSON-LD rendering to the 5 pages that are missing it, and add canonical URLs to all indexable public pages.

Purpose: Complete cross-site SEO hardening. Prior phases applied XSS-safe JSON-LD and metadata page-by-page but 5 pages were missed for XSS safety (blog index, blog detail, methodology, collections index, collection detail) and most pages lack canonical URLs.

Output: All public pages have consistent XSS-safe JSON-LD and canonical URL metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/blog/[slug]/page.tsx
@src/app/blog/page.tsx
@src/app/methodology/page.tsx
@src/app/best/page.tsx
@src/app/best/[slug]/page.tsx
@src/app/page.tsx
@src/app/providers/[slug]/page.tsx
@src/app/compare/[versus]/page.tsx
@src/app/sitemap.ts

<interfaces>
<!-- Existing XSS-safe pattern used on pages that already have it -->
From src/app/page.tsx (correct pattern):
```typescript
dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}
```

From src/app/[category]/page.tsx (correct canonical pattern):
```typescript
alternates: {
  canonical: `/${slug}`,
},
```

BASE_URL constant used across the site:
```typescript
const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? "https://foodboxfinder.com";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix XSS-safe JSON-LD on 5 pages missing the escape</name>
  <files>
    src/app/blog/[slug]/page.tsx,
    src/app/blog/page.tsx,
    src/app/methodology/page.tsx,
    src/app/best/page.tsx,
    src/app/best/[slug]/page.tsx
  </files>
  <read_first>
    src/app/blog/[slug]/page.tsx,
    src/app/blog/page.tsx,
    src/app/methodology/page.tsx,
    src/app/best/page.tsx,
    src/app/best/[slug]/page.tsx,
    src/app/page.tsx (reference for correct pattern)
  </read_first>
  <action>
    On each of the 5 files, find the JSON-LD script tag's `dangerouslySetInnerHTML` and change:
    ```
    {{ __html: JSON.stringify(jsonLd) }}
    ```
    to:
    ```
    {{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}
    ```

    Specific locations:
    1. `src/app/blog/[slug]/page.tsx` — the BlogPostJsonLd component return (~line 289). Change `JSON.stringify(jsonLd)` to `JSON.stringify(jsonLd).replace(/</g, "\\u003c")`
    2. `src/app/blog/page.tsx` — the BlogJsonLd component return (~line 38). Same change.
    3. `src/app/methodology/page.tsx` — the static jsonLd script tag (~line 40). Same change.
    4. `src/app/best/page.tsx` — the CollectionListJsonLd component return (~line 43). Same change.
    5. `src/app/best/[slug]/page.tsx` — the ItemListJsonLd component return (~line 111). Same change.

    Do NOT touch pages that already have the fix (page.tsx, [category]/page.tsx, providers/[slug]/page.tsx, compare/*.tsx, search/page.tsx, Breadcrumbs.tsx).
  </action>
  <verify>
    <automated>grep -rn 'JSON.stringify(jsonLd)' src/app/ --include="*.tsx" | grep -v 'replace' | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - grep for `JSON.stringify(jsonLd)` without `.replace` returns zero results in src/app/
    - All 5 files now contain `.replace(/&lt;/g, "\\u003c")`
  </acceptance_criteria>
  <done>Every JSON-LD script tag across all public pages uses the XSS-safe .replace() pattern. No unescaped JSON.stringify(jsonLd) remains in src/app/.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical URLs to all indexable public pages</name>
  <files>
    src/app/page.tsx,
    src/app/providers/[slug]/page.tsx,
    src/app/compare/[versus]/page.tsx,
    src/app/best/page.tsx,
    src/app/best/[slug]/page.tsx,
    src/app/blog/page.tsx,
    src/app/blog/[slug]/page.tsx,
    src/app/methodology/page.tsx
  </files>
  <read_first>
    src/app/page.tsx,
    src/app/providers/[slug]/page.tsx,
    src/app/compare/[versus]/page.tsx,
    src/app/best/page.tsx,
    src/app/best/[slug]/page.tsx,
    src/app/blog/page.tsx,
    src/app/blog/[slug]/page.tsx,
    src/app/methodology/page.tsx,
    src/app/[category]/page.tsx (reference for correct canonical pattern)
  </read_first>
  <action>
    Add `alternates.canonical` to the metadata export of each page. The category page already has this; skip it. Also skip search page and flexible compare page (they have `robots: { index: false }` — no canonical needed for noindex pages).

    For static metadata pages (export const metadata), add the alternates field:

    1. `src/app/page.tsx` — Add to `export const metadata`:
       ```typescript
       alternates: { canonical: "/" },
       ```

    2. `src/app/best/page.tsx` — Add to `export const metadata`:
       ```typescript
       alternates: { canonical: "/best" },
       ```

    3. `src/app/blog/page.tsx` — Add to `export const metadata`:
       ```typescript
       alternates: { canonical: "/blog" },
       ```

    4. `src/app/methodology/page.tsx` — Add to `export const metadata`:
       ```typescript
       alternates: { canonical: "/methodology" },
       ```

    For dynamic metadata pages (generateMetadata), add the alternates field to the returned object:

    5. `src/app/providers/[slug]/page.tsx` — Add to the return object in generateMetadata:
       ```typescript
       alternates: { canonical: `/providers/${slug}` },
       ```

    6. `src/app/compare/[versus]/page.tsx` — Add to the return object in generateMetadata:
       ```typescript
       alternates: { canonical: `/compare/${versus}` },
       ```

    7. `src/app/best/[slug]/page.tsx` — Add to the return object in generateMetadata:
       ```typescript
       alternates: { canonical: `/best/${collection.slug}` },
       ```

    8. `src/app/blog/[slug]/page.tsx` — Add to the return object in generateMetadata:
       ```typescript
       alternates: { canonical: `/blog/${slug}` },
       ```

    Use relative paths (not full URLs) — Next.js resolves them against the metadataBase (which defaults to the deployment URL).

    Do NOT add canonical to:
    - `src/app/search/page.tsx` (noindex)
    - `src/app/compare/page.tsx` (noindex flexible compare)
    - Any admin pages (not public)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "canonical" src/app/ --include="*.tsx" | grep -v node_modules | grep -v admin` returns at least 9 results (8 new + 1 existing category page)
    - `next build` succeeds without errors
  </acceptance_criteria>
  <done>All indexable public pages have alternates.canonical set. Noindex pages (search, flexible compare) correctly omit canonical. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'JSON.stringify(jsonLd)' src/app/ --include="*.tsx" | grep -v 'replace'` returns 0 results
2. `grep -rn 'canonical' src/app/ --include="*.tsx" | grep -v node_modules | grep -v admin` returns 9+ results
3. `npx next build` succeeds
4. Sitemap at src/app/sitemap.ts already covers all routes (homepage, 5 categories, all providers, all collections, all blog posts) — verify file exists and is unchanged
</verification>

<success_criteria>
- Zero unescaped JSON.stringify(jsonLd) calls remain in public pages
- All 9 indexable public page types have alternates.canonical in metadata
- Build passes without errors
- Sitemap coverage confirmed (no changes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-and-collections/09-01-SUMMARY.md`
</output>
