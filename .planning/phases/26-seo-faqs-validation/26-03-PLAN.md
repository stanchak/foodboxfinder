---
phase: 26-seo-faqs-validation
plan: 03
type: execute
wave: 2
depends_on: [26-01, 26-02]
files_modified:
  - prisma/scripts/26-generate-meta-faqs.ts
  - prisma/scripts/26-affiliate-validate.ts
autonomous: true
requirements: [R26.1, R26.2, R26.3, R26.4, R26.5, R26.6, R26.7]

must_haves:
  truths:
    - "All providers have metaTitle and metaDescription populated"
    - "All providers have at least 2 ProviderFaq records"
    - "At least 20 providers have affiliateUrl set"
    - "All providers have lastVerifiedAt timestamp"
    - "FAQ JSON-LD renders on provider detail pages with real FAQ data"
    - "next build passes with no errors"
  artifacts:
    - path: "prisma/scripts/26-generate-meta-faqs.ts"
      provides: "Tested at-scale SEO meta and FAQ script"
    - path: "prisma/scripts/26-affiliate-validate.ts"
      provides: "Tested at-scale affiliate and validation script"
  key_links:
    - from: "Provider.metaTitle"
      to: "generateMetadata() in src/app/providers/[slug]/page.tsx"
      via: "provider.metaTitle ?? fallback"
      pattern: "provider\\.metaTitle"
    - from: "ProviderFaq records"
      to: "FAQPage JSON-LD in src/app/providers/[slug]/page.tsx"
      via: "provider.faqs.map in JSON-LD script tag"
      pattern: "FAQPage"
---

<objective>
Run both scripts at full scale across all ~117 providers, verify complete coverage, and confirm FAQ JSON-LD renders correctly on provider detail pages. This is the final verification plan for Phase 26.

Purpose: Execute the batch operations built in Plans 01 and 02, then validate all Phase 26 success criteria are met including R26.7 (FAQ JSON-LD rendering) and the overall `next build` check.

Output: All providers fully populated with meta, FAQs, affiliate URLs, and validation timestamps. Build passes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-seo-faqs-validation/26-01-SUMMARY.md
@.planning/phases/26-seo-faqs-validation/26-02-SUMMARY.md
@prisma/schema.prisma
@src/app/providers/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run meta + FAQ generation at full scale</name>
  <files>prisma/scripts/26-generate-meta-faqs.ts</files>
  <read_first>
    - .planning/phases/26-seo-faqs-validation/26-01-SUMMARY.md (what was built, any issues)
    - prisma/scripts/26-generate-meta-faqs.ts (the script to run)
  </read_first>
  <action>
Run the SEO meta + FAQ generation script across ALL providers.

**Step 1: Try xAI first (if credits available)**
```bash
npx tsx prisma/scripts/26-generate-meta-faqs.ts --limit=2
```
If this succeeds (no 429 errors), run full batch:
```bash
npx tsx prisma/scripts/26-generate-meta-faqs.ts
```

**Step 2: If xAI fails with 429, use template-only mode**
```bash
npx tsx prisma/scripts/26-generate-meta-faqs.ts --template-only
```
This MUST work regardless of API availability — it is the guaranteed path.

**Step 3: Verify coverage**
After the run, check counts:
- Count providers with non-null metaTitle: should be ALL active providers (~116)
- Count providers with non-null metaDescription: should be ALL active providers (~116)
- Count providers with 2+ ProviderFaq records: should be ALL active providers (~116)

If any providers were skipped or failed, re-run with `--slug=X` for each failed slug.

**Step 4: Spot-check quality**
Pick 5 providers across categories and verify:
- metaTitle contains provider name and is under 70 chars
- metaDescription is under 160 chars and describes the provider
- FAQs have real answers (not empty or placeholder)
- FAQ answers reference actual provider data (pricing, dietary tags, flexibility)

**Expected totals:**
- ~99 providers get new metaTitle/metaDescription (18 hand-crafted already have them)
- ~117 providers get 3 ProviderFaq records each (only 18 had them before — even those should get FAQs now since the CONTEXT says "All ~117 providers need FAQs")
- 0 providers should be skipped entirely
  </action>
  <verify>
    <automated>npx tsx -e "
import 'dotenv/config';
import { config } from 'dotenv';
import { PrismaClient } from './src/generated/prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
config({ path: '.env.local' });
const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! });
const prisma = new PrismaClient({ adapter });
async function check() {
  const total = await prisma.provider.count({ where: { status: { not: 'DISCONTINUED' } } });
  const withMeta = await prisma.provider.count({ where: { status: { not: 'DISCONTINUED' }, metaTitle: { not: null } } });
  const withFaqs = await prisma.provider.count({ where: { status: { not: 'DISCONTINUED' }, faqs: { some: {} } } });
  const faqCount = await prisma.providerFaq.count();
  console.log('Total active providers:', total);
  console.log('With metaTitle:', withMeta);
  console.log('With FAQs:', withFaqs);
  console.log('Total FAQ records:', faqCount);
  console.log('Meta coverage:', Math.round(withMeta/total*100) + '%');
  console.log('FAQ coverage:', Math.round(withFaqs/total*100) + '%');
  if (withMeta < total) console.log('FAIL: Not all providers have metaTitle');
  if (withFaqs < total) console.log('FAIL: Not all providers have FAQs');
  if (withMeta === total && withFaqs === total) console.log('PASS: 100% coverage');
  await prisma.$disconnect();
}
check();
" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All active providers (~116) have non-null metaTitle
    - All active providers (~116) have non-null metaDescription
    - All active providers have at least 2 ProviderFaq records
    - Total ProviderFaq count is at least 300 (116 providers x ~3 FAQs each = ~348)
    - No provider has metaTitle longer than 70 chars
    - No provider has metaDescription longer than 160 chars
  </acceptance_criteria>
  <done>
    100% meta coverage (all active providers have metaTitle and metaDescription). 100% FAQ coverage (all active providers have 2+ ProviderFaq records).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run affiliate + validation at full scale, verify FAQ JSON-LD, confirm build</name>
  <files>prisma/scripts/26-affiliate-validate.ts</files>
  <read_first>
    - .planning/phases/26-seo-faqs-validation/26-02-SUMMARY.md (what was built, any issues)
    - prisma/scripts/26-affiliate-validate.ts (the script to run)
    - src/app/providers/[slug]/page.tsx (lines 190-206 — FAQ JSON-LD rendering)
  </read_first>
  <action>
**Step 1: Run affiliate + validation script**
```bash
npx tsx prisma/scripts/26-affiliate-validate.ts --template-only
```
Use `--template-only` since this is primarily about setting known affiliate URLs and timestamps. xAI validation is a nice-to-have.

If xAI credits are available, try without --template-only first:
```bash
npx tsx prisma/scripts/26-affiliate-validate.ts --limit=2
```

**Step 2: Verify affiliate coverage**
Check how many providers now have affiliateUrl set. Expected: at least 20+ (18 pre-existing + new ones from KNOWN_AFFILIATE_URLS map).

**Step 3: Verify lastVerifiedAt coverage**
ALL active providers should now have lastVerifiedAt set.

**Step 4: Review validation warnings**
The script outputs validation warnings at the end. Review them:
- Providers with missing descriptions: should be 0 (Phase 24 filled them all)
- Providers with missing plans: should be 0 (Phase 25 filled them all)
- Providers with invalid website URLs: flag for manual review

**Step 5: Verify FAQ JSON-LD renders (R26.7)**
The provider detail page (`src/app/providers/[slug]/page.tsx`) already renders FAQ JSON-LD when `provider.faqs.length > 0`. Since we just created FAQs for all providers, this should now render on every provider page.

Verify by running next build (which statically generates pages) — the build process will exercise the FAQ JSON-LD code path.

**Step 6: Run next build (R26.7 + overall verification)**
```bash
npx next build
```
This verifies:
- All provider pages generate successfully with new meta data
- FAQ JSON-LD structured data renders without errors
- No TypeScript or runtime errors from any changes
- generateMetadata() picks up the new metaTitle/metaDescription values

If build fails, diagnose and fix. Common issues:
- metaTitle/metaDescription with special characters that break JSON-LD
- FAQ answers with unescaped quotes
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - At least 30 providers have non-null affiliateUrl (18 pre-existing + 12+ new)
    - All active providers have lastVerifiedAt set to a timestamp within the last hour
    - Validation warnings report is printed (may have 0 warnings if all data is clean)
    - `next build` completes successfully with exit code 0
    - Build output shows all provider pages generated (static generation)
    - No TypeScript errors in build output
  </acceptance_criteria>
  <done>
    Affiliate URLs set for 30+ providers. lastVerifiedAt set for all active providers. FAQ JSON-LD confirmed rendering via successful next build. Zero build errors.
  </done>
</task>

</tasks>

<verification>
- 100% metaTitle/metaDescription coverage across all active providers
- 100% FAQ coverage (all providers have 2+ FAQs)
- 30+ providers have affiliateUrl
- 100% lastVerifiedAt coverage
- `next build` passes successfully
- FAQ JSON-LD renders on provider detail pages (confirmed by build success)
</verification>

<success_criteria>
- All Phase 26 success criteria met:
  - All providers have metaTitle and metaDescription
  - All providers have at least 2 FAQs
  - Affiliate URLs populated for major providers
  - lastVerifiedAt set for all validated providers
  - No critical data inaccuracies remain
  - FAQ JSON-LD renders on provider detail pages
  - `next build` passes
- v3.0 Data Completeness & Market Coverage milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/26-seo-faqs-validation/26-03-SUMMARY.md`
</output>
