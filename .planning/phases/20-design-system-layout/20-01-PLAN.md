---
phase: 20-design-system-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/categories.ts
autonomous: true
requirements: [DS-01]
must_haves:
  truths:
    - "Tailwind utility classes bg-primary-500, bg-accent-500, text-primary-700 generate valid CSS"
    - "The site renders with a white background regardless of OS dark mode setting"
    - "Category slugs map bidirectionally to CategoryType enum values"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Theme tokens for primary green, accent coral, star amber, shadows, radii"
      contains: "--color-primary-500"
    - path: "src/lib/categories.ts"
      provides: "CategoryType to slug/label mapping"
      exports: ["CATEGORY_MAP", "getCategoryBySlug", "getSlugByCategory", "CATEGORY_NAV_ITEMS"]
    - path: "src/app/page.tsx"
      provides: "Clean placeholder homepage without dark mode classes"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "Tailwind @theme tokens generating utility classes"
      pattern: "@theme"
    - from: "src/lib/categories.ts"
      to: "src/generated/prisma/enums.ts"
      via: "CategoryType import"
      pattern: "import.*CategoryType.*from.*@/generated/prisma"
---

<objective>
Establish the Tailwind CSS 4 theme foundation and category mapping utility for FoodBoxFinder.

Purpose: Every component and page in subsequent phases depends on brand color tokens, typography, shadow, and radius values being defined in the theme. The category mapping utility is used by header nav, footer nav, and all category-related pages.

Output: Branded globals.css with @theme tokens, category mapping utility, and a clean placeholder homepage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-design-system-layout/20-CONTEXT.md
@.planning/phases/20-design-system-layout/20-RESEARCH.md
@src/app/globals.css
@src/app/page.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Tailwind CSS 4 theme tokens and clean up dark mode</name>
  <files>src/app/globals.css, src/app/page.tsx</files>
  <read_first>
    - src/app/globals.css (current state with dark mode block and font-family override)
    - src/app/page.tsx (current state with dark: classes to remove)
    - src/app/layout.tsx (font variable names and body classes)
    - .planning/phases/20-design-system-layout/20-RESEARCH.md (complete @theme block example in Code Examples section)
  </read_first>
  <action>
    Replace the contents of `src/app/globals.css` with the complete theme. The file must contain exactly these sections:

    1. `@import "tailwindcss";` at the top (Tailwind CSS 4 syntax, NOT @tailwind directives)

    2. `:root` block with:
       - `--background: #ffffff;`
       - `--foreground: #111827;`

    3. `@theme inline` block with:
       - `--color-background: var(--background);`
       - `--color-foreground: var(--foreground);`
       - `--font-sans: var(--font-geist-sans);`
       - `--font-mono: var(--font-geist-mono);`

    4. `@theme` block (NOT inline) with ALL of these tokens (per D-01, D-08, D-09, D-10):

       Primary green palette (D-01 -- warm green, food-fresh, appetizing):
       - `--color-primary-50` through `--color-primary-950` (11 stops) using OKLCH values:
         - 50: `oklch(0.982 0.018 155.826)`
         - 100: `oklch(0.962 0.044 156.743)`
         - 200: `oklch(0.925 0.084 155.995)`
         - 300: `oklch(0.871 0.15 154.449)`
         - 400: `oklch(0.792 0.209 151.711)`
         - 500: `oklch(0.723 0.219 149.579)`
         - 600: `oklch(0.627 0.194 149.214)`
         - 700: `oklch(0.527 0.154 150.069)`
         - 800: `oklch(0.448 0.119 151.328)`
         - 900: `oklch(0.393 0.095 152.535)`
         - 950: `oklch(0.266 0.065 152.934)`

       Accent coral/orange palette (D-01 -- CTAs, attention):
       - `--color-accent-50` through `--color-accent-950` (11 stops) using OKLCH values:
         - 50: `oklch(0.98 0.016 73.684)`
         - 100: `oklch(0.954 0.038 75.164)`
         - 200: `oklch(0.901 0.076 70.697)`
         - 300: `oklch(0.837 0.128 66.29)`
         - 400: `oklch(0.75 0.183 55.934)`
         - 500: `oklch(0.705 0.213 47.604)`
         - 600: `oklch(0.646 0.222 41.116)`
         - 700: `oklch(0.553 0.195 38.402)`
         - 800: `oklch(0.47 0.157 37.304)`
         - 900: `oklch(0.408 0.123 38.172)`
         - 950: `oklch(0.266 0.079 36.259)`

       Star rating colors (D-11):
       - `--color-star: oklch(0.769 0.188 70.08);`
       - `--color-star-empty: oklch(0.872 0.01 258.338);`

       Shadows (D-08):
       - `--shadow-card: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);`
       - `--shadow-card-hover: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);`
       - `--shadow-header: 0 1px 3px 0 rgb(0 0 0 / 0.05);`

       Border radius (D-08, D-09, D-10):
       - `--radius-card: 0.75rem;` (rounded-xl equivalent)
       - `--radius-button: 0.5rem;` (rounded-lg equivalent)
       - `--radius-badge: 9999px;` (rounded-full equivalent)
       - `--radius-input: 0.5rem;` (rounded-lg equivalent)

    5. `body` rule with ONLY:
       - `background: var(--background);`
       - `color: var(--foreground);`
       - Do NOT include `font-family: Arial, Helvetica, sans-serif;` (per Pitfall 6 from research -- this overrides the Geist Sans font. The `--font-sans` theme token handles font via Tailwind `font-sans` utility.)

    6. REMOVE entirely: the `@media (prefers-color-scheme: dark)` block (per D-03 -- light mode only)

    Then replace `src/app/page.tsx` with a clean FoodBoxFinder placeholder:
    - Remove the `import Image from "next/image";` (no longer needed)
    - Remove ALL `dark:` Tailwind classes and all references to `dark:bg-black`, `dark:text-zinc-50`, `dark:invert`, etc.
    - Replace the entire component body with a simple centered placeholder:
      - A `<div>` with `className="flex flex-1 items-center justify-center"`
      - Inside: `<div className="text-center">` containing:
        - `<h1 className="text-4xl font-bold text-primary-700">FoodBoxFinder</h1>`
        - `<p className="mt-2 text-lg text-gray-600">Find your perfect food box subscription</p>`
    - Keep it as a default export named `Home`
    - No metadata export needed here (layout.tsx handles base metadata; Phase 30 will add homepage metadata)
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `@theme {` (non-inline theme block)
    - src/app/globals.css contains `--color-primary-500: oklch(0.723 0.219 149.579)`
    - src/app/globals.css contains `--color-accent-500: oklch(0.705 0.213 47.604)`
    - src/app/globals.css contains `--color-star: oklch(0.769 0.188 70.08)`
    - src/app/globals.css contains `--shadow-card:`
    - src/app/globals.css contains `--radius-card: 0.75rem`
    - src/app/globals.css does NOT contain `prefers-color-scheme: dark`
    - src/app/globals.css does NOT contain `font-family: Arial`
    - src/app/page.tsx does NOT contain `dark:`
    - src/app/page.tsx contains `text-primary-700`
    - src/app/page.tsx contains `FoodBoxFinder`
    - `npx next build` exits with code 0
  </acceptance_criteria>
  <done>
    Tailwind CSS 4 theme tokens (primary green 50-950, accent coral 50-950, star colors, shadows, radii) are defined in globals.css via @theme. Dark mode is removed. Placeholder homepage uses theme colors. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create category mapping utility</name>
  <files>src/lib/categories.ts</files>
  <read_first>
    - prisma/schema.prisma (CategoryType enum values: MEAL_KIT, PREPARED_MEAL, PROTEIN_BOX, PRODUCE_BOX, SPECIALTY)
    - src/generated/prisma/enums.ts (confirm CategoryType export path)
    - src/lib/queries.ts (import pattern for CategoryType: `import type { CategoryType } from "@/generated/prisma/client"`)
    - .planning/phases/20-design-system-layout/20-RESEARCH.md (Pattern 4: Category Mapping Utility example)
  </read_first>
  <action>
    Create `src/lib/categories.ts` with the canonical category mapping used by header nav, footer nav, and all category pages.

    The file must contain:

    1. Import: `import type { CategoryType } from "@/generated/prisma/client";`

    2. `CATEGORY_MAP` constant (exported, typed as `Record<CategoryType, { slug: string; label: string; description: string }>`):
       - `MEAL_KIT: { slug: "meal-kits", label: "Meal Kits", description: "Fresh ingredients with chef-designed recipes" }`
       - `PREPARED_MEAL: { slug: "prepared-meals", label: "Prepared Meals", description: "Ready-to-eat meals delivered to your door" }`
       - `PROTEIN_BOX: { slug: "protein-boxes", label: "Protein Boxes", description: "Premium meats and protein sources" }`
       - `PRODUCE_BOX: { slug: "produce-boxes", label: "Produce Boxes", description: "Farm-fresh fruits and vegetables" }`
       - `SPECIALTY: { slug: "specialty", label: "Specialty", description: "Unique and curated food experiences" }`

    3. `getCategoryBySlug` function (exported):
       - Signature: `(slug: string): { key: CategoryType; slug: string; label: string; description: string } | undefined`
       - Iterates over `Object.entries(CATEGORY_MAP)` and finds the entry where `v.slug === slug`
       - Returns `{ key: key as CategoryType, ...value }` or `undefined`

    4. `getSlugByCategory` function (exported):
       - Signature: `(category: CategoryType): string`
       - Returns `CATEGORY_MAP[category].slug`

    5. `CATEGORY_NAV_ITEMS` constant (exported):
       - Type: `Array<{ slug: string; label: string }>`
       - Computed from `Object.values(CATEGORY_MAP).map(({ slug, label }) => ({ slug, label }))`
       - This is a convenience export for Header and Footer nav rendering

    All exports must be named exports (no default export). File must NOT import Prisma client or any server-only modules (it's a pure mapping utility usable in both Server and Client Components).
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/categories.ts exists
    - src/lib/categories.ts contains `export const CATEGORY_MAP`
    - src/lib/categories.ts contains `export function getCategoryBySlug`
    - src/lib/categories.ts contains `export function getSlugByCategory`
    - src/lib/categories.ts contains `export const CATEGORY_NAV_ITEMS`
    - src/lib/categories.ts contains `MEAL_KIT:` with `slug: "meal-kits"`
    - src/lib/categories.ts contains `PREPARED_MEAL:` with `slug: "prepared-meals"`
    - src/lib/categories.ts contains `PROTEIN_BOX:` with `slug: "protein-boxes"`
    - src/lib/categories.ts contains `PRODUCE_BOX:` with `slug: "produce-boxes"`
    - src/lib/categories.ts contains `SPECIALTY:` with `slug: "specialty"`
    - src/lib/categories.ts contains `import type { CategoryType } from "@/generated/prisma/client"`
    - src/lib/categories.ts does NOT contain `import { prisma` or `import "server-only"`
    - `npx next build` exits with code 0
  </acceptance_criteria>
  <done>
    Category mapping utility provides bidirectional CategoryType-to-slug conversion and a CATEGORY_NAV_ITEMS array for nav rendering. All 5 categories are mapped. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- `src/app/globals.css` contains `@theme` block with primary and accent color scales
- No dark mode CSS remains in globals.css or page.tsx
- `src/lib/categories.ts` exports CATEGORY_MAP with all 5 CategoryType values
</verification>

<success_criteria>
1. Theme tokens generate valid Tailwind utilities (verified by successful build using `text-primary-700` in page.tsx)
2. Site renders white background on all OS themes (dark mode media query removed)
3. Category mapping utility exists and is type-safe against CategoryType enum
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-system-layout/20-01-SUMMARY.md`
</output>
