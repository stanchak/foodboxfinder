---
phase: 20-design-system-layout
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/app/layout.tsx
  - src/components/Header.tsx
  - src/components/MobileNav.tsx
  - src/components/Footer.tsx
autonomous: true
requirements: [DS-02]
must_haves:
  truths:
    - "Every page renders inside a layout with a visible header and footer"
    - "The header shows the FoodBoxFinder logo, 5 category nav links on desktop, and a hamburger button on mobile"
    - "The mobile hamburger opens a slide-out drawer with all navigation links"
    - "The header sticks to the top of the viewport on scroll"
    - "The footer displays category links, resource links, legal links, and copyright"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with Header, main, Footer structure"
      contains: "import Header"
    - path: "src/components/Header.tsx"
      provides: "Server Component with logo, desktop nav, search placeholder, MobileNav island"
      contains: "export default function Header"
    - path: "src/components/MobileNav.tsx"
      provides: "Client Component with hamburger toggle and slide-out drawer"
      contains: "use client"
    - path: "src/components/Footer.tsx"
      provides: "Server Component with 3-column footer"
      contains: "export default function Footer"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Header.tsx"
      via: "import and render in body"
      pattern: "import Header from.*@/components/Header"
    - from: "src/app/layout.tsx"
      to: "src/components/Footer.tsx"
      via: "import and render in body"
      pattern: "import Footer from.*@/components/Footer"
    - from: "src/components/Header.tsx"
      to: "src/components/MobileNav.tsx"
      via: "import Client Component island"
      pattern: "import MobileNav from.*@/components/MobileNav"
    - from: "src/components/Header.tsx"
      to: "src/lib/categories.ts"
      via: "CATEGORY_NAV_ITEMS for nav links"
      pattern: "import.*CATEGORY_NAV_ITEMS.*from.*@/lib/categories"
---

<objective>
Build the responsive root layout shell with Header, MobileNav, and Footer components.

Purpose: Every page in the application renders inside this layout. The header provides site-wide navigation (5 category links, search placeholder, mobile hamburger). The footer provides secondary navigation and legal links. This is the structural frame for all subsequent phases.

Output: Root layout wrapping Header + main + Footer, with responsive behavior from 375px to 1440px.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-design-system-layout/20-CONTEXT.md
@.planning/phases/20-design-system-layout/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 -->

From src/lib/categories.ts (created in Plan 01):
```typescript
import type { CategoryType } from "@/generated/prisma/client";

export const CATEGORY_MAP: Record<CategoryType, { slug: string; label: string; description: string }>;
export function getCategoryBySlug(slug: string): { key: CategoryType; slug: string; label: string; description: string } | undefined;
export function getSlugByCategory(category: CategoryType): string;
export const CATEGORY_NAV_ITEMS: Array<{ slug: string; label: string }>;
```

From src/app/globals.css (created in Plan 01):
- Theme tokens available: bg-primary-*, text-primary-*, bg-accent-*, text-accent-*, shadow-card, shadow-header, etc.
- Font: font-sans maps to Geist Sans via --font-sans theme token
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header, MobileNav, and Footer components</name>
  <files>src/components/Header.tsx, src/components/MobileNav.tsx, src/components/Footer.tsx</files>
  <read_first>
    - src/app/globals.css (theme tokens available -- primary, accent, shadow-header)
    - src/lib/categories.ts (CATEGORY_NAV_ITEMS export for nav link generation)
    - .planning/phases/20-design-system-layout/20-RESEARCH.md (Pattern 1: Server Component Layout with Client Island, anti-patterns section)
    - .planning/phases/20-design-system-layout/20-CONTEXT.md (decisions D-04 through D-07, D-13, D-15)
  </read_first>
  <action>
    Create the `src/components/` directory and three files.

    **src/components/Header.tsx** (Server Component -- NO "use client"):

    Per D-04, D-07, D-13:
    - `<header>` with classes: `sticky top-0 z-40 bg-white/95 backdrop-blur-sm border-b border-gray-100 shadow-header`
    - Inside: `<nav>` with classes: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
    - Inside nav: a flex container (`flex items-center justify-between h-16`)

    Left section:
    - `<Link href="/">` wrapping the site name
    - Text: "FoodBoxFinder" with classes `text-xl font-bold text-primary-700`
    - No logo image (text-only for now)

    Center section (desktop nav, hidden on mobile):
    - `<div className="hidden lg:flex lg:items-center lg:gap-6">`
    - Import `CATEGORY_NAV_ITEMS` from `@/lib/categories`
    - Map over `CATEGORY_NAV_ITEMS` to render `<Link>` elements:
      - `href={`/${item.slug}`}` (e.g., `/meal-kits`, `/prepared-meals`)
      - Classes: `text-sm font-medium text-gray-700 hover:text-primary-600 transition-colors`
      - `key={item.slug}`

    Right section:
    - A search placeholder: `<Link href="/search" className="hidden sm:flex items-center gap-2 text-sm text-gray-500 hover:text-gray-700 transition-colors">` containing a search SVG icon (magnifying glass, 20x20, `currentColor` stroke) and the text "Search..."
    - Import and render `<MobileNav />` (the Client Component island)

    Import `Link` from `"next/link"` and `MobileNav` from `"@/components/MobileNav"`.

    **src/components/MobileNav.tsx** (Client Component -- MUST have "use client"):

    Per D-05:
    - `"use client";` at the top
    - `import { useState } from "react";`
    - `import Link from "next/link";`
    - `import { CATEGORY_NAV_ITEMS } from "@/lib/categories";`

    State: `const [isOpen, setIsOpen] = useState(false);`

    Hamburger button (visible only on mobile/tablet):
    - `<button>` with classes: `lg:hidden p-2 text-gray-700 hover:text-primary-600`
    - `aria-label={isOpen ? "Close menu" : "Open menu"}`
    - `aria-expanded={isOpen}`
    - `onClick={() => setIsOpen(!isOpen)}`
    - SVG icon: 3-line hamburger when closed (three horizontal lines), X when open
    - Icon size: 24x24, `stroke="currentColor"` `strokeWidth={2}`

    Slide-out drawer (when `isOpen` is true):
    - Backdrop overlay: `<div>` with classes `fixed inset-0 z-50 bg-black/20` and `onClick={() => setIsOpen(false)}`
    - Drawer panel: `<div>` with classes `fixed top-0 right-0 z-50 h-full w-72 bg-white shadow-xl transform transition-transform duration-300` (translate-x-0 when open, translate-x-full when closed -- use conditional class)
    - Inside drawer panel:
      - Close button at top-right: `<button onClick={() => setIsOpen(false)} className="absolute top-4 right-4 p-2 text-gray-500 hover:text-gray-700" aria-label="Close menu">` with X SVG icon
      - Nav section with `<nav className="mt-16 px-6 space-y-1">`
      - Map `CATEGORY_NAV_ITEMS` to `<Link>` elements, each with:
        - `href={`/${item.slug}`}`
        - `className="block px-3 py-2.5 text-base font-medium text-gray-700 hover:bg-primary-50 hover:text-primary-700 rounded-lg transition-colors"`
        - `onClick={() => setIsOpen(false)}` (close drawer on navigation)
      - Divider: `<hr className="my-4 border-gray-200" />`
      - Additional links below divider:
        - `<Link href="/search">` with text "Search" (same styling as category links)
        - `<Link href="/compare">` with text "Compare" (same styling)
        - `<Link href="/blog">` with text "Blog" (same styling)

    When `isOpen` is false, render nothing for the drawer (conditionally render with `{isOpen && (...)}`).

    **src/components/Footer.tsx** (Server Component -- NO "use client"):

    Per D-06:
    - `<footer>` with classes: `bg-gray-50 border-t border-gray-200 mt-auto`
    - Inside: `<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12">`
    - Grid: `<div className="grid grid-cols-2 gap-8 md:grid-cols-4">`

    Column 1 - Categories:
    - `<h3 className="text-sm font-semibold text-gray-900">Categories</h3>`
    - `<ul className="mt-4 space-y-3">` with `CATEGORY_NAV_ITEMS` mapped to `<li>` > `<Link>` elements
    - Link classes: `text-sm text-gray-600 hover:text-primary-600 transition-colors`
    - `href={`/${item.slug}`}`

    Column 2 - Resources:
    - `<h3>` same style as above, text "Resources"
    - Links: Blog (`/blog`), Collections (`/best`), About (`/about`), How It Works (`/#how-it-works`)
    - Same link styling as column 1

    Column 3 - Legal:
    - `<h3>` same style, text "Legal"
    - Links: Privacy Policy (`/privacy`), Terms of Service (`/terms`)
    - Same link styling

    Column 4 - Brand:
    - `<h3 className="text-sm font-semibold text-gray-900">FoodBoxFinder</h3>`
    - `<p className="mt-4 text-sm text-gray-600">Helping you find the perfect food box subscription for your dietary needs and budget.</p>`

    Below the grid:
    - `<div className="mt-12 border-t border-gray-200 pt-8">`
    - `<p className="text-center text-sm text-gray-500">&copy; {new Date().getFullYear()} FoodBoxFinder. All rights reserved.</p>`

    Import `Link` from `"next/link"` and `CATEGORY_NAV_ITEMS` from `"@/lib/categories"`.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Header.tsx exists and contains `export default function Header`
    - src/components/Header.tsx contains `sticky top-0`
    - src/components/Header.tsx contains `import MobileNav from "@/components/MobileNav"`
    - src/components/Header.tsx contains `import { CATEGORY_NAV_ITEMS } from "@/lib/categories"`
    - src/components/Header.tsx contains `FoodBoxFinder`
    - src/components/Header.tsx does NOT contain `"use client"`
    - src/components/MobileNav.tsx exists and contains `"use client"` on the first line
    - src/components/MobileNav.tsx contains `useState`
    - src/components/MobileNav.tsx contains `aria-label`
    - src/components/MobileNav.tsx contains `aria-expanded`
    - src/components/MobileNav.tsx contains `CATEGORY_NAV_ITEMS`
    - src/components/Footer.tsx exists and contains `export default function Footer`
    - src/components/Footer.tsx contains `CATEGORY_NAV_ITEMS`
    - src/components/Footer.tsx contains `Categories`
    - src/components/Footer.tsx contains `Resources`
    - src/components/Footer.tsx contains `Legal`
    - src/components/Footer.tsx does NOT contain `"use client"`
    - `npx next build` exits with code 0
  </acceptance_criteria>
  <done>
    Header (Server), MobileNav (Client), and Footer (Server) components exist in src/components/. Header has sticky positioning, desktop nav with 5 category links, search placeholder, and MobileNav island. MobileNav has hamburger toggle and slide-out drawer. Footer has 4 columns. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor root layout to use Header and Footer</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current state -- Geist font loading, minimal body)
    - src/components/Header.tsx (just created -- confirm import path)
    - src/components/Footer.tsx (just created -- confirm import path)
    - .planning/phases/20-design-system-layout/20-RESEARCH.md (Pattern 1 layout example)
    - .planning/phases/20-design-system-layout/20-CONTEXT.md (D-13 for max-w-7xl, D-02 for typography)
  </read_first>
  <action>
    Update `src/app/layout.tsx` to:

    1. Keep existing imports: `Geist`, `Geist_Mono` from `"next/font/google"`, `"./globals.css"`
    2. Keep existing font declarations (`geistSans`, `geistMono`) exactly as they are
    3. Add new imports:
       - `import Header from "@/components/Header";`
       - `import Footer from "@/components/Footer";`

    4. Update `metadata` export:
       - `title: { default: "FoodBoxFinder - Discover & Compare Food Box Subscriptions", template: "%s | FoodBoxFinder" }`
       - `description: "Find and compare the best meal kits, prepared meals, protein boxes, and produce boxes. Honest reviews, transparent pricing, and side-by-side comparisons."`

    5. Update the `<html>` element:
       - Keep existing className: `` `${geistSans.variable} ${geistMono.variable} h-full antialiased` ``
       - Keep `lang="en"`

    6. Update `<body>` element:
       - className: `"min-h-full flex flex-col bg-white text-gray-900 font-sans"`
       - Note: `font-sans` maps to Geist Sans via the `--font-sans` theme token in globals.css
       - Note: `bg-white text-gray-900` ensures light mode colors per D-03

    7. Body content structure:
       ```
       <Header />
       <main className="flex-1">{children}</main>
       <Footer />
       ```

    The layout must remain a Server Component (no "use client"). The `import type { Metadata }` stays at the top.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `import Header from "@/components/Header"`
    - src/app/layout.tsx contains `import Footer from "@/components/Footer"`
    - src/app/layout.tsx contains `<Header />`
    - src/app/layout.tsx contains `<Footer />`
    - src/app/layout.tsx contains `<main className="flex-1">{children}</main>`
    - src/app/layout.tsx contains `bg-white text-gray-900 font-sans`
    - src/app/layout.tsx contains `FoodBoxFinder - Discover & Compare Food Box Subscriptions`
    - src/app/layout.tsx contains `template: "%s | FoodBoxFinder"`
    - src/app/layout.tsx does NOT contain `"use client"`
    - `npx next build` exits with code 0
  </acceptance_criteria>
  <done>
    Root layout wraps Header + main + Footer. Every page now renders inside the layout shell with sticky header and footer. Site-wide metadata is set with title template. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Header.tsx is a Server Component importing MobileNav Client Component island
- MobileNav.tsx has "use client" directive and useState for drawer toggle
- Footer.tsx is a Server Component with 4-column layout
- layout.tsx imports and renders Header, main with children, and Footer
- All nav links use CATEGORY_NAV_ITEMS from src/lib/categories.ts
</verification>

<success_criteria>
1. Root layout renders Header with sticky positioning, 5 category nav links (desktop), hamburger (mobile), search placeholder
2. Mobile hamburger opens a slide-out drawer with all navigation links
3. Footer renders 4 columns (Categories, Resources, Legal, Brand) with copyright
4. Layout uses max-w-7xl content width constraint per D-13
5. Build passes with all components integrated
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-system-layout/20-02-SUMMARY.md`
</output>
