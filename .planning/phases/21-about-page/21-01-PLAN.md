---
phase: 21-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
  - src/app/sitemap.ts
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05

must_haves:
  truths:
    - "User visits /about and sees a mission statement explaining what FoodBoxFinder is and who it serves"
    - "User sees a methodology section explaining 9-dimension filtering and side-by-side comparison"
    - "User sees an affiliate disclosure with editorial independence statement"
    - "/about has page title, meta description, and AboutPage JSON-LD structured data"
    - "/about is visually on-brand with Citrus Pop theme (orange primary, teal accent, Nunito fonts)"
    - "/about appears in sitemap.xml"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About page with mission, methodology, disclosure sections"
      min_lines: 100
      exports: ["metadata", "default"]
    - path: "src/app/sitemap.ts"
      provides: "Sitemap including /about entry"
      contains: "/about"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/Breadcrumbs.tsx"
      via: "import Breadcrumbs"
      pattern: "import Breadcrumbs from"
    - from: "src/app/sitemap.ts"
      to: "/about"
      via: "static pages array entry"
      pattern: "about"
---

<objective>
Build the /about page with mission statement, methodology explanation, and affiliate disclosure -- plus add /about to the sitemap for SEO.

Purpose: Give users a trust/credibility page that explains what FoodBoxFinder is, how providers are evaluated, and how the site earns revenue. Completes the v2.1 milestone.
Output: `src/app/about/page.tsx` (new), `src/app/sitemap.ts` (updated)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-about-page/21-CONTEXT.md

@src/app/best/page.tsx (pattern reference for metadata, JSON-LD, Breadcrumbs)
@src/app/blog/page.tsx (pattern reference for metadata, JSON-LD)
@src/app/sitemap.ts (file to update)
@src/components/Breadcrumbs.tsx (component to import)
@src/app/globals.css (Citrus Pop design tokens)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /about page with mission, methodology, and disclosure</name>
  <files>src/app/about/page.tsx</files>
  <read_first>
    - src/app/best/page.tsx (metadata + JSON-LD + Breadcrumbs pattern)
    - src/app/blog/page.tsx (metadata + JSON-LD pattern)
    - src/components/Breadcrumbs.tsx (import and usage pattern)
    - src/app/globals.css (Citrus Pop design tokens: --color-primary-*, --color-accent-*, --font-heading, --font-sans)
  </read_first>
  <action>
    Create `src/app/about/page.tsx` as a Server Component (no "use client"). Follow exact patterns from /best and /blog pages.

    **Metadata** (per ABOUT-04):
    - `export const metadata: Metadata` with:
      - title: "About FoodBoxFinder - Our Mission & Methodology"
      - description: "Learn how FoodBoxFinder helps you discover and compare 95+ food box subscriptions. Transparent methodology, honest reviews, and editorial independence."
      - alternates.canonical: "/about"
      - openGraph: title "About | FoodBoxFinder", description covering mission, type "website", url "https://foodboxfinder.com/about"

    **JSON-LD** (per ABOUT-04):
    - Create an `AboutPageJsonLd` component that renders a `<script type="application/ld+json">` tag
    - Use `dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}`
    - Schema:
      ```json
      {
        "@context": "https://schema.org",
        "@type": "AboutPage",
        "name": "About FoodBoxFinder",
        "description": "Learn how FoodBoxFinder helps you discover and compare food box subscriptions.",
        "url": "https://foodboxfinder.com/about",
        "publisher": {
          "@type": "Organization",
          "name": "FoodBoxFinder",
          "url": "https://foodboxfinder.com"
        }
      }
      ```

    **Breadcrumbs**:
    - Import `Breadcrumbs` from `@/components/Breadcrumbs`
    - Items: `[{ label: "Home", href: "/" }, { label: "About", href: "/about" }]`

    **Page Layout** -- use `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6` container (matches /best pattern):

    1. **Hero Section** (per ABOUT-01, ABOUT-05):
       - `<h1>` with classes `text-3xl font-extrabold tracking-tight text-neutral-900 sm:text-4xl font-heading` centered
       - Title text: "About FoodBoxFinder"
       - Subtitle `<p>` with `mt-3 text-lg text-neutral-600 max-w-2xl mx-auto` centered: "Your guide to discovering the perfect food box subscription."

    2. **Content sections** -- use `mt-12 max-w-3xl mx-auto space-y-12` wrapper for readable content width:

    3. **Mission Section** (per ABOUT-01):
       - Section heading: `<h2 className="text-2xl font-bold text-neutral-900 font-heading">Our Mission</h2>`
       - Content paragraphs with `mt-4 text-base text-neutral-700 leading-relaxed space-y-4`
       - Content themes: FoodBoxFinder is a free discovery platform helping consumers find the right food box subscription. There are 95+ providers across 5 categories (meal kits, prepared meals, protein boxes, produce boxes, specialty). The problem: too many choices, too little transparency. FoodBoxFinder solves this with structured comparison.

    4. **Methodology Section** (per ABOUT-02):
       - Section heading: `<h2 className="text-2xl font-bold text-neutral-900 font-heading">How We Compare</h2>`
       - Content explaining the 9-dimension filtering system: dietary needs, prep style, value tier, household size, flexibility, geography, protein source, cuisine type, and special features
       - Mention side-by-side comparison (up to 4 providers), transparent pricing, and real user reviews
       - Use a styled list or grid to display the filter dimensions visually -- e.g., a `grid grid-cols-2 sm:grid-cols-3 gap-3 mt-6` with each dimension in a `rounded-xl bg-primary-50 px-4 py-3 text-sm font-medium text-primary-800` badge-style card

    5. **Affiliate Disclosure Section** (per ABOUT-03):
       - Section heading: `<h2 className="text-2xl font-bold text-neutral-900 font-heading">How We Earn Revenue</h2>`
       - Content: FoodBoxFinder earns money through affiliate links. When you click through to a provider and make a purchase, we may earn a commission at no extra cost to you.
       - Editorial independence statement: "Our rankings, reviews, and recommendations are never influenced by affiliate partnerships. Every provider is evaluated using the same criteria, and our editorial team maintains full independence over all content."
       - Style the disclosure in a distinct card: `rounded-2xl bg-accent-50 border border-accent-200 p-6` to make it visually distinct and trustworthy (using teal accent per Citrus Pop)

    **Props pattern**: Use `Readonly<{}>` for any component prop types. Export `default function AboutPage()` as the page component.

    **Do NOT**: Add "use client", import Prisma or queries, add any database fetching, add a contact form. This is pure static content.
  </action>
  <acceptance_criteria>
    - File exists at src/app/about/page.tsx
    - Contains `export const metadata: Metadata`
    - metadata.title contains "About FoodBoxFinder"
    - metadata.alternates.canonical is "/about"
    - Contains openGraph with url "https://foodboxfinder.com/about"
    - Contains `"@type": "AboutPage"` in JSON-LD
    - Contains `"@type": "Organization"` in publisher JSON-LD
    - Contains `dangerouslySetInnerHTML` with `.replace(/</g, "\\u003c")` for XSS safety
    - Contains `import Breadcrumbs from "@/components/Breadcrumbs"`
    - Contains text "Our Mission" (h2 heading)
    - Contains text "How We Compare" (h2 heading)
    - Contains text "How We Earn Revenue" or "Affiliate" (disclosure heading)
    - Contains text "editorial independence" (editorial independence statement)
    - Contains text "affiliate" (affiliate disclosure)
    - Contains `font-heading` class (Citrus Pop Nunito heading font)
    - Contains `font-extrabold` class (matches existing page header pattern)
    - Contains `bg-accent-50` or `bg-accent-` (teal accent usage for Citrus Pop)
    - Contains `bg-primary-50` or `bg-primary-` (orange primary usage for Citrus Pop)
    - Does NOT contain "use client"
    - Does NOT contain "prisma" or "@/lib/db" or "@/lib/queries"
    - `export default function` is present (Server Component page export)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx tsc --noEmit src/app/about/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    /about page renders with mission statement, methodology explanation with 9 filter dimensions, affiliate disclosure with editorial independence statement, SEO metadata, AboutPage JSON-LD, Breadcrumbs, and Citrus Pop styling (orange primary, teal accent, Nunito fonts).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add /about to sitemap</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (current sitemap with static pages array)
  </read_first>
  <action>
    Edit `src/app/sitemap.ts` to add an /about entry to the `staticPages` array.

    Add this entry after the `/best` entry (last current static page) in the `staticPages` array:

    ```typescript
    {
      url: `${BASE_URL}/about`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.5,
    },
    ```

    Use `changeFrequency: "monthly"` (content rarely changes) and `priority: 0.5` (informational page, lower than discovery pages).

    No other changes to the file.
  </action>
  <acceptance_criteria>
    - src/app/sitemap.ts contains the string "/about"
    - The /about entry has changeFrequency "monthly"
    - The /about entry has priority 0.5
    - All existing sitemap entries are preserved unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -n "about" src/app/sitemap.ts</automated>
  </verify>
  <done>
    /about appears in sitemap.xml output alongside all existing static pages.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npx eslint src/app/about/page.tsx src/app/sitemap.ts` passes with no errors
3. `npm run build` completes successfully (about page statically generated)
4. Grep checks confirm all acceptance criteria strings are present in files
</verification>

<success_criteria>
- /about page exists with mission, methodology, and affiliate disclosure sections
- Page has proper SEO metadata and AboutPage JSON-LD structured data
- Page uses Citrus Pop theme (orange primary, teal accent, Nunito fonts)
- /about is included in sitemap.xml
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-about-page/21-01-SUMMARY.md`
</output>
