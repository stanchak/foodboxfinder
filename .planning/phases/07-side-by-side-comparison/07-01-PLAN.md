---
phase: 07-side-by-side-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries/providers.ts
  - src/components/ComparisonTable.tsx
  - src/app/compare/[versus]/page.tsx
  - src/app/compare/page.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, TRAY-01, TRAY-02, TRAY-03, TRAY-04]

must_haves:
  truths:
    - "ComparisonTable shows prepStyle, valueTier, householdFit, modelType, geography, flexibility, shippingNotes, and pros/cons fields for each provider"
    - "Rows where ALL compared providers have null/empty values for that field are hidden automatically"
    - "Visiting /compare/zebra-bites-vs-acme-meals redirects to /compare/acme-meals-vs-zebra-bites (alphabetical canonical order)"
    - "JSON-LD on both comparison pages uses XSS-safe rendering with .replace(/</g, '\\u003c')"
    - "Floating comparison tray allows adding/removing providers and navigates to comparison page"
  artifacts:
    - path: "src/lib/queries/providers.ts"
      provides: "getProvidersForComparison includes new provider fields"
      contains: "prepStyle"
    - path: "src/components/ComparisonTable.tsx"
      provides: "Full field matrix with N/A row hiding"
      contains: "allNa"
    - path: "src/app/compare/[versus]/page.tsx"
      provides: "Canonical slug ordering with redirect and XSS-safe JSON-LD"
      contains: "\\u003c"
    - path: "src/app/compare/page.tsx"
      provides: "XSS-safe JSON-LD"
      contains: "\\u003c"
  key_links:
    - from: "src/components/ComparisonTable.tsx"
      to: "src/lib/queries/providers.ts"
      via: "ComparisonProvider interface matches query select fields"
      pattern: "prepStyle.*valueTier.*modelType"
    - from: "src/app/compare/[versus]/page.tsx"
      to: "redirect"
      via: "next/navigation redirect for non-alphabetical slug order"
      pattern: "redirect.*compare"
---

<objective>
Update the existing comparison system to display new provider fields (prepStyle, valueTier, modelType, householdFit, geography, flexibility, shippingNotes, pros/cons) in the ComparisonTable, hide rows where all providers have N/A values, enforce canonical alphabetical slug ordering in comparison URLs, and fix JSON-LD XSS safety on both comparison pages.

Purpose: Complete the comparison feature with full field coverage from the Phase 1 schema extension, prevent duplicate content via canonical URLs, and ensure security compliance on structured data output.
Output: Updated query, ComparisonTable with N/A hiding, canonical redirect logic, XSS-safe JSON-LD on both comparison routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/queries/providers.ts
@src/components/ComparisonTable.tsx
@src/app/compare/[versus]/page.tsx
@src/app/compare/page.tsx
@prisma/schema.prisma

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/queries/providers.ts (getProvidersForComparison):
```typescript
export const getProvidersForComparison = cache(async (slugs: string[]) => {
  return prisma.provider.findMany({
    where: { slug: { in: slugs }, status: "ACTIVE" },
    include: {
      plans: { where: { active: true }, orderBy: { sortOrder: "asc" } },
      dietaryTags: true,
    },
  });
});
```

From src/components/ComparisonTable.tsx (ComparisonProvider interface):
```typescript
interface ComparisonProvider {
  id: string;
  name: string;
  slug: string;
  shortDescription: string | null;
  logoUrl: string | null;
  website: string;
  affiliateUrl: string | null;
  averageRating: number;
  reviewCount: number;
  minPricePerServingCents: number | null;
  maxPricePerServingCents: number | null;
  freeShipping: boolean;
  category: CategoryType;
  secondaryCategory: CategoryType | null;
  dietaryTags: Array<{ tag: DietaryTag }>;
  plans: ComparisonPlan[];
}
```

From prisma/schema.prisma (Provider new fields):
```prisma
modelType       String?
prepStyle       String?
valueTier       ValueTier?
householdFit    String?
geography       String?
shippingNotes   String?  @db.Text
flexibility     String?  @db.Text
prosJson        Json?    // JSON array of strings
consJson        Json?    // JSON array of strings
```

From src/lib/format.ts:
```typescript
export function formatPrice(cents: number | null): string;
export function formatPriceRange(minCents: number | null, maxCents: number | null): string;
```

XSS-safe JSON-LD pattern (established in Phase 04/05/06):
```typescript
dangerouslySetInnerHTML={{
  __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c"),
}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand query and ComparisonTable with new fields and N/A row hiding</name>
  <files>src/lib/queries/providers.ts, src/components/ComparisonTable.tsx</files>
  <read_first>
    - src/lib/queries/providers.ts
    - src/components/ComparisonTable.tsx
    - prisma/schema.prisma
    - src/lib/format.ts
    - src/lib/categories.ts
  </read_first>
  <action>
    **1. Update getProvidersForComparison query** in `src/lib/queries/providers.ts`:
    - Add a `select` clause (instead of relying on include-all) that explicitly includes all existing fields PLUS: `prepStyle`, `valueTier`, `modelType`, `householdFit`, `geography`, `shippingNotes`, `flexibility`, `prosJson`, `consJson`.
    - Keep existing `include` for `plans` and `dietaryTags` unchanged.

    **2. Update ComparisonProvider interface** in `src/components/ComparisonTable.tsx`:
    - Add these fields to the interface:
      - `prepStyle: string | null`
      - `valueTier: string | null` (use string since ValueTier enum renders as string)
      - `modelType: string | null`
      - `householdFit: string | null`
      - `geography: string | null`
      - `shippingNotes: string | null`
      - `flexibility: string | null`
      - `prosJson: unknown` (JSON field, parse as string array)
      - `consJson: unknown` (JSON field, parse as string array)

    **3. Add N/A row hiding logic**:
    - Create a helper function `isFieldEmpty(value: unknown): boolean` that returns true for `null`, `undefined`, empty string `""`, and empty arrays.
    - Create a wrapper component `HideableComparisonRow` that accepts the same props as `ComparisonRow` plus `values: unknown[]`. Before rendering, check if ALL values are empty via `isFieldEmpty`. If all are empty, return `null` (hide the row). Otherwise, render the existing `ComparisonRow`.

    **4. Add new field rows** to the ComparisonTable after the existing "Dietary Options" row and before the "Featured Plan" section header. Add a new section header "Provider Details" with these rows:
    - **Prep Style**: Display `provider.prepStyle ?? "N/A"`. Use `HideableComparisonRow` with `values={providers.map(p => p.prepStyle)}`.
    - **Value Tier**: Display `provider.valueTier` formatted as title case (BUDGET -> "Budget", MID -> "Mid-Range", PREMIUM -> "Premium", LUXURY -> "Luxury") or "N/A". Use title case map: `{ BUDGET: "Budget", MID: "Mid-Range", PREMIUM: "Premium", LUXURY: "Luxury" }`. Use `HideableComparisonRow`.
    - **Model Type**: Display `provider.modelType ?? "N/A"`. Use `HideableComparisonRow`.
    - **Household Fit**: Display `provider.householdFit ?? "N/A"`. Use `HideableComparisonRow`.
    - **Geography**: Display `provider.geography ?? "N/A"`. Use `HideableComparisonRow`.

    **5. Add Shipping & Flexibility section** after the existing "Featured Plan" section and before the "Flexibility" section:
    - Rename the existing "Flexibility" section header to "Plan Flexibility" (to distinguish from provider-level flexibility).
    - Add a new section header "Shipping & Details" before the existing "Free Shipping" row (move it there), and add:
      - **Shipping Notes**: Display `provider.shippingNotes ?? "N/A"`. Use `HideableComparisonRow`. Style as `text-sm text-gray-700 max-w-[200px] text-left`.

    Actually, simpler approach -- keep the existing row structure but ADD rows:
    - After "Dietary Options" row, add section header "Provider Details" and new rows for prepStyle, valueTier, modelType, householdFit, geography.
    - After the "Flexibility" section (canSkip, canCancel, totalPlans), add:
      - **Shipping Notes** row: `provider.shippingNotes ?? "N/A"`. Use `HideableComparisonRow`.
      - **Flexibility Info** row (provider-level): `provider.flexibility ?? "N/A"`. Use `HideableComparisonRow`.

    **6. Add Pros/Cons section** after "Provider Details", before "Featured Plan":
    - Section header: "Pros & Cons"
    - **Pros** row: Parse `provider.prosJson` as `string[]` (use `Array.isArray(provider.prosJson) ? provider.prosJson.filter((item): item is string => typeof item === "string") : []`). Display as a bulleted list: `<ul className="text-sm text-gray-700 text-left space-y-1">` with green bullet indicators. If empty, show "N/A". Use `HideableComparisonRow`.
    - **Cons** row: Same parsing pattern for `consJson`. Display with red/amber bullet indicators. Use `HideableComparisonRow`.

    **7. Apply `HideableComparisonRow`** to existing rows that could have all-null values:
    - Rating row: Keep as `ComparisonRow` (rating always exists, defaulting to 0).
    - Price/Serving row: Keep as `ComparisonRow` (always renders something).
    - Free Shipping row: Keep as `ComparisonRow` (boolean, always has a value).
    - Dietary Options row: Keep as `ComparisonRow` (renders "None listed" which is informative).
    - All Featured Plan rows: Keep as `ComparisonRow` (plan absence shows "N/A" which is informative).
    - Only new "Provider Details", "Shipping Notes", "Flexibility Info", "Pros", "Cons" rows use `HideableComparisonRow`.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prepStyle" src/components/ComparisonTable.tsx` returns at least 3 (interface + row label + value access)
    - `grep -c "valueTier" src/components/ComparisonTable.tsx` returns at least 3
    - `grep -c "modelType" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "householdFit" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "geography" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "prosJson" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "consJson" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "shippingNotes" src/components/ComparisonTable.tsx` returns at least 2
    - `grep -c "flexibility" src/components/ComparisonTable.tsx` returns at least 2
    - `grep "isFieldEmpty\|allNa\|HideableComparisonRow" src/components/ComparisonTable.tsx` matches (N/A hiding logic exists)
    - `grep "prepStyle\|valueTier\|modelType\|householdFit\|geography\|shippingNotes\|flexibility\|prosJson\|consJson" src/lib/queries/providers.ts` matches (query selects new fields)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>
    - getProvidersForComparison query returns all new provider fields (prepStyle, valueTier, modelType, householdFit, geography, shippingNotes, flexibility, prosJson, consJson)
    - ComparisonTable renders new field rows in organized sections (Provider Details, Pros & Cons, Shipping & Details)
    - Rows where ALL compared providers have null/empty values are hidden automatically via HideableComparisonRow
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add canonical slug ordering redirect and fix JSON-LD XSS safety</name>
  <files>src/app/compare/[versus]/page.tsx, src/app/compare/page.tsx</files>
  <read_first>
    - src/app/compare/[versus]/page.tsx
    - src/app/compare/page.tsx
    - src/app/providers/[slug]/page.tsx (for XSS pattern reference)
  </read_first>
  <action>
    **1. Add canonical slug ordering** in `src/app/compare/[versus]/page.tsx`:
    - In the `parseVersusSlug` function (or right after it's called), add alphabetical ordering check.
    - After parsing the two slugs `[a, b]`, check if `a.localeCompare(b) > 0` (meaning a comes after b alphabetically). If so, call `redirect(`/compare/${b}-vs-${a}`, 301)` from `next/navigation` to permanently redirect to the canonical alphabetical order.
    - Import `redirect` from `next/navigation` (it may already be imported; add if not).
    - This redirect must happen in BOTH `generateMetadata` and the page component. To avoid duplication, extract the logic into a helper function `getCanonicalVersusData(versus: string)` that:
      1. Parses the slug
      2. Checks alphabetical order and redirects if needed
      3. Returns the parsed slugs
    - Actually, `redirect()` in `generateMetadata` is tricky. Better approach: do the redirect check ONLY in the page component (before any rendering). In `generateMetadata`, just parse and return metadata for whatever slug order is given (search engines will follow the 301 redirect anyway).
    - In the page component `VersusPage`, right after parsing: if slugs are not alphabetically ordered, call `redirect(\`/compare/${sorted[0]}-vs-${sorted[1]}\`, 301)`. Use `permanentRedirect` from `next/navigation` for 308 redirect (or `redirect` for 307 -- use `redirect` with status code). Actually Next.js `redirect()` defaults to 307. For SEO canonical ordering, use `permanentRedirect()` from `next/navigation` which sends 308.

    Implementation:
    ```typescript
    import { notFound, permanentRedirect } from "next/navigation";
    ```

    In the page component, after parsing:
    ```typescript
    const parsed = parseVersusSlug(versus);
    if (!parsed) notFound();

    const [slugA, slugB] = parsed;
    // Enforce canonical alphabetical order
    if (slugA.localeCompare(slugB) > 0) {
      permanentRedirect(`/compare/${slugB}-vs-${slugA}`);
    }
    ```

    **2. Fix JSON-LD XSS safety** in `src/app/compare/[versus]/page.tsx`:
    - Find the existing `<script type="application/ld+json">` tag.
    - Change `dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}` to `dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}`.

    **3. Fix JSON-LD XSS safety** in `src/app/compare/page.tsx`:
    - There are TWO `<script type="application/ld+json">` tags in this file (one in the main render, one in `CompareEmptyState`).
    - Apply the same `.replace(/</g, "\\u003c")` fix to BOTH.

    **4. Update ComparisonTable provider type** in both comparison pages:
    - The pages pass fetched providers directly to `<ComparisonTable>`. Since Task 1 expands the `ComparisonProvider` interface with new fields, and `getProvidersForComparison` now returns those fields, the data should flow through automatically.
    - No changes needed in the page components for this -- the query returns the superset and ComparisonTable accepts it.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "permanentRedirect" src/app/compare/[versus]/page.tsx` matches (canonical redirect imported and used)
    - `grep "localeCompare" src/app/compare/[versus]/page.tsx` matches (alphabetical ordering check)
    - `grep -c '\\\\u003c' src/app/compare/[versus]/page.tsx` returns at least 1 (XSS-safe JSON-LD)
    - `grep -c '\\\\u003c' src/app/compare/page.tsx` returns at least 2 (both JSON-LD blocks fixed)
    - `grep -v "dangerouslySetInnerHTML.*JSON.stringify" src/app/compare/[versus]/page.tsx src/app/compare/page.tsx | grep -c "JSON.stringify(jsonLd)\s*}"` returns 0 (no unescaped JSON.stringify remaining)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>
    - Visiting /compare/z-provider-vs-a-provider permanently redirects to /compare/a-provider-vs-z-provider (alphabetical canonical order)
    - All JSON-LD script tags on both comparison pages use .replace(/</g, "\\u003c") for XSS prevention
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes -- all types are correct
2. `npx next build` completes successfully
3. `grep -r "prepStyle\|valueTier\|modelType" src/components/ComparisonTable.tsx` shows new fields in the comparison matrix
4. `grep "permanentRedirect" src/app/compare/[versus]/page.tsx` confirms canonical ordering
5. `grep -r "\\\\u003c" src/app/compare/` shows XSS-safe JSON-LD on all comparison pages
6. `grep "isFieldEmpty\|HideableComparisonRow\|allNa" src/components/ComparisonTable.tsx` confirms N/A row hiding logic
</verification>

<success_criteria>
- ComparisonTable displays all new provider fields (prepStyle, valueTier, modelType, householdFit, geography, shippingNotes, flexibility, pros/cons) in organized sections
- Rows where all compared providers have null/empty values are automatically hidden
- /compare/[versus] enforces alphabetical slug order via permanent redirect
- All JSON-LD on comparison pages uses XSS-safe .replace(/</g, "\\u003c") pattern
- Floating comparison tray continues to work (no regressions to existing TRAY functionality)
- TypeScript compiles and next build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-side-by-side-comparison/07-01-SUMMARY.md`
</output>
