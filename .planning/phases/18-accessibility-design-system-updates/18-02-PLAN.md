---
phase: 18-accessibility-design-system-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CategoryFilters.tsx
  - src/components/UnifiedFilters.tsx
  - src/components/Pagination.tsx
  - src/components/Header.tsx
  - src/components/RatingStars.tsx
  - src/components/SearchHero.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-04
  - A11Y-05

must_haves:
  truths:
    - "All filter labels are 16px (text-base) font size"
    - "All checkbox and radio inputs are 20x20px (h-5 w-5)"
    - "All filter option rows have min-height 44px"
    - "Pagination buttons meet 44x44px touch target minimum"
    - "Header nav links use 16px (text-base) font"
    - "Rating stars numeric text uses text-base on md size"
  artifacts:
    - path: "src/components/CategoryFilters.tsx"
      provides: "Accessible filter sidebar with 16px labels, 20px checkboxes, 44px rows"
      contains: "min-h-[44px]"
    - path: "src/components/UnifiedFilters.tsx"
      provides: "Accessible unified filters with 44px touch targets and 16px text"
      contains: "min-h-[44px]"
    - path: "src/components/Pagination.tsx"
      provides: "44px touch target pagination buttons"
      contains: "min-w-[44px] min-h-[44px]"
    - path: "src/components/Header.tsx"
      provides: "16px nav link text"
      contains: "text-base font-medium"
    - path: "src/components/RatingStars.tsx"
      provides: "Increased star sizes and 16px numeric text"
      contains: "text-base"
    - path: "src/components/SearchHero.tsx"
      provides: "Search button with accessible text-base sizing"
      contains: "text-base font-bold"
  key_links:
    - from: "src/components/CategoryFilters.tsx"
      to: "src/lib/filter-constants"
      via: "imports filter option arrays"
      pattern: "DIETARY_TAG_OPTIONS"
    - from: "src/components/UnifiedFilters.tsx"
      to: "src/lib/filter-constants"
      via: "imports filter option arrays"
      pattern: "DIETARY_TAG_OPTIONS"
---

<objective>
Increase font sizes, touch targets, checkbox/radio sizes, and filter row heights across all interactive navigation and filter components for elderly-friendly accessibility.

Purpose: Ensure all interactive elements meet WCAG 2.5.8 (44px touch targets) and all text meets the 16px minimum body text requirement for readability by older users.
Output: Updated CategoryFilters, UnifiedFilters, Pagination, Header, RatingStars, and SearchHero with larger, more accessible sizing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-accessibility-design-system-updates/18-CONTEXT.md

@src/components/CategoryFilters.tsx
@src/components/UnifiedFilters.tsx
@src/components/Pagination.tsx
@src/components/Header.tsx
@src/components/RatingStars.tsx
@src/components/SearchHero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase filter component sizing — CategoryFilters and UnifiedFilters</name>
  <files>src/components/CategoryFilters.tsx, src/components/UnifiedFilters.tsx</files>
  <read_first>src/components/CategoryFilters.tsx, src/components/UnifiedFilters.tsx</read_first>
  <action>
**CategoryFilters.tsx** -- This is the legacy category page filter sidebar. Apply ALL these changes:

1. **Sort label** (line ~357): Change `text-sm font-medium` to `text-base font-semibold` (per A11Y-01)

2. **Sort select** (line ~365): Change `text-sm` to `text-base` in the select element class

3. **All fieldset legend buttons** (the `toggleGroup` buttons for diet, prep, valueTier, household, model, geo -- 6 total): Change `text-sm font-medium` to `text-base font-semibold` and change `mb-2` to `mb-3` for more breathing room

4. **All checkbox inputs** (diet checkboxes): Change `h-4 w-4` to `h-5 w-5` (per A11Y-04)

5. **All radio inputs** (prep, valueTier, household, model, geo): Change `h-4 w-4` to `h-5 w-5` (per A11Y-04)

6. **All filter option labels** (the `<label>` wrappers with `flex items-center gap-2.5`): Add `min-h-[44px]` to their className (per A11Y-05). They currently have `flex items-center gap-2.5 cursor-pointer group/check` -- change to `flex items-center gap-3 cursor-pointer group/check min-h-[44px] rounded-lg px-2 -mx-2 hover:bg-neutral-50 transition-colors`

7. **All filter option text spans** (the `<span>` inside labels): Change `text-sm` to `text-base` (per A11Y-01)

8. **Desktop sidebar width**: Change `w-64` to `w-72` on the aside element (line ~614) to accommodate the larger text

9. **Mobile trigger button**: Change `text-sm` to `text-base` in the trigger button className (line ~638)

10. **Mobile drawer "Show Results" button**: Change `text-sm` to `text-base` (line ~732)

11. **Clear All button at bottom**: Change `text-sm` to `text-base` (line ~595)

12. **Active filter chips** (in `ActiveFilterChips`): Change `text-xs` to `text-sm` on the chip spans and `text-xs` to `text-sm` on the "Clear all" button

**UnifiedFilters.tsx** -- This already uses text-base for most elements from Phase 17. Apply these remaining changes:

1. **Radio input sizes** in Model Type and Geography fieldsets (lines ~653 and ~689): Already `h-5 w-5` -- verify, no change needed

2. **Filter option labels** in Model Type and Geography radio groups: Add `min-h-[44px] rounded-lg px-2 -mx-2 hover:bg-neutral-50 transition-colors` to the `<label>` elements that have `flex items-center gap-2.5 cursor-pointer group/check`. Change these from:
   `"flex items-center gap-2.5 cursor-pointer group/check"`
   to:
   `"flex items-center gap-3 cursor-pointer group/check min-h-[44px] rounded-lg px-2 -mx-2 hover:bg-neutral-50 transition-colors"`

3. **Active filter chips** (in `UnifiedActiveFilterChips`): Change chip `text-sm` to `text-base` on the span elements (line ~203). Change chip padding from `px-3 py-1.5` to `px-4 py-2`. Change the X button from `p-0.5` to `p-1` for larger touch target. Change "Clear all" button from `text-sm` to `text-base`.

4. **Desktop sidebar "Clear All" link** (line ~738): Change `text-sm` to `text-base`

5. **Mobile sheet header** h2: Already `text-lg font-bold` -- verify, no change needed

6. **Mobile bottom sheet buttons**: Already `text-base` -- verify, no change needed
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c "min-h-\[44px\]" src/components/CategoryFilters.tsx && grep -c "h-5 w-5" src/components/CategoryFilters.tsx && grep -c "text-base font-semibold" src/components/CategoryFilters.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CategoryFilters: All 6 fieldset legend buttons use "text-base font-semibold"
    - CategoryFilters: All checkbox inputs use "h-5 w-5" (not h-4 w-4)
    - CategoryFilters: All radio inputs use "h-5 w-5" (not h-4 w-4)
    - CategoryFilters: All filter option labels contain "min-h-[44px]"
    - CategoryFilters: All filter option text spans use "text-base" (not text-sm)
    - CategoryFilters: Desktop sidebar is w-72 (not w-64)
    - UnifiedFilters: Radio group labels contain "min-h-[44px]"
    - UnifiedFilters: Active filter chip text is "text-base" (not text-sm)
    - No text-sm remains on any filter label, option text, or interactive control text in either file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All filter text is 16px+, all checkboxes/radios are 20x20px, all filter option rows are 44px+ height, hover highlights on filter rows</done>
</task>

<task type="auto">
  <name>Task 2: Increase Pagination, Header, RatingStars, and SearchHero sizing</name>
  <files>src/components/Pagination.tsx, src/components/Header.tsx, src/components/RatingStars.tsx, src/components/SearchHero.tsx</files>
  <read_first>src/components/Pagination.tsx, src/components/Header.tsx, src/components/RatingStars.tsx, src/components/SearchHero.tsx</read_first>
  <action>
**Pagination.tsx** -- Update all pagination link/span elements for 44px touch targets:

1. **Previous/Next links and disabled spans** (4 occurrences): Change `px-3 py-2 text-sm` to `px-4 py-2.5 text-base` and add `min-h-[44px]`

2. **Page number links** (line ~108 and similar): Change `px-3 py-2 text-sm font-medium` to `px-3.5 py-2.5 text-base font-medium` and change `min-w-[40px]` to `min-w-[44px] min-h-[44px]`

3. **First page link** (line ~91): Same change -- `px-3.5 py-2.5 text-base` and `min-w-[44px] min-h-[44px]`

4. **Last page link** (line ~128): Same change -- `px-3.5 py-2.5 text-base` and `min-w-[44px] min-h-[44px]`

5. **Ellipsis spans**: Keep as-is (they're not interactive)

6. **Mobile page indicator** (line ~139): Change `text-sm` to `text-base`

7. **SVG icon sizes**: Change `width="16" height="16"` to `width="18" height="18"` on all 4 chevron SVGs

**Header.tsx** -- Update nav link text sizes:

1. **All desktop nav links** (the CATEGORY_NAV_ITEMS loop and Compare/Best Of/Blog links): Change `text-sm font-medium` to `text-base font-medium` (per A11Y-01 decision for navigation links 16px)

This is a simple find-and-replace: change all 4 occurrences of `text-sm font-medium text-neutral-600` to `text-base font-medium text-neutral-600` within the desktop nav section (lines ~28, ~36, ~42, ~48).

**RatingStars.tsx** -- Increase star sizes:

1. Update the `sizeMap` from:
```
sm: { width: 16, height: 16, textClass: "text-xs" },
md: { width: 20, height: 20, textClass: "text-sm" },
lg: { width: 24, height: 24, textClass: "text-base" },
```
To:
```
sm: { width: 18, height: 18, textClass: "text-sm" },
md: { width: 22, height: 22, textClass: "text-base" },
lg: { width: 26, height: 26, textClass: "text-lg" },
```

This ensures the md size (default) shows 16px numeric text per A11Y-01.

**SearchHero.tsx** -- Update the search button sizing:

1. **Search button** (line ~139): Change `text-sm font-bold` to `text-base font-bold` and change `px-5 py-2.5` to `px-6 py-3` for a larger touch target. The button class should become:
   `"absolute right-2 top-1/2 -translate-y-1/2 rounded-xl bg-primary-600 px-6 py-3 text-base font-bold text-white hover:bg-primary-700 transition-colors"`

2. **Search input** (line ~134): Already `text-lg` and `py-4` -- verify it provides 44px+ height. No change needed.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c "min-h-\[44px\]" src/components/Pagination.tsx && grep -c "text-base font-medium" src/components/Header.tsx && grep "text-base" src/components/RatingStars.tsx && grep "text-base font-bold" src/components/SearchHero.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Pagination: All page number links have "min-w-[44px] min-h-[44px]"
    - Pagination: All prev/next buttons use "text-base" (not text-sm)
    - Pagination: Mobile indicator uses "text-base"
    - Header: All 4 desktop nav link groups use "text-base font-medium" (not text-sm)
    - RatingStars: md sizeMap textClass is "text-base" (not text-sm)
    - RatingStars: sm sizeMap textClass is "text-sm" (not text-xs)
    - SearchHero: Search button uses "text-base font-bold" with "px-6 py-3"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Pagination has 44px touch targets, Header nav is 16px, RatingStars numeric text is 16px at md, SearchHero button is larger</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `grep -c "text-sm" src/components/CategoryFilters.tsx` should be 0 or very low (only in non-content contexts)
3. `grep -c "h-4 w-4" src/components/CategoryFilters.tsx` should be 0
4. `grep -c "min-h-\[44px\]" src/components/Pagination.tsx` shows touch targets applied
5. `grep "text-base" src/components/Header.tsx` shows all nav links are 16px
</verification>

<success_criteria>
- All filter labels and option text in CategoryFilters and UnifiedFilters are 16px (text-base) minimum
- All checkbox/radio inputs are 20x20px (h-5 w-5)
- All filter option rows have min-height 44px with hover highlight
- Pagination buttons are 44x44px minimum touch targets
- Header nav links are 16px (text-base)
- RatingStars numeric text is 16px at md size
- SearchHero button has text-base sizing
- All changes compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/18-accessibility-design-system-updates/18-02-SUMMARY.md`
</output>
