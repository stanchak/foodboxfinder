---
phase: 18-accessibility-design-system-updates
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/components/ProviderCard.tsx
  - src/app/search/page.tsx
  - src/app/search/loading.tsx
  - src/components/SearchHero.tsx
autonomous: true
requirements:
  - A11Y-03
  - CAT-03
  - SIZE-03
  - SIZE-04
  - SIZE-05

must_haves:
  truths:
    - "Provider cards show category-colored top border accent"
    - "Category badges on cards use per-category colors (not uniform primary)"
    - "Card title is text-lg, description is text-base, price is text-xl"
    - "Card padding is p-6 with image height h-48 lg:h-52"
    - "Results grid uses 3 columns on desktop with gap-8"
    - "Category quick-filter buttons are large pill-shaped with icon + text"
    - "Loading skeleton matches new card dimensions"
  artifacts:
    - path: "src/components/ProviderCard.tsx"
      provides: "Category-colored border-top, increased text sizes, increased padding"
      contains: "border-t-3"
    - path: "src/app/search/page.tsx"
      provides: "3-column desktop grid with gap-8"
      contains: "lg:grid-cols-3 gap-6 lg:gap-8"
    - path: "src/app/search/loading.tsx"
      provides: "Loading skeleton matching new card dimensions"
      contains: "h-48"
    - path: "src/components/SearchHero.tsx"
      provides: "Category tab buttons with icons"
      contains: "CategoryIcon"
  key_links:
    - from: "src/components/ProviderCard.tsx"
      to: "src/lib/categories.ts"
      via: "imports CATEGORY_COLOR_MAP for border-top and badge categoryType"
      pattern: "CATEGORY_COLOR_MAP"
    - from: "src/components/ProviderCard.tsx"
      to: "src/components/Badge.tsx"
      via: "passes categoryType prop to Badge"
      pattern: "categoryType={provider.category}"
---

<objective>
Update ProviderCard with category-colored borders, increased text/padding sizing, update the results grid to 3-column desktop layout, add category icons to SearchHero quick-filter tabs, and update loading skeleton dimensions.

Purpose: Complete the visual accessibility update with category color coding on cards, larger card text for readability, and a more spacious grid layout. Depends on Plan 01 for category color tokens and Badge categoryType prop.
Output: Category-colored provider cards, 3-column grid, icon-enhanced category tabs, matching loading skeletons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-accessibility-design-system-updates/18-CONTEXT.md
@.planning/phases/18-accessibility-design-system-updates/18-01-SUMMARY.md

@src/components/ProviderCard.tsx
@src/app/search/page.tsx
@src/app/search/loading.tsx
@src/components/SearchHero.tsx
@src/lib/categories.ts
@src/components/Badge.tsx

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From src/lib/categories.ts (added by Plan 01):
```typescript
export const CATEGORY_COLOR_MAP: Record<CategoryType, {
  badgeBg: string;
  badgeText: string;
  badgeRing: string;
  borderTop: string;
}>;
```

From src/components/Badge.tsx (updated by Plan 01):
```typescript
export default function Badge({
  children,
  color,
  categoryType?,  // NEW — optional CategoryType for per-category coloring
  className,
}: Readonly<{...}>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProviderCard with category colors and increased sizing</name>
  <files>src/components/ProviderCard.tsx</files>
  <read_first>src/components/ProviderCard.tsx, src/lib/categories.ts, src/components/Badge.tsx</read_first>
  <action>
Add import for CATEGORY_COLOR_MAP at the top of the file. The existing import line `import { CATEGORY_MAP } from "@/lib/categories"` should become:
`import { CATEGORY_MAP, CATEGORY_COLOR_MAP } from "@/lib/categories";`

Apply ALL these changes to the ProviderCard component:

1. **Category-colored top border** (per CAT-03): Add `border-t-3` (3px border-top) to the article element using the category-specific color from CATEGORY_COLOR_MAP. The article className currently starts with:
   `"group relative bg-white rounded-2xl ring-1 ring-neutral-100 shadow-card transition-all duration-200 hover:shadow-card-hover hover:-translate-y-1 overflow-hidden"`

   Change to dynamically include the border-top color:
   ```
   `group relative bg-white rounded-2xl ring-1 ring-neutral-100 shadow-card transition-all duration-200 hover:shadow-card-hover hover:-translate-y-1 overflow-hidden border-t-3 ${CATEGORY_COLOR_MAP[provider.category].borderTop}`
   ```

2. **Image height** (per SIZE-04): Change `h-44` to `h-48 lg:h-52` on the image container div (the `relative h-44 bg-gradient-to-br` div). The Image `sizes` attribute should change from `25vw` to `33vw` for the largest breakpoint since we're now 3 columns not 4.

3. **Card padding** (per SIZE-04): Change `p-5` to `p-6` on the content div.

4. **Badge gap**: Change `gap-1.5` to `gap-2` on the badge container div.

5. **Category badge** (per CAT-02): Pass the new `categoryType` prop to the category Badge:
   Change from:
   ```jsx
   <Badge color="category">
     {categoryInfo.label}
   </Badge>
   ```
   To:
   ```jsx
   <Badge color="category" categoryType={provider.category}>
     {categoryInfo.label}
   </Badge>
   ```

6. **Title text** (per SIZE-03): Change `text-base font-bold` to `text-lg font-bold` on the h3 element.

7. **Description text** (per SIZE-03): Change `text-sm text-neutral-600` to `text-base text-neutral-600` on the p element.

8. **Rating text**: Change `text-xs text-neutral-600 font-medium` to `text-sm text-neutral-600 font-medium` on the review count span.

9. **Price text** (per SIZE-03): Change `text-lg font-bold` to `text-xl font-bold` on the price p element.

10. **Dietary tag gap**: Change `gap-1.5` to `gap-2` on the dietary tags flex container.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep "border-t-3" src/components/ProviderCard.tsx && grep "categoryType={provider.category}" src/components/ProviderCard.tsx && grep "h-48 lg:h-52" src/components/ProviderCard.tsx && grep "text-lg font-bold" src/components/ProviderCard.tsx | head -3 && grep "p-6" src/components/ProviderCard.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Article element has "border-t-3" and dynamic CATEGORY_COLOR_MAP borderTop class
    - Image container uses "h-48 lg:h-52" (not h-44)
    - Content div uses "p-6" (not p-5)
    - Category Badge receives categoryType={provider.category} prop
    - Title h3 uses "text-lg font-bold" (not text-base)
    - Description p uses "text-base" (not text-sm)
    - Price p uses "text-xl font-bold" (not text-lg)
    - Image sizes attribute uses "33vw" for largest breakpoint
    - CATEGORY_COLOR_MAP is imported from @/lib/categories
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ProviderCard shows category-colored top border, per-category badge colors, and all text sizes increased per SIZE-03/SIZE-04</done>
</task>

<task type="auto">
  <name>Task 2: Update results grid, SearchHero category icons, and loading skeleton</name>
  <files>src/app/search/page.tsx, src/app/search/loading.tsx, src/components/SearchHero.tsx</files>
  <read_first>src/app/search/page.tsx, src/app/search/loading.tsx, src/components/SearchHero.tsx</read_first>
  <action>
**search/page.tsx** -- The grid already uses `xl:grid-cols-3 gap-6 lg:gap-8` which is correct for SIZE-05 (3 columns on desktop with gap-8). Verify this is the case and make NO grid changes if already correct.

However, change `xl:grid-cols-3` to `lg:grid-cols-3` so 3-column layout kicks in at lg breakpoint (1024px) instead of xl (1280px) — this matches the SIZE-05 requirement "3 columns on desktop" which should apply at the standard lg breakpoint where the sidebar is visible. The grid class (line ~120) should be:
`"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8"`

**search/loading.tsx** -- Update skeleton dimensions to match the new card sizing:

1. **Card grid**: Change `xl:grid-cols-3` to `lg:grid-cols-3` to match the page grid change.

2. **Image skeleton**: Change `h-44` to `h-48` on the card image placeholder divs (line ~53).

3. **Card content padding**: Change `p-5` to `p-6` on the card content area divs (line ~54).

4. **Badge skeletons**: Change `h-6 w-20` to `h-7 w-22` and `h-6 w-16` to `h-7 w-18` for the badge placeholders.

5. **Title skeleton**: Change `h-5 w-3/4` to `h-6 w-3/4` (matching text-lg).

6. **Price skeleton**: Change `h-6 w-20` to `h-7 w-24` (matching text-xl).

**SearchHero.tsx** -- Add category icons to quick-filter tab buttons (per A11Y-03: "large pill-shaped with icon + text"):

Add a helper component or inline SVG icons for each category. Define a `CATEGORY_ICONS` map at the top of the file (after imports, before the component):

```typescript
const CATEGORY_ICONS: Record<string, React.ReactNode> = {
  "meal-kits": (
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
      <path d="M3 2v7c0 1.1.9 2 2 2h4a2 2 0 0 0 2-2V2" /><path d="M7 2v20" /><path d="M21 15V2v0a5 5 0 0 0-5 5v6c0 1.1.9 2 2 2h3Zm0 0v7" />
    </svg>
  ),
  "prepared-meals": (
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
      <path d="M2 12h20" /><path d="M20 12c0-4.4-3.6-8-8-8s-8 3.6-8 8" /><path d="M4 12v7a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-7" />
    </svg>
  ),
  "protein-boxes": (
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
      <path d="m7.5 4.27 9 5.15" /><path d="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z" /><path d="m3.3 7 8.7 5 8.7-5" /><path d="M12 22V12" />
    </svg>
  ),
  "produce-boxes": (
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
      <path d="M7 21h10" /><path d="M12 21a9 9 0 0 0 9-9H3a9 9 0 0 0 9 9Z" /><path d="M11.38 12a2.4 2.4 0 0 1-.4-4.77 2.4 2.4 0 0 1 3.2-2.77 2.4 2.4 0 0 1 3.47-.63 2.4 2.4 0 0 1 3.13 1.33l-12.4 6.84Z" />
    </svg>
  ),
  "specialty": (
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
      <path d="M9.937 15.5A2 2 0 0 0 8.5 14.063l-6.135-1.582a.5.5 0 0 1 0-.962L8.5 9.936A2 2 0 0 0 9.937 8.5l1.582-6.135a.5.5 0 0 1 .963 0L14.063 8.5A2 2 0 0 0 15.5 9.937l6.135 1.581a.5.5 0 0 1 0 .964L15.5 14.063a2 2 0 0 0-1.437 1.437l-1.582 6.135a.5.5 0 0 1-.963 0z" />
    </svg>
  ),
};
```

Then update each category tab button to include the icon. For the per-category tabs (inside the `.map()` block), add the icon before the label text:

Change from:
```jsx
{item.label} ({count})
```
To:
```jsx
{CATEGORY_ICONS[item.slug]}
{item.label} ({count})
```

Add `gap-2` to each category button's className so the icon and text are spaced. Each button already has `min-h-[48px]` which is correct.

The "All" tab button should also get an icon. Add before "All ({allCount})":
```jsx
<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
  <rect width="7" height="7" x="3" y="3" rx="1" /><rect width="7" height="7" x="14" y="3" rx="1" /><rect width="7" height="7" x="14" y="14" rx="1" /><rect width="7" height="7" x="3" y="14" rx="1" />
</svg>
```

Add `gap-2` and `items-center` to the "All" button class as well. Both the All button and category buttons should have classes that include `inline-flex items-center gap-2`.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep "lg:grid-cols-3" src/app/search/page.tsx && grep "lg:grid-cols-3" src/app/search/loading.tsx && grep "p-6" src/app/search/loading.tsx && grep "CATEGORY_ICONS" src/components/SearchHero.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - search/page.tsx grid uses "lg:grid-cols-3" (not xl:grid-cols-3)
    - search/loading.tsx grid uses "lg:grid-cols-3" matching the page
    - search/loading.tsx card image skeleton uses "h-48" (not h-44)
    - search/loading.tsx card content uses "p-6" (not p-5)
    - SearchHero has CATEGORY_ICONS map with 5 category SVG icons
    - Each category tab button includes its icon via CATEGORY_ICONS[item.slug]
    - The "All" tab button includes a grid icon
    - All tab buttons have "inline-flex items-center gap-2" for icon+text layout
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Results grid is 3-column on desktop, category tabs have icons, loading skeleton matches new card dimensions</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npx next build` completes successfully
3. ProviderCard renders with colored top border per category
4. Category badges use per-category colors (not uniform primary)
5. Card text sizes: title=text-lg, desc=text-base, price=text-xl
6. Grid is 3 columns at lg breakpoint
7. Category tabs show icons alongside text
</verification>

<success_criteria>
- ProviderCard has category-colored border-t-3 using CATEGORY_COLOR_MAP
- Category badges use per-category colors via categoryType prop
- Card text increased: title text-lg, description text-base, price text-xl
- Card padding increased to p-6, image height h-48 lg:h-52
- Results grid is lg:grid-cols-3 with gap-8 on desktop
- Category quick-filter tabs have SVG icons + text (pill-shaped)
- Loading skeleton dimensions match new card layout
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-accessibility-design-system-updates/18-03-SUMMARY.md`
</output>
