---
phase: 18-accessibility-design-system-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/categories.ts
  - src/components/Button.tsx
  - src/components/Badge.tsx
  - src/components/Skeleton.tsx
autonomous: true
requirements:
  - CAT-01
  - CAT-04
  - CAT-02
  - SIZE-01
  - SIZE-02

must_haves:
  truths:
    - "5 category colors are defined as CSS custom properties in globals.css"
    - "Category color mapping constant exists in categories.ts with per-category color class names"
    - "Badge component renders per-category colors when color prop matches a category key"
    - "Button md size produces 48px+ min height with text-base font"
    - "Badge text is 12px (text-xs) with increased padding"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Category color tokens as --color-cat-* custom properties"
      contains: "--color-cat-meal-kits"
    - path: "src/lib/categories.ts"
      provides: "CATEGORY_COLOR_MAP constant mapping CategoryType to Tailwind class strings"
      exports: ["CATEGORY_COLOR_MAP"]
    - path: "src/components/Button.tsx"
      provides: "Updated md size with px-5 py-3 text-base"
      contains: "px-5 py-3 text-base"
    - path: "src/components/Badge.tsx"
      provides: "Per-category color variants and increased sizing"
      contains: "px-2.5 py-1 text-xs"
    - path: "src/components/Skeleton.tsx"
      provides: "Updated skeleton dimensions matching new card sizes"
      contains: "h-48"
  key_links:
    - from: "src/components/Badge.tsx"
      to: "src/app/globals.css"
      via: "Tailwind category color classes referencing --color-cat-* tokens"
      pattern: "cat-meal-kits"
    - from: "src/lib/categories.ts"
      to: "src/app/globals.css"
      via: "CATEGORY_COLOR_MAP referencing category color class names"
      pattern: "CATEGORY_COLOR_MAP"
---

<objective>
Define category color tokens, update categories.ts with color mapping, and increase Button/Badge/Skeleton sizing for elderly-friendly accessibility.

Purpose: Establish the design system foundation (color tokens + component sizing) that downstream Plan 03 (ProviderCard) depends on for category-colored borders and badges.
Output: Updated globals.css with 5 category color palettes, categories.ts with color mapping, and resized Button, Badge, Skeleton components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-accessibility-design-system-updates/18-CONTEXT.md

@src/app/globals.css
@src/lib/categories.ts
@src/components/Button.tsx
@src/components/Badge.tsx
@src/components/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category color tokens to globals.css and create CATEGORY_COLOR_MAP in categories.ts</name>
  <files>src/app/globals.css, src/lib/categories.ts</files>
  <read_first>src/app/globals.css, src/lib/categories.ts, src/components/Badge.tsx</read_first>
  <action>
**globals.css** -- Add 5 category color palettes inside the existing `@theme { }` block (the second one, after line 16), just before the closing `}`. Place them after the Warning colors and before the Shadows section. Use oklch color space exclusively per D-14 decision. Each category needs a 50 (background), 100 (ring), 600 (text/border), and 700 (text dark) shade:

```
/* Category: Meal Kits — Tangerine Orange (reuses primary hues 38-55) */
--color-cat-meal-kits-50: oklch(0.980 0.016 48);
--color-cat-meal-kits-100: oklch(0.940 0.045 45);
--color-cat-meal-kits-600: oklch(0.600 0.190 40);
--color-cat-meal-kits-700: oklch(0.520 0.170 38);

/* Category: Prepared Meals — Warm Berry (hue 340-350) */
--color-cat-prepared-meals-50: oklch(0.975 0.015 345);
--color-cat-prepared-meals-100: oklch(0.935 0.040 342);
--color-cat-prepared-meals-600: oklch(0.560 0.170 340);
--color-cat-prepared-meals-700: oklch(0.480 0.150 340);

/* Category: Protein Boxes — Rich Amber/Sienna (hue 48-70) */
--color-cat-protein-boxes-50: oklch(0.980 0.018 65);
--color-cat-protein-boxes-100: oklch(0.940 0.050 60);
--color-cat-protein-boxes-600: oklch(0.580 0.160 55);
--color-cat-protein-boxes-700: oklch(0.500 0.140 52);

/* Category: Produce Boxes — Garden Green (hue 148-150) */
--color-cat-produce-boxes-50: oklch(0.975 0.020 150);
--color-cat-produce-boxes-100: oklch(0.935 0.045 148);
--color-cat-produce-boxes-600: oklch(0.540 0.160 148);
--color-cat-produce-boxes-700: oklch(0.460 0.135 148);

/* Category: Specialty — Teal (reuses accent hues 185-190) */
--color-cat-specialty-50: oklch(0.975 0.014 190);
--color-cat-specialty-100: oklch(0.940 0.035 188);
--color-cat-specialty-600: oklch(0.560 0.104 185);
--color-cat-specialty-700: oklch(0.480 0.090 185);
```

These produce 5 distinct hues (orange ~40, berry ~340, amber ~55, green ~148, teal ~185) that are distinguishable in grayscale because their lightness values differ meaningfully (per CAT-04).

**categories.ts** -- Add a new exported constant `CATEGORY_COLOR_MAP` after the existing `CATEGORY_MAP`. This maps each `CategoryType` to an object with Tailwind class strings for badge background, badge text, badge ring, and card border-top:

```typescript
export const CATEGORY_COLOR_MAP: Record<CategoryType, {
  badgeBg: string;
  badgeText: string;
  badgeRing: string;
  borderTop: string;
}> = {
  MEAL_KIT: {
    badgeBg: "bg-cat-meal-kits-50",
    badgeText: "text-cat-meal-kits-700",
    badgeRing: "ring-cat-meal-kits-100",
    borderTop: "border-t-cat-meal-kits-600",
  },
  PREPARED_MEAL: {
    badgeBg: "bg-cat-prepared-meals-50",
    badgeText: "text-cat-prepared-meals-700",
    badgeRing: "ring-cat-prepared-meals-100",
    borderTop: "border-t-cat-prepared-meals-600",
  },
  PROTEIN_BOX: {
    badgeBg: "bg-cat-protein-boxes-50",
    badgeText: "text-cat-protein-boxes-700",
    badgeRing: "ring-cat-protein-boxes-100",
    borderTop: "border-t-cat-protein-boxes-600",
  },
  PRODUCE_BOX: {
    badgeBg: "bg-cat-produce-boxes-50",
    badgeText: "text-cat-produce-boxes-700",
    badgeRing: "ring-cat-produce-boxes-100",
    borderTop: "border-t-cat-produce-boxes-600",
  },
  SPECIALTY: {
    badgeBg: "bg-cat-specialty-50",
    badgeText: "text-cat-specialty-700",
    badgeRing: "ring-cat-specialty-100",
    borderTop: "border-t-cat-specialty-600",
  },
};
```

Do NOT change the existing `CATEGORY_MAP`, `getCategoryBySlug`, `getSlugByCategory`, or `CATEGORY_NAV_ITEMS`. Only add the new constant and its type import if not already present.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c "color-cat-meal-kits" src/app/globals.css && grep -c "CATEGORY_COLOR_MAP" src/lib/categories.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains exactly 20 new --color-cat-* custom properties (4 shades x 5 categories)
    - categories.ts exports CATEGORY_COLOR_MAP with entries for all 5 CategoryType values
    - Each entry has badgeBg, badgeText, badgeRing, borderTop string fields
    - All color values use oklch color space
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>5 category color palettes defined in globals.css, CATEGORY_COLOR_MAP exported from categories.ts, TypeScript passes</done>
</task>

<task type="auto">
  <name>Task 2: Update Button, Badge, and Skeleton component sizing</name>
  <files>src/components/Button.tsx, src/components/Badge.tsx, src/components/Skeleton.tsx</files>
  <read_first>src/components/Button.tsx, src/components/Badge.tsx, src/components/Skeleton.tsx, src/lib/categories.ts</read_first>
  <action>
**Button.tsx** -- Update the `sizes` const map. Change ONLY the size values, leave `variants` and the component JSX untouched:

Change the sizes map from:
```
sm: "px-3 py-1.5 text-sm gap-1.5",
md: "px-4 py-2.5 text-sm gap-2",
lg: "px-6 py-3 text-base gap-2.5",
```
To (per SIZE-01 and CONTEXT.md decisions):
```
sm: "px-3.5 py-2 text-sm gap-1.5",
md: "px-5 py-3 text-base gap-2",
lg: "px-7 py-3.5 text-lg gap-2.5",
```

This makes md produce ~48px height (text-base 16px line-height 24px + py-3 12px*2 = 48px) and lg produce ~52px height.

**Badge.tsx** -- Two changes:

1. Add a new `categoryColor` prop that accepts an optional string. When provided, it overrides the colorMap styling with dynamic Tailwind classes from CATEGORY_COLOR_MAP. Add this import at top: `import type { CategoryType } from "@/generated/prisma/client";` and `import { CATEGORY_COLOR_MAP } from "@/lib/categories";`

2. Update the base sizing from `px-2 py-0.5 text-[11px]` to `px-2.5 py-1 text-xs` (per SIZE-02).

The updated component should look like:

```typescript
import type { CategoryType } from "@/generated/prisma/client";
import { CATEGORY_COLOR_MAP } from "@/lib/categories";

const colorMap = {
  category: "rounded-full bg-primary-50 text-primary-700 ring-1 ring-primary-200",
  dietary: "rounded-full bg-neutral-100 text-neutral-700 ring-1 ring-neutral-200",
  valueTier: "rounded-full bg-accent-50 text-accent-700 ring-1 ring-accent-200",
  success: "rounded-full bg-success-50 text-success-700 ring-1 ring-success-600/20",
  collection: "rounded-full bg-primary-100 text-primary-600 ring-1 ring-primary-200",
  default: "rounded-full bg-neutral-100 text-neutral-700 ring-1 ring-neutral-200",
} as const;

export default function Badge({
  children,
  color = "default",
  categoryType,
  className,
}: Readonly<{
  children: React.ReactNode;
  color?: "category" | "dietary" | "valueTier" | "success" | "collection" | "default";
  categoryType?: CategoryType;
  className?: string;
}>) {
  // Use per-category colors when categoryType is provided and color is "category"
  const colorClasses = categoryType && color === "category"
    ? `rounded-full ${CATEGORY_COLOR_MAP[categoryType].badgeBg} ${CATEGORY_COLOR_MAP[categoryType].badgeText} ring-1 ${CATEGORY_COLOR_MAP[categoryType].badgeRing}`
    : colorMap[color];

  return (
    <span
      className={`inline-flex items-center px-2.5 py-1 text-xs font-semibold uppercase tracking-wider ${colorClasses} ${className ?? ""}`}
    >
      {children}
    </span>
  );
}
```

The `categoryType` prop is optional -- existing callers passing `color="category"` without `categoryType` will still get the old primary-colored badge (backwards compatible). Plan 03 will update ProviderCard to pass the new prop.

**Skeleton.tsx** -- Update the `skeletonVariants` map to match the increased card dimensions:

Change from:
```
card: "h-64 w-full rounded-xl",
image: "h-48 w-full rounded-xl",
button: "h-10 w-28 rounded-lg",
```
To:
```
card: "h-72 w-full rounded-xl",
image: "h-52 w-full rounded-xl",
button: "h-12 w-32 rounded-full",
```

Also update `badge` from `"h-6 w-16 rounded-full"` to `"h-7 w-18 rounded-full"` and `rating` from `"h-4 w-24 rounded"` to `"h-5 w-28 rounded"`.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep "px-5 py-3 text-base" src/components/Button.tsx && grep "px-2.5 py-1 text-xs" src/components/Badge.tsx && grep "categoryType" src/components/Badge.tsx && grep "h-72" src/components/Skeleton.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Button.tsx sizes map: sm="px-3.5 py-2 text-sm", md="px-5 py-3 text-base", lg="px-7 py-3.5 text-lg"
    - Badge.tsx base classes contain "px-2.5 py-1 text-xs" (not "px-2 py-0.5 text-[11px]")
    - Badge.tsx accepts optional categoryType prop of type CategoryType
    - Badge.tsx uses CATEGORY_COLOR_MAP when categoryType provided with color="category"
    - Badge.tsx remains backwards compatible (no categoryType = old behavior)
    - Skeleton.tsx card variant is "h-72", image is "h-52", button is "h-12 w-32 rounded-full"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Button md is 48px+ height, Badge text is 12px with per-category color support, Skeleton dimensions match new card sizing, TypeScript passes</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `grep -c "color-cat-" src/app/globals.css` returns 20 (4 shades x 5 categories)
3. `grep "CATEGORY_COLOR_MAP" src/lib/categories.ts` shows the export
4. `grep "px-5 py-3 text-base" src/components/Button.tsx` confirms md size
5. `grep "categoryType" src/components/Badge.tsx` confirms new prop
</verification>

<success_criteria>
- 5 distinct category color palettes defined as oklch CSS custom properties in globals.css
- CATEGORY_COLOR_MAP exported from categories.ts with all 5 CategoryType entries
- Button md size: px-5 py-3 text-base (48px min height)
- Badge text: text-xs (12px) with px-2.5 py-1 padding
- Badge supports per-category coloring via optional categoryType prop
- Skeleton dimensions updated for larger cards
- All changes compile cleanly with `npx tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/18-accessibility-design-system-updates/18-01-SUMMARY.md`
</output>
