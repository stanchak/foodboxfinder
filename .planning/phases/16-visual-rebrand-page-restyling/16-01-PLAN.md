---
phase: 16-visual-rebrand-page-restyling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - REBRAND-HOMEPAGE
must_haves:
  truths:
    - "Homepage hero uses diagonal gradient bg-gradient-to-br from-primary-50 via-white to-accent-50/30"
    - "Homepage h1 uses font-extrabold text-gray-950"
    - "Hero CTAs use rounded-xl with shadow and hover lift"
    - "Category pills in hero use uppercase tracking-wide"
    - "Featured section uses py-20 sm:py-24 spacing and font-extrabold headings"
    - "Categories section uses bg-neutral-100 background, rounded-2xl cards with hover lift"
    - "Category icon containers use rounded-xl with hover:scale-110"
    - "How It Works step numbers use bg-gray-900"
    - "Social proof section uses bg-gray-900 with text-4xl/5xl font-extrabold stats and uppercase tracking-wider labels"
    - "Final CTA uses py-20 sm:py-28 with rounded-xl buttons"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Restyled homepage with new brand design language"
  key_links: []
---

<objective>
Restyle the homepage to the new brand design language. The homepage is the most impactful page and has 6 sections requiring visual updates: Hero, Featured Providers, Browse by Category, How It Works, Social Proof, and Final CTA.

Purpose: Apply Phase 14/15 brand identity (deep teal, warm amber, Inter font, oklch tokens) at the page layout level.
Output: Restyled homepage with all 6 sections updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle homepage Hero, Featured, and Categories sections</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/app/globals.css</read_first>
  <action>
Update the homepage (src/app/page.tsx) — first 3 sections:

**Hero Section (line ~285):**
- Section bg: `bg-gradient-to-b from-primary-50 to-white` -> `bg-gradient-to-br from-primary-50 via-white to-accent-50/30`
- h1: `text-4xl font-bold tracking-tight text-gray-900` -> `text-4xl font-extrabold tracking-tight text-gray-950`
- "Search All Providers" CTA: `rounded-lg bg-primary-600 px-6 py-3.5 text-base font-semibold` -> `rounded-xl bg-primary-600 px-6 py-3.5 text-base font-semibold shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all`
- "Compare Side by Side" CTA: `rounded-lg border border-primary-600` -> `rounded-xl border border-primary-600 shadow-sm hover:shadow-md hover:-translate-y-0.5 transition-all`
- Category quick-links: `rounded-full bg-white px-5 py-2.5 text-sm font-medium text-gray-700` -> `rounded-full bg-white px-5 py-2.5 text-xs font-semibold uppercase tracking-wide text-gray-700`

**Featured Providers Section (line ~357):**
- Section: `py-16 sm:py-20` -> `py-20 sm:py-24`
- h2: `text-3xl font-bold tracking-tight text-gray-900` -> `text-2xl font-extrabold tracking-tight text-gray-900`
- Scroll indicator gradient: `from-white` -> `from-[var(--background)]` (matches warm off-white page bg)

**Browse by Category Section (line ~420):**
- Section: `bg-gray-50 py-16 sm:py-20` -> `bg-neutral-100 py-20 sm:py-24`
- h2: `text-3xl font-bold tracking-tight text-gray-900` -> `text-2xl font-extrabold tracking-tight text-gray-900`
- Category cards: `rounded-xl bg-white p-8 shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200` -> `rounded-2xl bg-white p-8 shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200 hover:-translate-y-1`
- Icon container: `rounded-full bg-primary-50` -> `rounded-xl bg-primary-50 transition-transform group-hover:scale-110`
- Provider count: `text-sm font-medium text-primary-600` -> `text-xs font-semibold uppercase tracking-wide text-primary-600`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/page.tsx returns at least 3
- grep -c "rounded-2xl" src/app/page.tsx returns at least 1
- grep -c "uppercase tracking-wide" src/app/page.tsx returns at least 2
- grep -c "bg-gradient-to-br" src/app/page.tsx returns 1
- grep -c "bg-neutral-100" src/app/page.tsx returns 1
  </acceptance_criteria>
  <done>Hero uses diagonal gradient and font-extrabold h1 with rounded-xl lift CTAs, Featured uses py-20 and font-extrabold heading, Categories uses bg-neutral-100 with rounded-2xl hover-lift cards and uppercase counts</done>
</task>

<task type="auto">
  <name>Task 2: Restyle homepage How It Works, Social Proof, and Final CTA sections</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>
Continue updating src/app/page.tsx — remaining 3 sections:

**How It Works Section (line ~490):**
- Section: `py-16 sm:py-20` -> `py-20 sm:py-24`
- h2: `text-3xl font-bold tracking-tight text-gray-900` -> `text-2xl font-extrabold tracking-tight text-gray-900`
- Icon container: `rounded-full bg-primary-50` -> `rounded-2xl bg-primary-50`
- Step number badge: `rounded-full bg-primary-600 text-xs font-bold text-white` -> `rounded-full bg-gray-900 text-xs font-bold text-white`
- Step title: `text-xl font-semibold text-gray-900` -> `text-xl font-bold text-gray-900`
- Description: `text-sm leading-6 text-gray-600` -> `text-sm leading-6 text-gray-600 max-w-xs mx-auto`

**Social Proof Section (line ~554):**
- Section: `bg-primary-700 py-14 sm:py-16` -> `bg-gray-900 py-16 sm:py-20`
- Stat value: `text-3xl font-bold text-white sm:text-4xl` -> `text-4xl font-extrabold text-white sm:text-5xl`
- Stat label: `text-base font-semibold text-primary-100` -> `text-xs font-semibold uppercase tracking-wider text-gray-400`
- Stat description: `text-sm text-primary-200` -> `text-sm text-gray-500`

**Final CTA Section (line ~602):**
- Section: `py-16 sm:py-20` -> `py-20 sm:py-28`
- h2: `text-3xl font-bold tracking-tight text-gray-900` -> `text-2xl font-extrabold tracking-tight text-gray-900`
- "Start Exploring" CTA: `rounded-lg bg-primary-600 px-6 py-3.5 text-base font-semibold` -> `rounded-xl bg-primary-600 px-6 py-3.5 text-base font-semibold shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all`
- "Browse Collections" CTA: `rounded-lg border border-gray-300 px-6 py-3.5` -> `rounded-xl border border-gray-300 px-6 py-3.5 shadow-sm hover:shadow-md hover:-translate-y-0.5 transition-all`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "bg-gray-900" src/app/page.tsx returns at least 2 (social proof section bg + step number)
- grep -c "tracking-wider" src/app/page.tsx returns at least 1
- grep "py-20 sm:py-28" src/app/page.tsx returns a match
- grep -c "rounded-2xl" src/app/page.tsx returns at least 2 (categories cards + how-it-works icons)
- grep -c "max-w-xs" src/app/page.tsx returns at least 1
  </acceptance_criteria>
  <done>How It Works uses bg-gray-900 step numbers and rounded-2xl icon containers, Social Proof uses bg-gray-900 with text-4xl/5xl font-extrabold stats and uppercase tracking-wider labels, Final CTA uses py-20 sm:py-28 with rounded-xl lift buttons</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All 6 homepage sections use the new brand design language
- No functional changes to data fetching or JSON-LD
</verification>

<success_criteria>
Homepage renders with the new brand visual identity: diagonal gradient hero, font-extrabold headings, rounded-2xl cards with hover lift, bg-gray-900 social proof, uppercase tracking-wide labels, and rounded-xl CTA buttons with shadow lift.
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-rebrand-page-restyling/16-01-SUMMARY.md`
</output>
