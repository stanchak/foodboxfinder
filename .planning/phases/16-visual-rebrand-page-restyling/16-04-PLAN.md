---
phase: 16-visual-rebrand-page-restyling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/best/page.tsx
  - src/app/best/[slug]/page.tsx
  - src/app/blog/page.tsx
  - src/app/blog/[slug]/page.tsx
autonomous: true
requirements:
  - REBRAND-COLLECTIONS
  - REBRAND-BLOG
must_haves:
  truths:
    - "Collection index h1 uses font-extrabold"
    - "Collection detail h1 uses font-extrabold"
    - "Collection cards use rounded-2xl with hover lift"
    - "Blog index h1 uses font-extrabold"
    - "Blog post h1 uses font-extrabold"
    - "Blog cards use rounded-2xl"
    - "Featured blog post badge uses bg-gray-900 instead of bg-primary-600"
    - "All CTA buttons on these pages use rounded-xl"
  artifacts:
    - path: "src/app/best/page.tsx"
      provides: "Restyled collections index page"
    - path: "src/app/best/[slug]/page.tsx"
      provides: "Restyled collection detail page"
    - path: "src/app/blog/page.tsx"
      provides: "Restyled blog index page"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Restyled blog detail page"
  key_links: []
---

<objective>
Restyle the collections and blog pages to the new brand design language. These 4 content-focused pages need consistent heading typography, card elevation, and button styling updates to match the brand established in Phases 14-15.

Purpose: Complete the page-level brand consistency across all content pages.
Output: 4 restyled page files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/best/page.tsx
@src/app/best/[slug]/page.tsx
@src/app/blog/page.tsx
@src/app/blog/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Collections index and detail pages</name>
  <files>src/app/best/page.tsx, src/app/best/[slug]/page.tsx</files>
  <read_first>src/app/best/page.tsx, src/app/best/[slug]/page.tsx</read_first>
  <action>
**Collections Index (src/app/best/page.tsx):**

- h1 (line ~68): `text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- Collection cards (line ~84): `rounded-xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200` -> `rounded-2xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200 hover:-translate-y-1`
- Card title h2 (line ~125): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- Empty state "No collections" h2 (line ~173): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- Empty state CTA button (line ~181): `rounded-lg bg-primary-600 px-5 py-2.5 text-sm font-medium` -> `rounded-xl bg-primary-600 px-5 py-2.5 text-sm font-semibold shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all`

**Collection Detail (src/app/best/[slug]/page.tsx):**

- h1 with cover image (line ~160): `text-2xl font-bold text-white sm:text-3xl lg:text-4xl` -> `text-2xl font-extrabold text-white sm:text-3xl lg:text-4xl`
- Cover image container (line ~149): `rounded-xl overflow-hidden` -> `rounded-2xl overflow-hidden`
- h1 without cover (line ~174): `text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- Provider list items (line ~221): `bg-white rounded-xl shadow-card ring-1 ring-gray-100 overflow-hidden transition-shadow hover:shadow-card-hover` -> `bg-white rounded-2xl shadow-card ring-1 ring-gray-100 overflow-hidden transition-all hover:shadow-card-hover hover:-translate-y-0.5`
- Provider name h3 (line ~260): `text-xl font-semibold text-gray-900` -> `text-xl font-bold text-gray-900`
- "Why we picked it" label (line ~345): `text-xs font-semibold text-primary-800 uppercase tracking-wide` -> `text-[11px] font-bold text-primary-800 uppercase tracking-widest`
- "Why we picked it" container (line ~344): `rounded-lg border border-primary-200 bg-primary-50/50 p-4` -> `rounded-lg bg-primary-50/30 p-4`  (remove border, use /30 opacity)
- View details CTA (line ~320): `rounded-lg bg-primary-600 px-4 py-2.5 text-sm font-medium` -> `rounded-xl bg-primary-600 px-4 py-2.5 text-sm font-semibold shadow-sm hover:shadow-md transition-all`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/best/page.tsx returns at least 1
- grep -c "rounded-2xl" src/app/best/page.tsx returns at least 1
- grep "hover:-translate-y-1" src/app/best/page.tsx returns a match
- grep -c "font-extrabold" src/app/best/\[slug\]/page.tsx returns at least 2
- grep -c "rounded-2xl" src/app/best/\[slug\]/page.tsx returns at least 2
- grep "tracking-widest" src/app/best/\[slug\]/page.tsx returns a match
  </acceptance_criteria>
  <done>Collections index uses font-extrabold h1, rounded-2xl hover-lift cards, and rounded-xl CTA. Collection detail uses font-extrabold headings, rounded-2xl cards with hover lift, tracking-widest editorial labels, and /30 opacity note backgrounds</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Blog index and detail pages</name>
  <files>src/app/blog/page.tsx, src/app/blog/[slug]/page.tsx</files>
  <read_first>src/app/blog/page.tsx, src/app/blog/[slug]/page.tsx</read_first>
  <action>
**Blog Index (src/app/blog/page.tsx):**

- h1 (line ~91): `text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- Featured post hero card (line ~107): `rounded-xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200` -> `rounded-2xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200`
- Featured badge (line ~143): `rounded-full bg-primary-600 px-3 py-1 text-xs font-medium text-white` -> `rounded-full bg-gray-900 px-3 py-1 text-xs font-semibold text-white`
- Featured post h2 (line ~164): `text-2xl font-bold text-gray-900` -> `text-2xl font-extrabold text-gray-900`
- Post grid cards (line ~216): `rounded-xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200` -> `rounded-2xl bg-white shadow-card ring-1 ring-gray-100 transition-all hover:shadow-card-hover hover:ring-primary-200`
- Post card h2 (line ~268): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- Pagination buttons (lines ~299, ~326): `rounded-lg border border-gray-300 px-4 py-2 text-sm font-medium` -> `rounded-xl border border-gray-300 px-4 py-2 text-sm font-semibold`
- "No blog posts" h2 (line ~370): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- "No blog posts" CTA (line ~378): `rounded-lg bg-primary-600 px-5 py-2.5 text-sm font-medium` -> `rounded-xl bg-primary-600 px-5 py-2.5 text-sm font-semibold shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all`

**Blog Detail (src/app/blog/[slug]/page.tsx):**

- h1 (line ~341): `text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl lg:text-5xl leading-tight` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl lg:text-5xl leading-tight`
- Cover image container (line ~371): `rounded-xl overflow-hidden` -> `rounded-2xl overflow-hidden`
- ToC sidebar heading (line ~389): `text-xs font-semibold text-gray-500 uppercase tracking-wider` -> `text-[11px] font-bold text-gray-500 uppercase tracking-widest`
- Mobile ToC details (line ~412): `rounded-lg border border-gray-200 bg-gray-50 p-4` -> `rounded-xl border border-gray-200 bg-neutral-50 p-4`
- Mobile ToC summary (line ~413): `text-sm font-medium text-gray-700` -> `text-sm font-semibold text-gray-700`
- Footer border section (line ~444): keep as-is (border-t is fine)
- "Browse providers" footer CTA (line ~469): `rounded-lg border border-gray-300 px-4 py-2 text-sm font-medium` -> `rounded-xl border border-gray-300 px-4 py-2 text-sm font-semibold`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/blog/page.tsx returns at least 2
- grep -c "rounded-2xl" src/app/blog/page.tsx returns at least 2
- grep "bg-gray-900" src/app/blog/page.tsx returns a match (featured badge)
- grep "font-extrabold" src/app/blog/\[slug\]/page.tsx returns a match
- grep "rounded-2xl" src/app/blog/\[slug\]/page.tsx returns a match
- grep "tracking-widest" src/app/blog/\[slug\]/page.tsx returns a match
  </acceptance_criteria>
  <done>Blog index uses font-extrabold headings, rounded-2xl cards, bg-gray-900 featured badge, and rounded-xl buttons. Blog detail uses font-extrabold h1, rounded-2xl cover image, tracking-widest ToC label, rounded-xl mobile ToC and footer CTAs</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All 4 content pages use consistent brand design language
- No functional changes to data fetching, JSON-LD, or component usage
</verification>

<success_criteria>
Collection and blog pages render with consistent brand identity: font-extrabold headings, rounded-2xl cards, hover lift effects, tracking-widest editorial labels, and rounded-xl CTA buttons. Featured blog badge uses bg-gray-900 instead of primary color.
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-rebrand-page-restyling/16-04-SUMMARY.md`
</output>
