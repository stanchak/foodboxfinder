---
phase: 16-visual-rebrand-page-restyling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/providers/[slug]/page.tsx
autonomous: true
requirements:
  - REBRAND-PROVIDER-DETAIL
must_haves:
  truths:
    - "Provider name h1 uses font-extrabold tracking-tight"
    - "Section nav uses frosted glass bg-white/90 backdrop-blur-lg"
    - "Section headings use text-xl font-extrabold"
    - "Editor Note label uses text-[11px] font-bold tracking-widest uppercase"
    - "Pros/cons cards use /30 opacity backgrounds with uppercase tracking-wide headers"
    - "Key details cards use bg-neutral-50 filled style with no borders and uppercase labels"
    - "Bottom CTA uses rounded-2xl with gradient background"
  artifacts:
    - path: "src/app/providers/[slug]/page.tsx"
      provides: "Restyled provider detail page with new brand design language"
  key_links: []
---

<objective>
Restyle the provider detail page to the new brand design language. This is the second most important page with multiple sections: Hero, Section Nav, Overview (Editor Note, Pros/Cons), Key Details, Flexibility & Shipping, Plans & Pricing, Reviews, FAQ, Bottom CTA, and Related Providers.

Purpose: Apply consistent brand identity across the most content-rich page type.
Output: Restyled provider detail page with all sections updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/providers/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle provider detail Hero, Section Nav, Overview, and Key Details</name>
  <files>src/app/providers/[slug]/page.tsx</files>
  <read_first>src/app/providers/[slug]/page.tsx, src/app/globals.css</read_first>
  <action>
Update src/app/providers/[slug]/page.tsx — top half of page:

**Provider Name h1 (line ~267):**
- `text-3xl font-bold text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`

**Section Navigation (line ~354-370):**
- nav: `bg-white border-b border-gray-200` -> `bg-white/90 backdrop-blur-lg border-b border-gray-200`
- nav links: `text-sm font-medium text-gray-600` -> `text-sm font-semibold text-gray-600`

**Overview Section h2 (line ~374):**
- `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`

**Editor Note (line ~385-393):**
- Container: `rounded-xl border border-primary-200 bg-primary-50/50 p-5` -> `rounded-xl border border-primary-200 bg-primary-50/50 p-5`  (no change to container)
- Label h3: `text-sm font-semibold text-primary-800 uppercase tracking-wide` -> `text-[11px] font-bold text-primary-800 uppercase tracking-widest`

**Pros Card (line ~400-443):**
- Container: `rounded-xl border border-green-200 bg-green-50/50 p-5` -> `rounded-xl bg-success-50/30 p-5` (remove border, use /30 opacity)
- Header h3: `font-semibold text-green-800` -> `text-xs font-bold uppercase tracking-wide text-success-700`

**Cons Card (line ~447-493):**
- Container: `rounded-xl border border-red-200 bg-red-50/50 p-5` -> `rounded-xl bg-error-50/30 p-5` (remove border, use /30 opacity)
- Header h3: `font-semibold text-red-800` -> `text-xs font-bold uppercase tracking-wide text-error-700`

**Key Details Section (line ~501-535):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- Detail cards: `rounded-lg border border-gray-200 p-4` -> `rounded-lg bg-neutral-50 p-4` (fill instead of border)
- dt labels: `text-sm font-medium text-gray-500` -> `text-[11px] font-semibold uppercase tracking-wide text-gray-500`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/providers/\[slug\]/page.tsx returns at least 3
- grep "backdrop-blur-lg" src/app/providers/\[slug\]/page.tsx returns a match
- grep "tracking-widest" src/app/providers/\[slug\]/page.tsx returns a match
- grep "bg-success-50/30" src/app/providers/\[slug\]/page.tsx returns a match
- grep "bg-error-50/30" src/app/providers/\[slug\]/page.tsx returns a match
- grep -c "bg-neutral-50" src/app/providers/\[slug\]/page.tsx returns at least 1
  </acceptance_criteria>
  <done>Provider h1 uses font-extrabold, section nav has frosted glass, Editor Note uses tracking-widest label, pros/cons use /30 opacity semantic colors without borders, key detail cards are filled bg-neutral-50 with uppercase labels</done>
</task>

<task type="auto">
  <name>Task 2: Restyle provider detail Flex/Shipping, Pricing, Reviews, FAQ, Bottom CTA, Related sections</name>
  <files>src/app/providers/[slug]/page.tsx</files>
  <read_first>src/app/providers/[slug]/page.tsx</read_first>
  <action>
Continue updating src/app/providers/[slug]/page.tsx — bottom half:

**Flexibility & Shipping Section (line ~541):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- Flexibility card label: `text-sm font-semibold text-primary-800 uppercase tracking-wide` -> `text-[11px] font-bold text-primary-800 uppercase tracking-widest`
- Shipping card label: `text-sm font-semibold text-gray-800 uppercase tracking-wide` -> `text-[11px] font-bold text-gray-800 uppercase tracking-widest`

**Plans & Pricing Section (line ~560):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`

**Reviews Section (line ~571):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- Write a Review h3: `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`

**FAQ Section (line ~616):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`

**Bottom CTA Section (line ~636):**
- Container: `rounded-xl border border-accent-200 bg-accent-50/50 p-6 sm:p-8 text-center` -> `rounded-2xl bg-gradient-to-br from-primary-50 via-accent-50/30 to-primary-50 p-8 sm:p-10 text-center`
- h2: `text-xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- Commission disclaimer: keep existing styling

**Related Providers Section (line ~663):**
- h2: `text-2xl font-bold text-gray-900` -> `text-xl font-extrabold text-gray-900`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/providers/\[slug\]/page.tsx returns at least 7 (all section headings + h1)
- grep "rounded-2xl bg-gradient-to-br" src/app/providers/\[slug\]/page.tsx returns a match for bottom CTA
- grep -c "tracking-widest" src/app/providers/\[slug\]/page.tsx returns at least 3 (editor note + flex label + shipping label)
  </acceptance_criteria>
  <done>All provider detail section headings use text-xl font-extrabold, bottom CTA uses rounded-2xl gradient background, Flex/Shipping labels use tracking-widest, entire provider detail page follows new brand design language</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All provider detail sections use the new brand design language
- No functional changes to data fetching, JSON-LD, or component usage
</verification>

<success_criteria>
Provider detail page renders with the new brand visual identity: font-extrabold headings, frosted glass section nav, /30 opacity pro/con backgrounds, filled key detail cards, tracking-widest labels, and rounded-2xl gradient bottom CTA.
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-rebrand-page-restyling/16-02-SUMMARY.md`
</output>
