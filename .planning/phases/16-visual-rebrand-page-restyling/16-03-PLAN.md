---
phase: 16-visual-rebrand-page-restyling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[category]/page.tsx
  - src/app/search/page.tsx
  - src/app/compare/page.tsx
  - src/app/compare/[versus]/page.tsx
autonomous: true
requirements:
  - REBRAND-CATEGORY
  - REBRAND-SEARCH
  - REBRAND-COMPARE
must_haves:
  truths:
    - "Category page uses bg-neutral-50 page background on outer container"
    - "Category page h1 uses font-extrabold"
    - "Search page headings use font-extrabold"
    - "Compare pages use font-extrabold headings"
    - "Compare versus page summary cards use rounded-2xl"
    - "Compare versus page verdict box uses rounded-2xl"
    - "All CTA buttons on these pages use rounded-xl"
  artifacts:
    - path: "src/app/[category]/page.tsx"
      provides: "Restyled category listing page"
    - path: "src/app/search/page.tsx"
      provides: "Restyled search page"
    - path: "src/app/compare/page.tsx"
      provides: "Restyled flexible comparison page"
    - path: "src/app/compare/[versus]/page.tsx"
      provides: "Restyled canonical comparison page"
  key_links: []
---

<objective>
Restyle the category listing, search, and comparison pages to the new brand design language. These pages have lighter styling needs since their main UI (ProviderCard, ComparisonTable, CategoryFilters) was already restyled in Phase 15. Focus on page-level headings, spacing, backgrounds, and CTA buttons.

Purpose: Ensure consistent brand identity across all browsing and comparison pages.
Output: 4 restyled page files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/[category]/page.tsx
@src/app/search/page.tsx
@src/app/compare/page.tsx
@src/app/compare/[versus]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Category and Search pages</name>
  <files>src/app/[category]/page.tsx, src/app/search/page.tsx</files>
  <read_first>src/app/[category]/page.tsx, src/app/search/page.tsx</read_first>
  <action>
**Category Page (src/app/[category]/page.tsx):**

- Outer container div (line ~150): `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8` -> `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8 bg-neutral-50 min-h-screen` (add warm bg)
- h1 (line ~159): `text-3xl font-bold text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- Empty state heading (line ~234): `text-base font-medium text-gray-900` -> `text-base font-bold text-gray-900`

**Search Page (src/app/search/page.tsx):**

- h1 (line ~250): `text-2xl sm:text-3xl font-bold text-gray-900` -> `text-2xl sm:text-3xl font-extrabold tracking-tight text-gray-900`
- Section headings "Providers", "Blog Posts", "Collections" (lines ~114, ~132, ~180): `text-lg font-semibold text-gray-900` -> `text-lg font-extrabold text-gray-900`
- No results heading (line ~72): `text-xl font-semibold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- "Browse by category" heading (line ~81): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- Category browse links (lines ~89, ~329): `rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm font-medium` -> `rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm font-semibold hover:shadow-sm`
- "Start typing" empty state heading (line ~312): `text-xl font-semibold text-gray-900` -> `text-xl font-extrabold text-gray-900`
- "Or browse by category" heading (line ~321): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep "font-extrabold" src/app/\[category\]/page.tsx returns a match
- grep "bg-neutral-50" src/app/\[category\]/page.tsx returns a match
- grep -c "font-extrabold" src/app/search/page.tsx returns at least 4
  </acceptance_criteria>
  <done>Category page has bg-neutral-50 background and font-extrabold h1, Search page uses font-extrabold for all headings</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Compare (flexible) and Compare (versus) pages</name>
  <files>src/app/compare/page.tsx, src/app/compare/[versus]/page.tsx</files>
  <read_first>src/app/compare/page.tsx, src/app/compare/[versus]/page.tsx</read_first>
  <action>
**Flexible Compare Page (src/app/compare/page.tsx):**

- Empty state h1 (line ~245): `text-3xl font-bold text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- "Not Enough" h1 (line ~108): `text-2xl font-bold text-gray-900` -> `text-2xl font-extrabold text-gray-900`
- Comparison result h1 (line ~155): `text-3xl font-bold text-gray-900` -> `text-3xl font-extrabold tracking-tight text-gray-900`
- "Popular Providers" h2 (line ~294): `text-2xl font-bold text-gray-900 text-center` -> `text-2xl font-extrabold text-gray-900 text-center`
- Step number circles (line ~278): `rounded-full bg-primary-600 text-white text-sm font-bold` -> `rounded-full bg-gray-900 text-white text-sm font-bold`
- CTA buttons (lines ~119-128): `rounded-lg` -> `rounded-xl` on all buttons
- "Browse Providers to Compare" button: add `shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all`

**Canonical Compare/Versus Page (src/app/compare/[versus]/page.tsx):**

- h1 (line ~137): `text-3xl font-bold text-gray-900 sm:text-4xl` -> `text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl`
- "Full Comparison" h2 (line ~155): `text-2xl font-bold text-gray-900` -> `text-2xl font-extrabold text-gray-900`
- "The Bottom Line" h2 (line ~170): `text-2xl font-bold text-gray-900` -> `text-2xl font-extrabold text-gray-900`
- Quick summary cards (line ~223): `rounded-xl border border-gray-200 bg-white p-6` -> `rounded-2xl border border-gray-200 bg-white p-6 shadow-card`
- Summary card h3 (line ~225): `text-lg font-semibold text-gray-900` -> `text-lg font-bold text-gray-900`
- Verdict box (line ~174): `rounded-xl border border-gray-200 bg-gray-50/50 p-6` -> `rounded-2xl border border-gray-200 bg-neutral-50 p-6`
- CTA buttons (lines ~182-198): `rounded-lg` -> `rounded-xl` on all buttons, add `shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all` to primary button
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "font-extrabold" src/app/compare/page.tsx returns at least 3
- grep "bg-gray-900" src/app/compare/page.tsx returns a match (step numbers)
- grep -c "font-extrabold" src/app/compare/\[versus\]/page.tsx returns at least 3
- grep -c "rounded-2xl" src/app/compare/\[versus\]/page.tsx returns at least 2 (summary cards + verdict box)
- grep -c "rounded-xl" src/app/compare/page.tsx returns at least 2 (CTA buttons)
  </acceptance_criteria>
  <done>Flexible compare page uses font-extrabold headings and bg-gray-900 step numbers with rounded-xl CTAs, Canonical versus page uses font-extrabold headings, rounded-2xl summary cards with shadow-card, and rounded-2xl verdict box</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All 4 pages use consistent brand design language
- No functional changes to data fetching, JSON-LD, or component usage
</verification>

<success_criteria>
Category, search, and comparison pages render with consistent brand identity: font-extrabold headings, rounded-xl CTAs, bg-neutral-50 backgrounds where specified, rounded-2xl cards, and consistent typography weight upgrades.
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-rebrand-page-restyling/16-03-SUMMARY.md`
</output>
