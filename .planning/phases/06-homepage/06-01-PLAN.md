---
phase: 06-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Homepage hero section clearly communicates FoodBoxFinder's value proposition and who it serves"
    - "Featured providers section shows editorially-selected provider cards linking to detail pages"
    - "All 5 category cards link to their respective hub pages with accurate provider counts"
    - "Social proof section shows real data-derived stats, not hardcoded placeholders"
    - "Page has title/description metadata and XSS-safe WebSite + Organization JSON-LD"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with hero, featured providers, category cards, social proof, JSON-LD"
      contains: "replace(/</g"
  key_links:
    - from: "src/app/page.tsx"
      to: "getFeaturedProviders"
      via: "import from @/lib/queries"
      pattern: "getFeaturedProviders"
    - from: "src/app/page.tsx"
      to: "getCategoryCounts"
      via: "import from @/lib/queries"
      pattern: "getCategoryCounts"
    - from: "src/app/page.tsx"
      to: "ProviderCard"
      via: "import from @/components/ProviderCard"
      pattern: "ProviderCard"
---

<objective>
Fix two issues in the existing homepage: XSS-unsafe JSON-LD rendering and a misleading hardcoded review count in the social proof section. Then verify the homepage renders correctly with the 95 seeded providers.

Purpose: The homepage already satisfies all HOME-01 through HOME-05 requirements structurally (hero, featured providers, category cards, social proof, metadata+JSON-LD). However, it has a security gap (JSON-LD not XSS-safe per the pattern established in Phase 4) and a data integrity issue (hardcoded "500+" reviews fallback when no reviews exist).

Output: A homepage that passes all 5 HOME requirements with consistent security patterns and honest data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/lib/queries/providers.ts
@src/components/ProviderCard.tsx
@src/lib/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix XSS-unsafe JSON-LD and misleading social proof stats</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/components/Breadcrumbs.tsx (reference for XSS-safe JSON-LD pattern)
  </read_first>
  <action>
Two targeted fixes in the existing homepage:

**Fix 1: XSS-safe JSON-LD (per Phase 4 decision — .replace(/&lt;/g, '\\u003c') after JSON.stringify)**

In the WebsiteJsonLd component (around line 231), change:
```tsx
dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
```
to:
```tsx
dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}
```

In the OrganizationJsonLd component (around line 252), make the same change:
```tsx
dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }}
```

**Fix 2: Remove misleading hardcoded review count fallback**

In the social proof stats array (around line 560), change:
```tsx
value: `${totalReviews > 0 ? totalReviews : 500}+`,
label: "Reviews",
description: "From real customers",
```
to:
```tsx
value: `${totalReviews}`,
label: "Reviews",
description: "From real customers",
```

This removes the dishonest "500+" fallback. If no reviews exist yet, it shows "0" which is honest. This stat will become meaningful once reviews are populated.

Do NOT change any other sections — the hero (HOME-01), featured providers (HOME-02), category cards (HOME-03), social proof structure (HOME-04), and metadata export (HOME-05) are all correct.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c 'replace(/&lt;/g' src/app/page.tsx | grep -q '2' && echo "PASS: Both JSON-LD blocks use XSS-safe rendering" || echo "FAIL: Not all JSON-LD blocks are XSS-safe"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'replace(/&lt;/g, "\\u003c")' src/app/page.tsx returns exactly 2 matches (WebsiteJsonLd and OrganizationJsonLd)
    - grep '500' src/app/page.tsx returns 0 matches (no hardcoded review fallback)
    - npx next build completes without errors
  </acceptance_criteria>
  <done>Both JSON-LD script tags use XSS-safe rendering consistent with Breadcrumbs.tsx, detail page, and category page. Social proof section shows real data without hardcoded fallbacks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify homepage renders correctly with seeded data</name>
  <files>src/app/page.tsx</files>
  <action>
Human visual verification of the homepage after Task 1 fixes. The homepage already has all structural sections built from prior work. This checkpoint confirms it renders correctly with the 95 seeded providers and that the JSON-LD/social-proof fixes are live.
  </action>
  <what-built>Fixed JSON-LD XSS safety and removed misleading review count fallback on the existing homepage. The homepage already has: hero section with value proposition and CTAs (HOME-01), featured providers grid with ProviderCard components (HOME-02), category cards with counts linking to hub pages (HOME-03), social proof stats section (HOME-04), and metadata + WebSite/Organization JSON-LD (HOME-05).</what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Visit http://localhost:3000
    3. Verify hero section shows "Find Your Perfect Food Box Subscription" heading with search and compare CTAs
    4. Verify featured providers section shows provider cards with logos, names, ratings, and category badges
    5. Verify "Browse by Category" section shows all 5 categories (Meal Kits, Prepared Meals, Protein Boxes, Produce Boxes, Specialty) with accurate provider counts and correct links
    6. Verify social proof section shows real provider count and category count (reviews will show 0 until review data is seeded)
    7. Right-click > View Source and search for "application/ld+json" — confirm WebSite and Organization JSON-LD blocks are present
    8. Click a category card and confirm it navigates to the correct category hub page
    9. Click a featured provider card and confirm it navigates to the provider detail page
  </how-to-verify>
  <verify>Human confirms all 9 verification steps pass</verify>
  <done>Homepage visually confirmed working with real provider data, correct navigation links, and proper JSON-LD structured data.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 5 HOME requirements addressed:
  - HOME-01: Hero section with value proposition (existing, verified)
  - HOME-02: Featured providers with ProviderCard (existing, verified)
  - HOME-03: Category cards linking to 5 hub pages (existing, verified)
  - HOME-04: Social proof section with real stats (fixed review fallback)
  - HOME-05: Metadata + WebSite/Organization JSON-LD (fixed XSS safety)
- JSON-LD XSS pattern consistent with all other pages
- Build passes
</verification>

<success_criteria>
- Homepage renders hero, featured providers, category cards, and social proof with real data
- Both JSON-LD blocks use .replace(/&lt;/g, "\\u003c") pattern
- No hardcoded fallback values in social proof stats
- next build passes without errors
- Category cards show accurate provider counts from database
- Featured provider cards link to /providers/[slug] detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage/06-01-SUMMARY.md`
</output>
