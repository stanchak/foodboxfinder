---
phase: 13-design-polish-ux-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - P2-SCROLL-INDICATOR

must_haves:
  truths:
    - "Mobile homepage featured providers section has a scroll fade indicator on the right edge"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Scroll indicator gradient on featured providers mobile scroll"
      contains: "gradient"
  key_links:
    - from: "src/app/page.tsx"
      to: "Featured providers scroll"
      via: "CSS gradient overlay"
      pattern: "bg-gradient-to-l"
---

<objective>
Add a scroll indicator gradient to the homepage featured providers horizontal scroll on mobile.

Purpose: Address P2 UX improvement -- users on mobile should see a visual cue that there are more cards to scroll to the right.
Output: Homepage featured providers section has a subtle right-edge fade gradient on mobile.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-polish-ux-improvements/13-CONTEXT.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll indicator gradient to homepage featured providers</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>
In the featured providers section (around line ~372), add a gradient overlay on the right edge of the scroll container to indicate more content. Wrap the existing scroll container in a relative div and add a gradient pseudo-element.

Find the current scroll container structure:
```tsx
<div className="mt-10 -mx-4 px-4 sm:mx-0 sm:px-0">
  <div className="flex gap-6 overflow-x-auto pb-4 snap-x snap-mandatory sm:grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 sm:overflow-visible sm:pb-0">
```

Change to:
```tsx
<div className="mt-10 -mx-4 px-4 sm:mx-0 sm:px-0">
  <div className="relative">
    <div className="flex gap-6 overflow-x-auto pb-4 snap-x snap-mandatory sm:grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 sm:overflow-visible sm:pb-0">
      {featuredProviders.map((provider) => (
        <article
          key={provider.id}
          className="min-w-[280px] max-w-[320px] snap-start sm:min-w-0 sm:max-w-none"
        >
          <ProviderCard provider={provider} />
        </article>
      ))}
    </div>
    {/* Scroll indicator gradient - mobile only */}
    <div
      className="pointer-events-none absolute inset-y-0 right-0 w-12 bg-gradient-to-l from-white to-transparent sm:hidden"
      aria-hidden="true"
    />
  </div>
</div>
```

The gradient is:
- `pointer-events-none` so it does not block card clicks
- `absolute inset-y-0 right-0 w-12` positioned on the right edge
- `bg-gradient-to-l from-white to-transparent` fades from white on the right to transparent
- `sm:hidden` only visible on mobile (the scroll is only horizontal on mobile)
- `aria-hidden="true"` since it is purely decorative
  </action>
  <acceptance_criteria>
    - grep finds `bg-gradient-to-l` in src/app/page.tsx
    - grep finds `pointer-events-none` in src/app/page.tsx (scroll indicator)
    - grep finds `sm:hidden` associated with the gradient div in page.tsx
    - grep finds `aria-hidden="true"` on the gradient div
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Homepage featured providers section has a right-edge fade gradient on mobile indicating horizontal scrollability. Hidden on desktop where grid layout is used.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx next build` completes successfully
</verification>

<success_criteria>
- Mobile homepage shows a subtle right-edge gradient on the featured providers scroll area
- Gradient is hidden on desktop (sm:hidden)
- Gradient does not block pointer events on cards
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish-ux-improvements/13-04-SUMMARY.md`
</output>
