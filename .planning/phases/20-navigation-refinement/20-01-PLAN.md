---
phase: 20-navigation-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
  - src/components/MobileNav.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "Compare link is not visible in Header or MobileNav navigation"
    - "Best Of and Blog links are visually larger and more prominent than Discover and About"
    - "About link appears in Header desktop nav and MobileNav drawer"
    - "Compare tray (CompareBar) still appears when providers are added via AddToCompareButton"
    - "Navigating to /compare directly still loads the comparison page"
    - "Footer About link remains unchanged"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Desktop navigation with Discover, Best Of, Blog, About links (no Compare)"
      contains: "Best Of"
    - path: "src/components/MobileNav.tsx"
      provides: "Mobile navigation drawer with Discover, Best Of, Blog, About links (no Compare)"
      contains: "About"
  key_links:
    - from: "src/components/Header.tsx"
      to: "/about"
      via: "Link href"
      pattern: 'href="/about"'
    - from: "src/components/MobileNav.tsx"
      to: "/about"
      via: "Link href"
      pattern: 'href="/about"'
    - from: "src/components/CompareBar.tsx"
      to: "/compare"
      via: "Link or router.push"
      pattern: "/compare"
---

<objective>
Update Header and MobileNav to remove Compare from top-level navigation, visually elevate Best Of and Blog links, and add an About link -- resulting in a cleaner nav hierarchy: Discover | Best Of | Blog | About.

Purpose: Compare is a tool accessed via provider cards, not a destination users browse to directly. Elevating Best Of and Blog highlights discovery content. Adding About builds trust.

Output: Updated Header.tsx and MobileNav.tsx with new nav hierarchy and styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-navigation-refinement/20-CONTEXT.md

<interfaces>
<!-- Key components and their current nav link patterns -->

From src/components/Header.tsx (Server Component):
- Desktop nav links inside: `<div className="hidden lg:flex lg:items-center lg:gap-6">`
- Current link styling: `text-base font-medium text-neutral-600 hover:text-primary-600 transition-colors rounded focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600`
- Current links: Discover (/search), Compare (/compare), Best Of (/best), Blog (/blog)

From src/components/MobileNav.tsx (Client Component, "use client"):
- Mobile nav links inside: `<nav className="mt-16 px-6 space-y-1">`
- Current link styling: `block px-3 py-2.5 text-base font-medium text-neutral-700 hover:bg-neutral-50 hover:text-neutral-900 rounded-xl transition-colors`
- Each link has `onClick={() => setIsOpen(false)}` to close drawer on navigation
- Current links: Discover All Providers (/search), Compare (/compare), Best Of (/best), Blog (/blog)

From src/components/Footer.tsx:
- Already has About link at `/about` in Resources column -- NO changes needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Header desktop navigation</name>
  <files>src/components/Header.tsx</files>
  <read_first>src/components/Header.tsx</read_first>
  <action>
Modify the desktop nav link section (the `hidden lg:flex` div, lines 22-47) in Header.tsx:

1. **Remove the Compare link** entirely (the Link to "/compare" on lines 29-34). (per NAV-01)

2. **Elevate Best Of and Blog links** by changing their className from the standard style to an elevated style (per NAV-02):
   - Standard style (for Discover and About): `text-base font-medium text-neutral-600 hover:text-primary-600 transition-colors rounded focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600`
   - Elevated style (for Best Of and Blog): `text-lg font-semibold text-neutral-700 hover:text-accent-600 transition-colors rounded focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600`
   - The key differences: `text-lg` instead of `text-base`, `font-semibold` instead of `font-medium`, `text-neutral-700` instead of `text-neutral-600`, `hover:text-accent-600` instead of `hover:text-primary-600`

3. **Add About link** after Blog (per NAV-04):
   - href="/about"
   - Use the standard style (same as Discover): `text-base font-medium text-neutral-600 hover:text-primary-600 transition-colors rounded focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600`

Final link order: Discover | Best Of | Blog | About

The result should have 4 links total (was 4, still 4 -- Compare removed, About added).
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c 'href="/compare"' src/components/Header.tsx | grep -q '^0$' && echo "PASS: No compare link" || echo "FAIL: Compare link still present"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -q 'href="/about"' src/components/Header.tsx && echo "PASS: About link present" || echo "FAIL: About link missing"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -q 'text-lg font-semibold' src/components/Header.tsx && echo "PASS: Elevated styling present" || echo "FAIL: Elevated styling missing"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep 'href="/compare"' src/components/Header.tsx returns NO matches (0 lines)
    - grep 'href="/about"' src/components/Header.tsx returns exactly 1 match
    - grep 'href="/search"' src/components/Header.tsx returns exactly 1 match (Discover unchanged)
    - grep 'href="/best"' src/components/Header.tsx returns exactly 1 match
    - grep 'href="/blog"' src/components/Header.tsx returns exactly 1 match
    - grep 'text-lg font-semibold' src/components/Header.tsx returns exactly 2 matches (Best Of and Blog)
    - grep 'hover:text-accent-600' src/components/Header.tsx returns exactly 2 matches (Best Of and Blog)
    - The 4 links appear in order: Discover, Best Of, Blog, About
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Header has 4 nav links (Discover, Best Of, Blog, About) with Best Of and Blog using elevated text-lg font-semibold styling with accent hover, and no Compare link present.</done>
</task>

<task type="auto">
  <name>Task 2: Update MobileNav drawer navigation</name>
  <files>src/components/MobileNav.tsx</files>
  <read_first>src/components/MobileNav.tsx</read_first>
  <action>
Modify the mobile nav links section (the `nav` element starting at line 157) in MobileNav.tsx:

1. **Remove the Compare link** entirely (the Link to "/compare" on lines 165-171). (per NAV-01)

2. **Elevate Best Of and Blog links** by changing their className to an elevated style (per NAV-02):
   - Standard style (for Discover All Providers and About): `block px-3 py-2.5 text-base font-medium text-neutral-700 hover:bg-neutral-50 hover:text-neutral-900 rounded-xl transition-colors`
   - Elevated style (for Best Of and Blog): `block px-3 py-3 text-lg font-semibold text-neutral-800 hover:bg-accent-50 hover:text-accent-700 rounded-xl transition-colors`
   - The key differences: `py-3` instead of `py-2.5` (slightly taller tap target), `text-lg` instead of `text-base`, `font-semibold` instead of `font-medium`, `text-neutral-800` instead of `text-neutral-700`, `hover:bg-accent-50` instead of `hover:bg-neutral-50`, `hover:text-accent-700` instead of `hover:text-neutral-900`

3. **Add About link** after Blog (per NAV-04):
   - href="/about"
   - Text content: "About"
   - Use the standard style (same as Discover All Providers): `block px-3 py-2.5 text-base font-medium text-neutral-700 hover:bg-neutral-50 hover:text-neutral-900 rounded-xl transition-colors`
   - Include `onClick={() => setIsOpen(false)}` to close drawer on click

Final link order: Discover All Providers | Best Of | Blog | About

The result should have 4 links total (was 4, still 4 -- Compare removed, About added).
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -c 'href="/compare"' src/components/MobileNav.tsx | grep -q '^0$' && echo "PASS: No compare link" || echo "FAIL: Compare link still present"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -q 'href="/about"' src/components/MobileNav.tsx && echo "PASS: About link present" || echo "FAIL: About link missing"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -q 'text-lg font-semibold' src/components/MobileNav.tsx && echo "PASS: Elevated styling present" || echo "FAIL: Elevated styling missing"</automated>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep 'href="/compare"' src/components/MobileNav.tsx returns NO matches (0 lines)
    - grep 'href="/about"' src/components/MobileNav.tsx returns exactly 1 match
    - grep 'href="/search"' src/components/MobileNav.tsx returns exactly 1 match (Discover unchanged)
    - grep 'href="/best"' src/components/MobileNav.tsx returns exactly 1 match
    - grep 'href="/blog"' src/components/MobileNav.tsx returns exactly 1 match
    - grep 'text-lg font-semibold' src/components/MobileNav.tsx returns exactly 2 matches (Best Of and Blog)
    - grep 'hover:bg-accent-50' src/components/MobileNav.tsx returns exactly 2 matches (Best Of and Blog)
    - The About link includes onClick={() => setIsOpen(false)}
    - The 4 links appear in order: Discover All Providers, Best Of, Blog, About
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>MobileNav has 4 nav links (Discover All Providers, Best Of, Blog, About) with Best Of and Blog using elevated text-lg font-semibold styling with accent hover, and no Compare link present. About link closes the drawer on click.</done>
</task>

</tasks>

<verification>
Overall phase verification:

1. **NAV-01 (Compare removed):** `grep -r 'href="/compare"' src/components/Header.tsx src/components/MobileNav.tsx` returns 0 results
2. **NAV-02 (Best Of and Blog elevated):** `grep -c 'text-lg font-semibold' src/components/Header.tsx src/components/MobileNav.tsx` returns 2 for each file
3. **NAV-03 (Compare still works):** `grep -q 'href="/compare"' src/components/CompareBar.tsx && echo "CompareBar still links to /compare"` -- compare functionality is unchanged since CompareBar and AddToCompareButton are independent components
4. **NAV-04 (About link added):** `grep 'href="/about"' src/components/Header.tsx src/components/MobileNav.tsx src/components/Footer.tsx` returns matches in all 3 files
5. **Build passes:** `npx next build` completes without errors
</verification>

<success_criteria>
- Header shows: Discover | Best Of | Blog | About (no Compare)
- MobileNav shows: Discover All Providers | Best Of | Blog | About (no Compare)
- Best Of and Blog use text-lg font-semibold with accent color hover in both Header and MobileNav
- Discover and About use standard text-base font-medium styling
- Footer About link unchanged (already exists at /about)
- CompareBar and AddToCompareButton remain functional (untouched)
- /compare route remains accessible via direct URL
- next build passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-navigation-refinement/20-01-SUMMARY.md`
</output>
