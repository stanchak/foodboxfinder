---
phase: 11-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/compare/loading.tsx
  - src/app/compare/[versus]/loading.tsx
  - src/app/best/loading.tsx
  - src/app/blog/loading.tsx
  - src/app/methodology/loading.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "Every public route segment has a loading.tsx that shows skeleton UI during page transitions"
    - "Error boundaries catch errors on all route segments and show a recovery UI"
    - "Custom 404 pages show search and category suggestions"
    - "All pages render correctly on mobile and desktop breakpoints"
    - "The sticky header is visible on all pages while scrolling"
  artifacts:
    - path: "src/app/compare/loading.tsx"
      provides: "Loading skeleton for compare hub page"
      min_lines: 10
    - path: "src/app/compare/[versus]/loading.tsx"
      provides: "Loading skeleton for versus comparison page"
      min_lines: 10
    - path: "src/app/best/loading.tsx"
      provides: "Loading skeleton for collections index page"
      min_lines: 10
    - path: "src/app/blog/loading.tsx"
      provides: "Loading skeleton for blog index page"
      min_lines: 10
    - path: "src/app/methodology/loading.tsx"
      provides: "Loading skeleton for methodology page"
      min_lines: 10
  key_links:
    - from: "src/app/compare/loading.tsx"
      to: "src/app/compare/page.tsx"
      via: "Next.js Suspense boundary auto-wrapping"
      pattern: "loading\\.tsx"
    - from: "src/app/error.tsx"
      to: "all route segments"
      via: "Next.js error boundary bubbling"
      pattern: "error\\.tsx"
---

<objective>
Create loading.tsx skeleton files for the 5 public route segments that are missing them, then verify that all UX polish requirements (error boundaries, 404 pages, loading states, mobile responsiveness, sticky header) are met across the entire site.

Purpose: Complete UX polish coverage so every page transition shows a skeleton loading state, and all error/404/mobile/header requirements are verified as satisfied.
Output: 5 new loading.tsx files + verified UX completeness
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/error.tsx
@src/app/not-found.tsx
@src/app/global-error.tsx
@src/components/Header.tsx
@src/components/Skeleton.tsx

# Existing loading.tsx files to follow patterns from:
@src/app/providers/[slug]/loading.tsx
@src/app/[category]/loading.tsx
@src/app/search/loading.tsx
@src/app/best/[slug]/loading.tsx
@src/app/blog/[slug]/loading.tsx

# Pages that need loading.tsx created (reference for skeleton structure):
@src/app/compare/page.tsx
@src/app/compare/[versus]/page.tsx
@src/app/best/page.tsx
@src/app/blog/page.tsx
@src/app/methodology/page.tsx
</context>

<interfaces>
<!-- UX infrastructure already in place that this plan verifies -->

From src/app/error.tsx (root error boundary - covers all segments):
```typescript
"use client";
export default function Error({ reset }: { error: Error & { digest?: string }; reset: () => void })
// Shows "Something went wrong" with Try Again + Go to Homepage actions
```

From src/app/not-found.tsx (root 404 - covers all segments):
```typescript
export default function NotFound()
// Shows 404 with SearchInput, category suggestions, and home link
```

From src/app/global-error.tsx (unrecoverable errors):
```typescript
"use client";
export default function GlobalError({ reset }: { error: Error & { digest?: string }; reset: () => void })
// Renders its own <html>/<body> with inline styles (Tailwind may not be loaded)
```

From src/components/Header.tsx:
```typescript
export default function Header()
// Already has: className="sticky top-0 z-40 bg-white/95 backdrop-blur-sm border-b border-gray-100 shadow-header"
```

Loading.tsx pattern from existing files:
```typescript
// All use animate-pulse with div placeholders matching the page layout
// No "use client" directive (Server Components)
// Named export default function with descriptive name (e.g., CategoryLoading)
// Use bg-gray-200 for skeleton elements, bg-gray-100 for section backgrounds
// Match the exact layout structure of the corresponding page.tsx
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeletons for 5 remaining public route segments</name>
  <files>
    src/app/compare/loading.tsx,
    src/app/compare/[versus]/loading.tsx,
    src/app/best/loading.tsx,
    src/app/blog/loading.tsx,
    src/app/methodology/loading.tsx
  </files>
  <read_first>
    src/app/compare/page.tsx,
    src/app/compare/[versus]/page.tsx,
    src/app/best/page.tsx,
    src/app/blog/page.tsx,
    src/app/methodology/page.tsx,
    src/app/[category]/loading.tsx,
    src/app/best/[slug]/loading.tsx
  </read_first>
  <action>
    Create 5 loading.tsx files following the established pattern from existing loading.tsx files. Each must be a Server Component (no "use client"), use animate-pulse with bg-gray-200 skeleton divs, and match the layout structure of its corresponding page.tsx.

    **src/app/compare/loading.tsx** (Compare hub page):
    - Breadcrumb skeleton (Home > Compare)
    - Centered hero section: icon placeholder (h-20 w-20 rounded-full), title skeleton (h-10 w-80), description skeleton (2 lines, h-5)
    - 3-column "how it works" steps skeleton
    - Section heading skeleton for "Popular Providers to Compare"
    - 4-column provider card grid skeleton (6 cards matching ProviderCard shape)

    **src/app/compare/[versus]/loading.tsx** (Versus comparison page):
    - Breadcrumb skeleton (Home > Compare > [vs name])
    - Title skeleton (h-10 w-96) + description skeleton (2 lines, h-5)
    - 2-column quick summary cards skeleton (each with dl grid: rating, price, shipping, dietary)
    - "Full Comparison" heading skeleton + comparison table skeleton (8-10 rows of h-10)
    - "The Bottom Line" section skeleton (3 paragraph lines)
    - CTA buttons skeleton

    **src/app/best/loading.tsx** (Collections index):
    - Breadcrumb skeleton (Home > Collections)
    - Centered title (h-10 w-72) + subtitle (h-5 w-96)
    - 3-column grid of collection card skeletons (6 cards with h-48 image, title, description, count badge)

    **src/app/blog/loading.tsx** (Blog index):
    - Breadcrumb skeleton (Home > Blog)
    - Centered title (h-10 w-48) + subtitle (h-5 w-80)
    - Featured post skeleton: large card with h-64 image area, title, excerpt, meta line
    - 3-column grid of blog post card skeletons (6 cards with h-40 image, title, excerpt, date)

    **src/app/methodology/loading.tsx** (Methodology page):
    - Breadcrumb skeleton (Home > Our Methodology)
    - max-w-3xl container matching methodology page layout
    - Title skeleton (h-10 w-64) + intro paragraph skeleton (3 lines, h-5)
    - 4 section skeletons each with: heading (h-7 w-48) + paragraph lines (4 lines, h-4) + bullet list placeholder (3-4 items with circle + line)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "animate-pulse" src/app/compare/loading.tsx returns a match
    - grep -l "animate-pulse" src/app/compare/\[versus\]/loading.tsx returns a match
    - grep -l "animate-pulse" src/app/best/loading.tsx returns a match
    - grep -l "animate-pulse" src/app/blog/loading.tsx returns a match
    - grep -l "animate-pulse" src/app/methodology/loading.tsx returns a match
    - grep -rl "animate-pulse" src/app/ | grep loading.tsx | wc -l returns 10 (5 existing + 5 new)
    - grep -rL "use client" src/app/compare/loading.tsx src/app/compare/\[versus\]/loading.tsx src/app/best/loading.tsx src/app/blog/loading.tsx src/app/methodology/loading.tsx returns all 5 files (confirms none have "use client")
    - npx next build succeeds without errors
  </acceptance_criteria>
  <done>All 10 public route segments have loading.tsx files with animate-pulse skeleton UI matching their page layout. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify complete UX polish coverage across all requirements</name>
  <files>None (verification only)</files>
  <read_first>
    src/app/error.tsx,
    src/app/not-found.tsx,
    src/app/global-error.tsx,
    src/components/Header.tsx,
    src/components/MobileNav.tsx,
    src/app/layout.tsx
  </read_first>
  <action>
    Run verification commands to confirm all 5 UX requirements are satisfied. This is a verification-only task -- no code changes expected.

    **UX-01 (Error boundaries):** Verify src/app/error.tsx exists with "use client" directive and reset button. Verify src/app/global-error.tsx exists. The root error.tsx bubbles to all route segments as a catch-all boundary.

    **UX-02 (Not-found pages):** Verify src/app/not-found.tsx exists with SearchInput component and category navigation. Verify pages that load by slug (providers/[slug], best/[slug], blog/[slug], compare/[versus]) call notFound() for invalid slugs.

    **UX-03 (Loading states):** Verify all 10 public route segments now have loading.tsx: [category], providers/[slug], search, best/[slug], blog/[slug], compare, compare/[versus], best, blog, methodology.

    **UX-04 (Mobile responsive):** Verify that responsive breakpoint classes (sm:, md:, lg:, xl:) are used in key page files and components. Verify MobileNav exists with lg:hidden hamburger toggle.

    **UX-05 (Sticky header):** Verify Header.tsx has "sticky top-0" in its className. Verify layout.tsx includes Header component.

    If any verification fails, fix the issue. Expected result: all pass without changes.
  </action>
  <verify>
    <automated>echo "UX-01:" && test -f src/app/error.tsx && test -f src/app/global-error.tsx && echo "PASS" || echo "FAIL" && echo "UX-02:" && test -f src/app/not-found.tsx && grep -q "SearchInput" src/app/not-found.tsx && echo "PASS" || echo "FAIL" && echo "UX-03:" && test $(find src/app -name "loading.tsx" -not -path "*/admin/*" | wc -l) -ge 10 && echo "PASS" || echo "FAIL" && echo "UX-05:" && grep -q "sticky top-0" src/components/Header.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/error.tsx exits 0
    - test -f src/app/global-error.tsx exits 0
    - test -f src/app/not-found.tsx exits 0
    - grep -q "SearchInput" src/app/not-found.tsx exits 0
    - grep -q "reset" src/app/error.tsx exits 0
    - find src/app -name "loading.tsx" -not -path "*/admin/*" | wc -l outputs at least 10
    - grep -q "sticky top-0" src/components/Header.tsx exits 0
    - grep -q "lg:hidden" src/components/MobileNav.tsx exits 0
    - grep -rq "sm:\|md:\|lg:" src/app/page.tsx exits 0
  </acceptance_criteria>
  <done>All 5 UX requirements (UX-01 through UX-05) verified as satisfied with automated checks passing.</done>
</task>

</tasks>

<verification>
All UX polish requirements verified:
1. UX-01: Root error.tsx + global-error.tsx provide error boundary coverage for all route segments
2. UX-02: Root not-found.tsx provides custom 404 with search + category suggestions
3. UX-03: All 10 public route segments have loading.tsx with skeleton UI
4. UX-04: Tailwind responsive classes and MobileNav provide mobile responsiveness
5. UX-05: Header has sticky top-0 z-40 positioning
</verification>

<success_criteria>
- All 10 public route segments have loading.tsx files
- `npx next build` passes
- Error boundary files exist at root level (error.tsx, global-error.tsx)
- Custom 404 page exists with search and category links
- Header has sticky positioning
- Mobile navigation exists with responsive toggle
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish/11-01-SUMMARY.md`
</output>
