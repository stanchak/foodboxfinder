---
phase: 15-visual-rebrand-component-restyling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProviderCard.tsx
  - src/components/ProviderLogo.tsx
  - src/components/Breadcrumbs.tsx
autonomous: true
requirements: [REBRAND-PROVIDERCARD, REBRAND-PROVIDERLOGO, REBRAND-BREADCRUMBS]

must_haves:
  truths:
    - "ProviderCard uses rounded-2xl with ring-1 ring-gray-100 border and hover:-translate-y-1 lift"
    - "ProviderCard logo area is h-36 with gradient background"
    - "ProviderCard price text is text-lg font-bold"
    - "ProviderCard dietary tags section has border-t separator"
    - "ProviderLogo uses rounded-2xl with shadow-xs"
    - "ProviderLogo fallback text is font-extrabold text-gray-300"
    - "Breadcrumbs use text-xs text-gray-400 with font-semibold text-gray-700 current page"
  artifacts:
    - path: "src/components/ProviderCard.tsx"
      provides: "Restyled provider card with elevated hover and gradient logo area"
      contains: "rounded-2xl"
    - path: "src/components/ProviderLogo.tsx"
      provides: "Restyled logo with rounded-2xl and shadow-xs"
      contains: "rounded-2xl"
    - path: "src/components/Breadcrumbs.tsx"
      provides: "Restyled breadcrumbs with lighter gray text"
      contains: "text-xs text-gray-400"
  key_links:
    - from: "src/components/ProviderCard.tsx"
      to: "src/components/ProviderLogo.tsx"
      via: "ProviderLogo rendered inside ProviderCard"
      pattern: "<ProviderLogo"
---

<objective>
Restyle listing surface components (ProviderCard, ProviderLogo, Breadcrumbs) to the new brand design.

Purpose: ProviderCard is the most visible component on the site -- appears on every listing, search result, and homepage. ProviderLogo and Breadcrumbs appear on nearly every page.
Output: Elevated card with hover lift, softer logo containers, and refined breadcrumb typography.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ProviderCard and ProviderLogo</name>
  <files>src/components/ProviderCard.tsx, src/components/ProviderLogo.tsx</files>
  <read_first>src/components/ProviderCard.tsx, src/components/ProviderLogo.tsx</read_first>
  <action>
**ProviderCard.tsx** -- update these classes:

1. Outer `<article>`:
   - BEFORE: `group relative bg-white rounded-xl shadow-card transition-shadow duration-200 hover:shadow-card-hover overflow-hidden`
   - AFTER: `group relative bg-white rounded-2xl ring-1 ring-gray-100 shadow-card transition-all duration-200 hover:shadow-lg hover:-translate-y-1 overflow-hidden`

2. Logo area `<div>` (the h-32 container):
   - BEFORE: `relative h-32 bg-gray-50 flex items-center justify-center overflow-hidden`
   - AFTER: `relative h-36 bg-gradient-to-br from-gray-50 to-gray-100/80 flex items-center justify-center overflow-hidden`

3. Content padding `<div>`:
   - BEFORE: `p-4`
   - AFTER: `p-5`

4. Provider name `<h3>`:
   - BEFORE: `text-lg font-semibold text-gray-900 group-hover:text-primary-700 transition-colors line-clamp-1`
   - AFTER: `text-base font-bold text-gray-900 group-hover:text-primary-700 transition-colors line-clamp-1`

5. Price `<p>`:
   - BEFORE: `mt-2 text-base font-semibold text-primary-700`
   - AFTER: `mt-2 text-lg font-bold text-primary-700`

6. Dietary tags wrapper `<div>`:
   - BEFORE: `mt-3 flex flex-wrap gap-1.5`
   - AFTER: `mt-3 pt-3 border-t border-gray-100 flex flex-wrap gap-1.5`

**ProviderLogo.tsx** -- update these classes:

1. Container `<div>`:
   - BEFORE: `rounded-xl bg-gray-50 border border-gray-100`
   - AFTER: `rounded-2xl bg-white shadow-xs`
   (Remove the `border border-gray-100` and add `shadow-xs` instead)

2. Fallback `<span>`:
   - BEFORE: `font-bold text-gray-400`
   - AFTER: `font-extrabold text-gray-300`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ProviderCard article contains `rounded-2xl` and `ring-1 ring-gray-100` (grep confirms)
    - ProviderCard article contains `hover:-translate-y-1` (grep confirms)
    - ProviderCard logo area contains `h-36` and `bg-gradient-to-br` (grep confirms)
    - ProviderCard content div uses `p-5` (grep confirms)
    - ProviderCard name h3 contains `font-bold text-gray-900` (not font-semibold)
    - ProviderCard price contains `text-lg font-bold` (grep confirms)
    - ProviderCard dietary tags div contains `border-t border-gray-100` (grep confirms)
    - ProviderLogo container contains `rounded-2xl bg-white shadow-xs` (grep confirms)
    - ProviderLogo fallback contains `font-extrabold text-gray-300` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>ProviderCard uses rounded-2xl with ring-1, hover:-translate-y-1 lift, h-36 gradient logo area, p-5 content, font-bold text-base name, text-lg font-bold price, border-t dietary tags. ProviderLogo uses rounded-2xl bg-white shadow-xs with font-extrabold text-gray-300 fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Breadcrumbs</name>
  <files>src/components/Breadcrumbs.tsx</files>
  <read_first>src/components/Breadcrumbs.tsx</read_first>
  <action>
**Breadcrumbs.tsx** -- update these classes:

1. `<nav>` element:
   - BEFORE: `text-sm text-gray-500`
   - AFTER: `text-xs text-gray-400`

2. Current page `<span>` (the one with aria-current="page"):
   - BEFORE: `font-medium text-gray-900`
   - AFTER: `font-semibold text-gray-700`

3. Chevron separator `<svg>` className:
   - BEFORE: `text-gray-400 shrink-0`
   - AFTER: `text-gray-300 shrink-0`
   (Lighter separator to match the lighter breadcrumb text)
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Breadcrumbs nav contains `text-xs text-gray-400` (grep confirms)
    - Breadcrumbs current page span contains `font-semibold text-gray-700` (grep confirms)
    - Breadcrumbs chevron svg contains `text-gray-300` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>Breadcrumbs use text-xs text-gray-400 base text, font-semibold text-gray-700 current page, and text-gray-300 separators.</done>
</task>

</tasks>

<verification>
After both tasks, verify all three files compile cleanly:
```bash
npx next build
```
</verification>

<success_criteria>
- ProviderCard has rounded-2xl, ring border, hover lift, h-36 gradient logo area, p-5 padding, bold pricing, border-t on dietary tags
- ProviderLogo has rounded-2xl, shadow-xs, font-extrabold fallback
- Breadcrumbs are text-xs text-gray-400 with font-semibold text-gray-700 current
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-rebrand-component-restyling/15-02-SUMMARY.md`
</output>
