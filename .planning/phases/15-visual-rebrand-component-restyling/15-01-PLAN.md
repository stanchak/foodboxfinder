---
phase: 15-visual-rebrand-component-restyling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/components/MobileNav.tsx
autonomous: true
requirements: [REBRAND-HEADER, REBRAND-FOOTER, REBRAND-MOBILENAV]

must_haves:
  truths:
    - "Header has frosted glass bg-white/80 backdrop-blur-xl effect"
    - "Header logo text is font-extrabold tracking-tight text-gray-900"
    - "Footer has dark bg-gray-900 with light text-gray-300"
    - "Footer headings are uppercase tracking-widest"
    - "MobileNav drawer is w-80 with bg-black/40 backdrop-blur-sm backdrop"
    - "MobileNav links use hover:bg-gray-50 neutral hover state"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Restyled header with frosted glass and updated typography"
      contains: "backdrop-blur-xl"
    - path: "src/components/Footer.tsx"
      provides: "Dark footer with warm neutral styling"
      contains: "bg-gray-900"
    - path: "src/components/MobileNav.tsx"
      provides: "Restyled mobile nav with wider drawer and neutral hover"
      contains: "w-80"
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/components/MobileNav.tsx"
      via: "MobileNav rendered inside Header"
      pattern: "MobileNav"
---

<objective>
Restyle the global chrome components (Header, Footer, MobileNav) to the new brand design language.

Purpose: These three components frame every page. Updating them gives the entire site the new visual identity immediately.
Output: Header with frosted glass effect, Footer with dark treatment, MobileNav with wider drawer and neutral tones.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@.planning/phases/14-visual-rebrand-design-system-foundation/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Header and Footer</name>
  <files>src/components/Header.tsx, src/components/Footer.tsx</files>
  <read_first>src/components/Header.tsx, src/components/Footer.tsx</read_first>
  <action>
**Header.tsx** -- update these classes:

1. Outer `<header>` element:
   - BEFORE: `bg-white/95 backdrop-blur-sm border-b border-gray-100 shadow-header`
   - AFTER: `bg-white/80 backdrop-blur-xl border-b border-gray-100 shadow-header`

2. Logo `<Link href="/">`:
   - BEFORE: `text-xl font-bold text-primary-700`
   - AFTER: `text-xl font-extrabold tracking-tight text-gray-900`

3. All nav `<Link>` elements (category links, Compare, Best Of, Blog):
   - BEFORE: `text-sm font-medium text-gray-700 hover:text-primary-600 transition-colors`
   - AFTER: `text-sm font-medium text-gray-600 hover:text-primary-600 transition-colors`

**Footer.tsx** -- update these classes:

1. Outer `<footer>`:
   - BEFORE: `bg-gray-50 border-t border-gray-200 mt-auto`
   - AFTER: `bg-gray-900 border-t border-gray-800 mt-auto`

2. All section `<h3>` headings ("Categories", "Resources", "Legal", "FoodBoxFinder"):
   - BEFORE: `text-sm font-semibold text-gray-900`
   - AFTER: `text-xs font-semibold uppercase tracking-widest text-gray-300`

3. All `<Link>` elements in footer:
   - BEFORE: `text-sm text-gray-600 hover:text-primary-600 transition-colors`
   - AFTER: `text-sm text-gray-400 hover:text-white transition-colors`

4. Description `<p>` (under "FoodBoxFinder"):
   - BEFORE: `mt-4 text-sm text-gray-600`
   - AFTER: `mt-4 text-sm text-gray-400`

5. Bottom bar `<div>` (border-t):
   - BEFORE: `mt-12 border-t border-gray-200 pt-8`
   - AFTER: `mt-12 border-t border-gray-800 pt-8`

6. Copyright `<p>`:
   - BEFORE: `text-center text-sm text-gray-500`
   - AFTER: `text-center text-sm text-gray-500`
   (no change needed -- gray-500 is fine on dark bg)
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Header contains `backdrop-blur-xl` (grep confirms)
    - Header contains `font-extrabold tracking-tight text-gray-900` (grep confirms)
    - Header nav links contain `text-gray-600` (grep confirms)
    - Footer contains `bg-gray-900` (grep confirms)
    - Footer h3 elements contain `uppercase tracking-widest text-gray-300` (grep confirms)
    - Footer links contain `text-gray-400 hover:text-white` (grep confirms)
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Header has frosted glass bg-white/80 backdrop-blur-xl, logo is font-extrabold tracking-tight text-gray-900, nav links are text-gray-600. Footer is bg-gray-900 with uppercase tracking-widest headings and text-gray-400 hover:text-white links.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle MobileNav</name>
  <files>src/components/MobileNav.tsx</files>
  <read_first>src/components/MobileNav.tsx</read_first>
  <action>
**MobileNav.tsx** -- update these classes:

1. Backdrop overlay `<div>` (the fixed inset-0):
   - BEFORE: `fixed inset-0 z-50 bg-black/20`
   - AFTER: `fixed inset-0 z-50 bg-black/40 backdrop-blur-sm`

2. Drawer panel `<div>`:
   - BEFORE: `fixed top-0 right-0 z-50 h-full w-72 bg-white shadow-xl transform transition-transform duration-300 translate-x-0`
   - AFTER: `fixed top-0 right-0 z-50 h-full w-80 bg-white shadow-xl transform transition-transform duration-300 translate-x-0`

3. All nav `<Link>` elements (category links, Search, Compare, Blog):
   - BEFORE: `block px-3 py-2.5 text-base font-medium text-gray-700 hover:bg-primary-50 hover:text-primary-700 rounded-lg transition-colors`
   - AFTER: `block px-3 py-2.5 text-base font-medium text-gray-700 hover:bg-gray-50 hover:text-gray-900 rounded-lg transition-colors`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MobileNav contains `bg-black/40 backdrop-blur-sm` (grep confirms)
    - MobileNav contains `w-80` (grep confirms)
    - MobileNav links contain `hover:bg-gray-50 hover:text-gray-900` (grep confirms)
    - Build succeeds without errors
  </acceptance_criteria>
  <done>MobileNav backdrop uses bg-black/40 backdrop-blur-sm, drawer is w-80, nav links use neutral hover:bg-gray-50 hover:text-gray-900 instead of primary tints.</done>
</task>

</tasks>

<verification>
After both tasks, verify all three files compile cleanly:
```bash
npx next build
```
</verification>

<success_criteria>
- Header shows frosted glass (bg-white/80 backdrop-blur-xl)
- Header logo is font-extrabold tracking-tight text-gray-900
- Footer is dark (bg-gray-900) with uppercase tracking-widest headings and light links
- MobileNav has wider drawer (w-80), darker backdrop (bg-black/40), neutral hover states
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-rebrand-component-restyling/15-01-SUMMARY.md`
</output>
