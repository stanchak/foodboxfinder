---
phase: 15-visual-rebrand-component-restyling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ComparisonTable.tsx
  - src/components/PricingTable.tsx
  - src/components/CompareBar.tsx
autonomous: true
requirements: [REBRAND-COMPARISONTABLE, REBRAND-PRICINGTABLE, REBRAND-COMPAREBAR]

must_haves:
  truths:
    - "ComparisonTable outer container uses rounded-2xl shadow-sm"
    - "ComparisonTable provider header cells have p-6 padding"
    - "ComparisonTable section headers use bg-gray-100"
    - "ComparisonTable alternating highlight rows use bg-surface-50 (the neutral-50 warm off-white)"
    - "PricingTable cards use rounded-2xl"
    - "PricingTable featured card has scale-[1.02] border-2 shadow-md"
    - "PricingTable price is text-4xl font-extrabold"
    - "CompareBar uses bg-white/90 backdrop-blur-xl"
    - "CompareBar chips are bg-gray-900 text-white"
    - "CompareBar compare button uses bg-accent-500"
  artifacts:
    - path: "src/components/ComparisonTable.tsx"
      provides: "Restyled comparison table with rounded-2xl and alternating warm rows"
      contains: "rounded-2xl"
    - path: "src/components/PricingTable.tsx"
      provides: "Restyled pricing cards with featured scale effect"
      contains: "scale-\\[1.02\\]"
    - path: "src/components/CompareBar.tsx"
      provides: "Restyled compare bar with frosted glass and dark chips"
      contains: "backdrop-blur-xl"
  key_links:
    - from: "src/components/CompareBar.tsx"
      to: "src/components/CompareProvider.tsx"
      via: "useCompare hook"
      pattern: "useCompare"
---

<objective>
Restyle comparison surface components (ComparisonTable, PricingTable, CompareBar) to the new brand design.

Purpose: These components are the core of the comparison flow -- the highest-value interaction on the site.
Output: Elevated comparison table with warm alternating rows, dramatic pricing cards with featured scale, and frosted glass compare bar with dark chips.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ComparisonTable</name>
  <files>src/components/ComparisonTable.tsx</files>
  <read_first>src/components/ComparisonTable.tsx</read_first>
  <action>
**ComparisonTable.tsx** -- update these classes:

1. Outer wrapper `<div>` (the inline-block with sm:rounded-xl):
   - BEFORE: `inline-block min-w-full align-top sm:rounded-xl sm:border sm:border-gray-200 sm:overflow-hidden`
   - AFTER: `inline-block min-w-full align-top sm:rounded-2xl sm:shadow-sm sm:border sm:border-gray-200 sm:overflow-hidden`

2. Provider header `<th>` cells (the ones with `scope="col"` and provider content):
   - BEFORE: `p-4 text-center border-b border-gray-200 bg-white min-w-[200px]`
   - AFTER: `p-6 text-center border-b border-gray-200 bg-white min-w-[200px]`

3. SectionHeader `<td>` (the bg-gray-50 section dividers):
   - BEFORE: `bg-gray-50 px-4 py-2.5 text-xs font-semibold text-gray-500 uppercase tracking-wide border-y border-gray-200`
   - AFTER: `bg-gray-100 px-4 py-2.5 text-xs font-semibold text-gray-500 uppercase tracking-wide border-y border-gray-200`

4. ComparisonRow highlight bgClass:
   - BEFORE: `const bgClass = highlight ? "bg-gray-50/50" : "bg-white";`
   - AFTER: `const bgClass = highlight ? "bg-neutral-50" : "bg-white";`
   (Use the warm neutral-50 token instead of transparent gray)
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonTable outer div contains `sm:rounded-2xl sm:shadow-sm` (grep confirms)
    - ComparisonTable provider th cells contain `p-6` (grep confirms)
    - SectionHeader td contains `bg-gray-100` (grep confirms)
    - ComparisonRow bgClass uses `bg-neutral-50` for highlight (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>ComparisonTable uses rounded-2xl shadow-sm outer wrapper, p-6 provider headers, bg-gray-100 section headers, and bg-neutral-50 warm alternating rows.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle PricingTable and CompareBar</name>
  <files>src/components/PricingTable.tsx, src/components/CompareBar.tsx</files>
  <read_first>src/components/PricingTable.tsx, src/components/CompareBar.tsx</read_first>
  <action>
**PricingTable.tsx** -- update these classes:

1. Plan card `<div>` (non-featured):
   - BEFORE: `relative rounded-xl border p-6 flex flex-col` then conditional `border-gray-200 bg-white`
   - AFTER: `relative rounded-2xl border p-6 flex flex-col` then conditional `border-gray-200 bg-white`

2. Plan card (featured) conditional classes:
   - BEFORE: `border-primary-400 ring-2 ring-primary-100 bg-primary-50/30`
   - AFTER: `border-primary-400 border-2 ring-2 ring-primary-100 bg-primary-50/30 scale-[1.02] shadow-md`

3. Featured badge `<div>` (the absolute -top-3):
   - BEFORE: `absolute -top-3 left-4`
   - AFTER: `absolute -top-3 left-1/2 -translate-x-1/2`
   (Center the badge above the card)

4. Price `<span>` (the text-3xl):
   - BEFORE: `text-3xl font-bold text-gray-900`
   - AFTER: `text-4xl font-extrabold text-gray-900`

**CompareBar.tsx** -- update these classes:

1. Outer `<div>`:
   - BEFORE: `fixed bottom-0 inset-x-0 z-50 border-t border-gray-200 bg-white/95 backdrop-blur-sm shadow-[0_-4px_12px_rgb(0_0_0/0.08)]`
   - AFTER: `fixed bottom-0 inset-x-0 z-50 border-t border-gray-200 bg-white/90 backdrop-blur-xl shadow-[0_-4px_12px_rgb(0_0_0/0.08)]`

2. Provider chip `<div>` (the inline-flex with selected providers):
   - BEFORE: `inline-flex items-center gap-1.5 rounded-full bg-primary-50 border border-primary-200 pl-3 pr-1.5 py-1 text-sm font-medium text-primary-700 shrink-0`
   - AFTER: `inline-flex items-center gap-1.5 rounded-full bg-gray-900 pl-3 pr-1.5 py-1 text-sm font-medium text-white shrink-0`

3. Chip remove button:
   - BEFORE: `inline-flex items-center justify-center w-5 h-5 rounded-full hover:bg-primary-200 transition-colors`
   - AFTER: `inline-flex items-center justify-center w-5 h-5 rounded-full hover:bg-gray-700 transition-colors`

4. Compare button:
   - BEFORE: `inline-flex items-center justify-center gap-1.5 rounded-lg bg-primary-600 px-5 py-2.5 text-sm font-semibold text-white hover:bg-primary-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed transition-colors`
   - AFTER: `inline-flex items-center justify-center gap-1.5 rounded-lg bg-accent-500 px-5 py-2.5 text-sm font-semibold text-white hover:bg-accent-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-400 focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed transition-colors`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PricingTable card contains `rounded-2xl` (grep confirms)
    - PricingTable featured card contains `scale-[1.02]` and `shadow-md` and `border-2` (grep confirms)
    - PricingTable featured badge container uses `-translate-x-1/2` centering (grep confirms)
    - PricingTable price contains `text-4xl font-extrabold` (grep confirms)
    - CompareBar outer div contains `bg-white/90 backdrop-blur-xl` (grep confirms)
    - CompareBar chips contain `bg-gray-900` and `text-white` (grep confirms)
    - CompareBar compare button contains `bg-accent-500` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>PricingTable uses rounded-2xl with featured scale-[1.02] border-2 shadow-md, centered badge, and text-4xl font-extrabold prices. CompareBar uses bg-white/90 backdrop-blur-xl, bg-gray-900 text-white dark chips, and bg-accent-500 compare button.</done>
</task>

</tasks>

<verification>
After both tasks, verify all three files compile cleanly:
```bash
npx next build
```
</verification>

<success_criteria>
- ComparisonTable has rounded-2xl, p-6 headers, bg-gray-100 section headers, bg-neutral-50 warm alternating rows
- PricingTable has rounded-2xl cards, featured scale-[1.02] with border-2 and shadow-md, centered badge, text-4xl prices
- CompareBar has frosted glass bg-white/90 backdrop-blur-xl, dark chips bg-gray-900, accent compare button
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-rebrand-component-restyling/15-03-SUMMARY.md`
</output>
