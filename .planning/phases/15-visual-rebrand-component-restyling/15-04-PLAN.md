---
phase: 15-visual-rebrand-component-restyling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CategoryFilters.tsx
  - src/components/HeaderSearchForm.tsx
autonomous: true
requirements: [REBRAND-CATEGORYFILTERS, REBRAND-HEADERSEARCHFORM]

must_haves:
  truths:
    - "CategoryFilters desktop sidebar has bg-white rounded-xl p-5 shadow-sm ring-1"
    - "CategoryFilters has divide-y between filter groups"
    - "CategoryFilters title is uppercase"
    - "CategoryFilters active filter chips use bg-primary-600 text-white"
    - "HeaderSearchForm input has rounded-full pill shape"
    - "HeaderSearchForm has focus:w-64 expand animation"
  artifacts:
    - path: "src/components/CategoryFilters.tsx"
      provides: "Restyled filter sidebar with card container and divide-y groups"
      contains: "rounded-xl"
    - path: "src/components/HeaderSearchForm.tsx"
      provides: "Restyled search with pill shape and expand animation"
      contains: "rounded-full"
  key_links:
    - from: "src/components/CategoryFilters.tsx"
      to: "URL search params"
      via: "useSearchParams hook"
      pattern: "useSearchParams"
---

<objective>
Restyle interactive input components (CategoryFilters, HeaderSearchForm) to the new brand design.

Purpose: These are the primary interactive components users engage with to filter and search. Updated styling improves the experience.
Output: Filter sidebar with card container and group dividers; search input with pill shape and focus expand.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle CategoryFilters</name>
  <files>src/components/CategoryFilters.tsx</files>
  <read_first>src/components/CategoryFilters.tsx</read_first>
  <action>
**CategoryFilters.tsx** -- update these classes:

1. Desktop sidebar inner container (the `<div>` inside `<aside>` with `className="sticky top-20"`):
   Wrap the existing content in an additional container. Currently the `sticky top-20` div directly contains the title + filterContent. Add a container wrapper around just the title + filterContent:
   - Add to the sticky div's content: wrap both the title bar (`<div className="flex items-center...">`) and `{filterContent}` in a new `<div className="bg-white rounded-xl p-5 shadow-sm ring-1 ring-gray-100">`.

2. Filters title `<h2>`:
   - BEFORE: `text-base font-semibold text-gray-900`
   - AFTER: `text-sm font-semibold uppercase tracking-wide text-gray-900`

3. filterContent wrapper `<div>`:
   - BEFORE: `space-y-6`
   - AFTER: `divide-y divide-gray-100 space-y-0`
   Then add `pt-5` to each direct child `<div>` and `<fieldset>` that is not the first child (the Sort div). This means each filter group gets a top border and top padding. Implementation: change `space-y-6` to `divide-y divide-gray-100` and add `py-5 first:pt-0 last:pb-0` or simply add `pt-5` to each fieldset and the clear button's wrapper. The simplest approach: keep `space-y-6` but add `divide-y divide-gray-100` to get the visual dividers, which gives both spacing and dividers.
   - AFTER: `space-y-6 divide-y divide-gray-100` (the divide-y adds visual lines between groups; fieldsets already have spacing from space-y-6; each fieldset should add `pt-6` top padding to clear the divider, so update each fieldset to add `pt-6` class -- but since space-y-6 already adds gap, the divide-y line sits at the top of each group naturally)

   Actually, simplest approach that looks correct: change filterContent wrapper to `divide-y divide-gray-100` and change spacing to padding on each child. But since there are many children and the space-y-6 already works, just add `divide-y divide-gray-100` alongside `space-y-6`:
   - FINAL: `space-y-6 divide-y divide-gray-100`

4. ActiveFilterChips -- the chip `<span>` elements:
   - BEFORE: `inline-flex items-center gap-1 rounded-full bg-primary-50 text-primary-700 text-xs font-medium px-2.5 py-1 ring-1 ring-inset ring-primary-600/20`
   - AFTER: `inline-flex items-center gap-1 rounded-full bg-primary-600 text-white text-xs font-medium px-2.5 py-1`

5. ActiveFilterChips -- the chip remove button:
   - BEFORE: `ml-0.5 inline-flex items-center rounded-full p-0.5 hover:bg-primary-100 transition-colors`
   - AFTER: `ml-0.5 inline-flex items-center rounded-full p-0.5 hover:bg-primary-500 transition-colors`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CategoryFilters desktop sidebar contains `bg-white rounded-xl p-5 shadow-sm ring-1 ring-gray-100` (grep confirms)
    - CategoryFilters title h2 contains `uppercase tracking-wide` (grep confirms)
    - CategoryFilters filterContent wrapper contains `divide-y divide-gray-100` (grep confirms)
    - ActiveFilterChips chips contain `bg-primary-600 text-white` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>CategoryFilters desktop sidebar wrapped in bg-white rounded-xl p-5 shadow-sm ring-1 card container. Filter groups separated by divide-y divide-gray-100 dividers. Title is uppercase tracking-wide. Active filter chips use bg-primary-600 text-white.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle HeaderSearchForm</name>
  <files>src/components/HeaderSearchForm.tsx</files>
  <read_first>src/components/HeaderSearchForm.tsx</read_first>
  <action>
**HeaderSearchForm.tsx** -- update these classes:

1. Search `<input>`:
   - BEFORE: `w-44 rounded-lg border border-gray-200 bg-gray-50 py-1.5 pl-9 pr-3 text-sm text-gray-700 placeholder:text-gray-400 focus:border-primary-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-primary-500/20 transition-colors`
   - AFTER: `w-44 rounded-full border border-gray-200 bg-gray-50 py-1.5 pl-9 pr-3 text-sm text-gray-700 placeholder:text-gray-400 focus:border-primary-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-primary-500/20 focus:w-64 transition-all duration-300`
   Key changes: `rounded-lg` to `rounded-full` for pill shape, add `focus:w-64` for expand on focus, change `transition-colors` to `transition-all duration-300` to animate the width change.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HeaderSearchForm input contains `rounded-full` (grep confirms)
    - HeaderSearchForm input contains `focus:w-64` (grep confirms)
    - HeaderSearchForm input contains `transition-all duration-300` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>HeaderSearchForm input uses rounded-full pill shape with focus:w-64 expand animation and transition-all duration-300.</done>
</task>

</tasks>

<verification>
After both tasks, verify both files compile cleanly:
```bash
npx next build
```
</verification>

<success_criteria>
- CategoryFilters has card container (bg-white rounded-xl p-5 shadow-sm ring-1), uppercase title, divide-y groups, bg-primary-600 active chips
- HeaderSearchForm has rounded-full pill, focus:w-64 expand, transition-all duration-300
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-rebrand-component-restyling/15-04-SUMMARY.md`
</output>
