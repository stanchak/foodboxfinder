---
phase: 15-visual-rebrand-component-restyling
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FaqAccordion.tsx
  - src/components/ReviewCard.tsx
  - src/components/RatingBreakdown.tsx
  - src/components/AffiliateLink.tsx
autonomous: true
requirements: [REBRAND-FAQACCORDION, REBRAND-REVIEWCARD, REBRAND-RATINGBREAKDOWN, REBRAND-AFFILIATELINK]

must_haves:
  truths:
    - "FaqAccordion has rounded-xl border container"
    - "FaqAccordion questions have px-5 py-5 hover:bg-gray-50"
    - "ReviewCard uses py-8 spacing"
    - "ReviewCard avatar uses rounded-xl neutral background"
    - "ReviewCard borders are lighter (border-gray-100)"
    - "RatingBreakdown bars are h-2"
    - "RatingBreakdown numbers are font-extrabold tracking-tight"
    - "AffiliateLink primary uses rounded-xl with hover:-translate-y-0.5 lift"
  artifacts:
    - path: "src/components/FaqAccordion.tsx"
      provides: "Restyled FAQ with rounded-xl container and hover states"
      contains: "rounded-xl"
    - path: "src/components/ReviewCard.tsx"
      provides: "Restyled review card with larger spacing and neutral avatar"
      contains: "py-8"
    - path: "src/components/RatingBreakdown.tsx"
      provides: "Restyled rating breakdown with slimmer bars"
      contains: "h-2"
    - path: "src/components/AffiliateLink.tsx"
      provides: "Restyled affiliate link with lift effect"
      contains: "hover:-translate-y-0.5"
  key_links:
    - from: "src/components/ReviewCard.tsx"
      to: "src/components/RatingStars.tsx"
      via: "RatingStars rendered inside ReviewCard"
      pattern: "<RatingStars"
---

<objective>
Restyle detail page sub-components (FaqAccordion, ReviewCard, RatingBreakdown, AffiliateLink) to the new brand design.

Purpose: These components appear on provider detail pages and define the content consumption experience for individual providers.
Output: FAQ with card container and hover states, reviews with spacious layout, slimmer rating bars, and affiliate links with hover lift.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle FaqAccordion and AffiliateLink</name>
  <files>src/components/FaqAccordion.tsx, src/components/AffiliateLink.tsx</files>
  <read_first>src/components/FaqAccordion.tsx, src/components/AffiliateLink.tsx</read_first>
  <action>
**FaqAccordion.tsx** -- update these classes:

1. Outer container `<div>`:
   - BEFORE: `divide-y divide-gray-200 border-t border-b border-gray-200`
   - AFTER: `divide-y divide-gray-100 rounded-xl border border-gray-200 overflow-hidden`
   (Change from top/bottom borders to a full rounded-xl card container; lighter dividers)

2. Question `<button>`:
   - BEFORE: `flex w-full items-center justify-between gap-4 py-4 text-left text-gray-900 hover:text-primary-700 transition-colors`
   - AFTER: `flex w-full items-center justify-between gap-4 px-5 py-5 text-left text-gray-900 hover:bg-gray-50 transition-colors`
   (Add px-5, increase py to py-5, change hover from text color to background color)

3. Answer panel `<div>` (the one with `role="region"`):
   - BEFORE: `className={isOpen ? "pb-4" : ""}`
   - AFTER: `className={isOpen ? "px-5 pb-5" : ""}`
   (Add px-5 to match question padding, increase pb to pb-5)

**AffiliateLink.tsx** -- update these classes:

1. Primary variant `<a>` (the full-size button):
   - BEFORE: `inline-flex items-center justify-center gap-2 rounded-lg bg-accent-500 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-accent-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-400 focus-visible:ring-offset-2 transition-colors`
   - AFTER: `inline-flex items-center justify-center gap-2 rounded-xl bg-accent-500 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-accent-600 hover:-translate-y-0.5 hover:shadow-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-400 focus-visible:ring-offset-2 transition-all duration-200`
   Key changes: `rounded-lg` to `rounded-xl`, add `hover:-translate-y-0.5 hover:shadow-md`, change `transition-colors` to `transition-all duration-200`

2. Compact variant `<a>`:
   - BEFORE: `inline-flex items-center justify-center gap-1.5 rounded-lg bg-accent-500 px-4 py-2 text-sm font-semibold text-white hover:bg-accent-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-400 focus-visible:ring-offset-2 transition-colors`
   - AFTER: `inline-flex items-center justify-center gap-1.5 rounded-xl bg-accent-500 px-4 py-2 text-sm font-semibold text-white hover:bg-accent-600 hover:-translate-y-0.5 hover:shadow-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-400 focus-visible:ring-offset-2 transition-all duration-200`
   Same changes: `rounded-lg` to `rounded-xl`, add hover lift, `transition-all duration-200`
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FaqAccordion container contains `rounded-xl border border-gray-200` (grep confirms)
    - FaqAccordion container contains `divide-gray-100` (grep confirms)
    - FaqAccordion question button contains `px-5 py-5` and `hover:bg-gray-50` (grep confirms)
    - FaqAccordion answer panel contains `px-5 pb-5` (grep confirms)
    - AffiliateLink primary contains `rounded-xl` and `hover:-translate-y-0.5 hover:shadow-md` (grep confirms)
    - AffiliateLink compact contains `rounded-xl` and `hover:-translate-y-0.5` (grep confirms)
    - AffiliateLink both variants contain `transition-all duration-200` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>FaqAccordion wrapped in rounded-xl border container with divide-gray-100, px-5 py-5 questions with hover:bg-gray-50, px-5 pb-5 answers. AffiliateLink both variants use rounded-xl with hover:-translate-y-0.5 hover:shadow-md lift effect.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ReviewCard and RatingBreakdown</name>
  <files>src/components/ReviewCard.tsx, src/components/RatingBreakdown.tsx</files>
  <read_first>src/components/ReviewCard.tsx, src/components/RatingBreakdown.tsx</read_first>
  <action>
**ReviewCard.tsx** -- update these classes:

1. Outer `<article>`:
   - BEFORE: `border-b border-gray-200 py-6 last:border-0`
   - AFTER: `border-b border-gray-100 py-8 last:border-0`
   (Lighter border, more vertical spacing)

2. Avatar `<div>`:
   - BEFORE: `flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-primary-100 text-primary-700 font-semibold text-sm`
   - AFTER: `flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-neutral-100 text-neutral-600 font-semibold text-sm`
   (Change from rounded-full + primary colors to rounded-xl + neutral colors)

**RatingBreakdown.tsx** -- update these classes:

1. Average rating `<p>`:
   - BEFORE: `text-5xl font-bold text-gray-900`
   - AFTER: `text-5xl font-extrabold tracking-tight text-gray-900`

2. Rating bar `<div>` (the h-3 bg-gray-100 bar container):
   - BEFORE: `flex-1 h-3 bg-gray-100 rounded-full overflow-hidden`
   - AFTER: `flex-1 h-2 bg-gray-100 rounded-full overflow-hidden`
   (Slimmer bars: h-3 to h-2)

3. Bar fill `<div>`:
   - No class changes needed (h-full inherits from parent container)
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReviewCard article contains `border-gray-100 py-8` (grep confirms)
    - ReviewCard avatar contains `rounded-xl bg-neutral-100 text-neutral-600` (grep confirms)
    - RatingBreakdown average rating contains `font-extrabold tracking-tight` (grep confirms)
    - RatingBreakdown bar container contains `h-2` (grep confirms)
    - Build succeeds
  </acceptance_criteria>
  <done>ReviewCard uses border-gray-100 lighter borders, py-8 spacing, and rounded-xl bg-neutral-100 neutral avatar. RatingBreakdown uses font-extrabold tracking-tight numbers and h-2 slimmer bars.</done>
</task>

</tasks>

<verification>
After both tasks, verify all four files compile cleanly:
```bash
npx next build
```
</verification>

<success_criteria>
- FaqAccordion has rounded-xl border container, divide-gray-100, px-5 py-5 questions with hover:bg-gray-50
- AffiliateLink has rounded-xl with hover:-translate-y-0.5 hover:shadow-md lift
- ReviewCard has py-8 spacing, border-gray-100, rounded-xl neutral avatar
- RatingBreakdown has font-extrabold tracking-tight numbers and h-2 bars
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-rebrand-component-restyling/15-05-SUMMARY.md`
</output>
