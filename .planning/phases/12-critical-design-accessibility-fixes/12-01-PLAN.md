---
phase: 12-critical-design-accessibility-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/ProviderLogo.tsx
  - src/components/ComparisonTable.tsx
  - src/components/PricingTable.tsx
  - src/components/Header.tsx
  - src/app/providers/[slug]/page.tsx
autonomous: true
requirements:
  - P0-SKIP-NAV
  - P0-FOCUS-VISIBLE
  - P0-LOGO-CONTRAST
  - P0-ICON-CONSISTENCY
  - P0-SECTION-NAV
  - P0-COMPARE-PADDING
  - P0-MOBILE-SEARCH

must_haves:
  truths:
    - "Keyboard users can skip to main content via a visible skip link on Tab"
    - "All interactive elements show a visible focus ring when navigated via keyboard"
    - "ProviderLogo fallback initial letter has sufficient contrast against its background"
    - "Check and X icons use consistent colors across ComparisonTable and PricingTable"
    - "Provider detail section nav does not overlap the sticky header"
    - "Page content is not hidden behind the CompareBar when providers are selected"
    - "Mobile users can access search without opening the hamburger menu"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Skip navigation link as first focusable element"
      contains: "skip-to-main"
    - path: "src/app/globals.css"
      provides: "Baseline :focus-visible outline style"
      contains: "focus-visible"
    - path: "src/components/ProviderLogo.tsx"
      provides: "Accessible contrast on fallback text"
      contains: "text-gray-400"
    - path: "src/components/ComparisonTable.tsx"
      provides: "Standardized check/X icon colors"
      contains: "text-primary-600"
    - path: "src/components/PricingTable.tsx"
      provides: "Standardized check/X icon colors matching ComparisonTable"
      contains: "text-primary-600"
    - path: "src/app/providers/[slug]/page.tsx"
      provides: "Correct section nav stacking below header"
      contains: "sticky top-16"
    - path: "src/components/Header.tsx"
      provides: "Search form visible on mobile"
  key_links:
    - from: "src/app/layout.tsx"
      to: "main element"
      via: "skip link href=#main-content and id=main-content on main"
      pattern: "id=\"main-content\""
---

<objective>
Fix 7 low-complexity P0 issues: skip navigation, focus-visible baseline, ProviderLogo contrast, icon color consistency, section nav z-index, CompareBar padding, and mobile search visibility.

Purpose: These are all 1-5 line changes across separate files that fix WCAG Level A failures, visual inconsistencies, and layout overlap bugs identified by the UX/UI/A11y review agents.
Output: 7 files updated with targeted fixes, no behavioral regressions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-design-accessibility-fixes/12-CONTEXT.md

<interfaces>
<!-- Key files being modified. Executor should read each before editing. -->

From src/app/layout.tsx:
- RootLayout wraps all pages in CompareProvider > Header > main > Footer > CompareBar
- main element currently has className="flex-1"

From src/app/globals.css:
- Tailwind CSS 4 with @import "tailwindcss" and @theme blocks
- No existing :focus-visible rules

From src/components/ProviderLogo.tsx:
- Fallback text uses className="font-bold text-gray-300" (line 38)

From src/components/ComparisonTable.tsx:
- CheckIcon uses className="text-green-600" (line 110)
- XIcon uses className="text-red-400" (line 130)

From src/components/PricingTable.tsx:
- CheckIcon uses className="text-primary-600" (line 41)
- XIcon uses className="text-gray-300" (line 66)

From src/components/Header.tsx:
- HeaderSearchForm wrapped in div with className="hidden sm:block" (line 28)

From src/app/providers/[slug]/page.tsx:
- Section nav has className="sticky top-0 z-10" (line 356)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skip nav, focus-visible, logo contrast, section nav fix, CompareBar padding, mobile search</name>
  <files>src/app/layout.tsx, src/app/globals.css, src/components/ProviderLogo.tsx, src/components/Header.tsx, src/app/providers/[slug]/page.tsx</files>
  <read_first>src/app/layout.tsx, src/app/globals.css, src/components/ProviderLogo.tsx, src/components/Header.tsx, src/app/providers/[slug]/page.tsx</read_first>
  <action>
1. **Skip navigation link** (layout.tsx): Add a skip link as the FIRST child inside body, before CompareProvider. Use this exact markup:
   ```
   <a href="#main-content" className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:rounded-lg focus:bg-primary-600 focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-white focus:shadow-lg">
     Skip to main content
   </a>
   ```
   Also add `id="main-content"` to the existing main element.

2. **:focus-visible baseline** (globals.css): After the closing `}` of the body block, add:
   ```css
   :focus-visible {
     outline: 2px solid oklch(0.627 0.194 149.214);
     outline-offset: 2px;
   }
   ```
   (This uses --color-primary-600 value as a raw oklch for the baseline. Components with their own focus-visible styles via Tailwind will override this.)

3. **ProviderLogo fallback contrast** (ProviderLogo.tsx): Change `text-gray-300` to `text-gray-400` on line 38 (the fallback initial letter span).

4. **Section nav z-index fix** (providers/[slug]/page.tsx): In the section navigation nav element (around line 356), change `sticky top-0 z-10` to `sticky top-16 z-20`. This ensures the section nav stacks below the header (which is z-40 at top-0).

5. **CompareBar bottom padding** (layout.tsx): Add `pb-20` to the main element className so content is not hidden behind the CompareBar when it appears. The main className becomes `"flex-1 pb-20"`. (20 = 5rem = adequate space for the compare bar height.)

6. **Mobile search visibility** (Header.tsx): Change the div wrapping HeaderSearchForm from `className="hidden sm:block"` to `className="hidden xs:block"`. Actually, to make search visible on ALL screen sizes including mobile, remove `hidden sm:block` entirely and just render HeaderSearchForm without the wrapper div, OR change to only hide on very small screens. The simplest approach: change `"hidden sm:block"` to just `"block"` to show search on all viewports. The search input is already compact (w-44) so it fits on mobile.
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'skip-to-main\|main-content' src/app/layout.tsx (skip link present)
    - grep -q 'focus-visible' src/app/globals.css (focus-visible baseline present)
    - grep -q 'text-gray-400' src/components/ProviderLogo.tsx (contrast fixed)
    - grep -q 'sticky top-16 z-20' src/app/providers/\[slug\]/page.tsx (section nav fixed)
    - grep -q 'pb-20' src/app/layout.tsx (CompareBar padding present)
    - NOT grep -q 'hidden sm:block' src/components/Header.tsx (search visible on mobile)
  </acceptance_criteria>
  <done>Skip link is first focusable element and targets main content. :focus-visible provides 2px primary outline on all elements. ProviderLogo fallback uses text-gray-400. Section nav sits below header. Main has bottom padding for CompareBar. Search form is visible on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Check/X icon colors in ComparisonTable and PricingTable</name>
  <files>src/components/ComparisonTable.tsx, src/components/PricingTable.tsx</files>
  <read_first>src/components/ComparisonTable.tsx, src/components/PricingTable.tsx</read_first>
  <action>
Standardize both files to use `text-primary-600` for CheckIcon and `text-gray-300` for XIcon (the PricingTable pattern). This is the correct semantic pairing: primary green for positive, neutral gray for negative.

1. **ComparisonTable.tsx**: Change CheckIcon className from `"text-green-600"` to `"text-primary-600"` (line 110). Change XIcon className from `"text-red-400"` to `"text-gray-300"` (line 130).

2. **PricingTable.tsx**: Already uses `text-primary-600` and `text-gray-300` — no changes needed. Verify by reading the file.

Result: Both tables use identical icon color scheme (primary-600 for check, gray-300 for X).
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && grep -n 'text-green-600\|text-red-400' src/components/ComparisonTable.tsx; echo "Should show no results"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'text-primary-600' src/components/ComparisonTable.tsx (check icon standardized)
    - grep -q 'text-gray-300' src/components/ComparisonTable.tsx (X icon standardized)
    - NOT grep -q 'text-green-600' src/components/ComparisonTable.tsx (old green removed)
    - NOT grep -q 'text-red-400' src/components/ComparisonTable.tsx (old red removed)
  </acceptance_criteria>
  <done>Check and X icons use text-primary-600 and text-gray-300 consistently across both ComparisonTable and PricingTable.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Skip link appears when pressing Tab on any page
- Provider detail section nav does not overlap header when scrolling
- Search form is visible on mobile viewport
- Check/X icons in comparison and pricing tables use matching colors
</verification>

<success_criteria>
All 7 low-complexity P0 fixes applied. Build passes. No regressions in existing functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-design-accessibility-fixes/12-01-SUMMARY.md`
</output>
