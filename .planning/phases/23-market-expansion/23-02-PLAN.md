---
phase: 23-market-expansion
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - prisma/scripts/23-add-providers.ts
autonomous: true
requirements: [R23.1, R23.2, R23.3, R23.5, R23.6]

must_haves:
  truths:
    - "22 new providers exist in the database with ACTIVE status"
    - "All new providers appear on the /search page"
    - "All new provider detail pages render without errors at /providers/{slug}"
    - "next build passes with no errors"
    - "Total provider count is approximately 117"
  artifacts:
    - path: "prisma/scripts/23-add-providers.ts"
      provides: "Migration script to insert 22 new providers"
      min_lines: 100
  key_links:
    - from: "prisma/scripts/23-add-providers.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient provider.upsert"
      pattern: "prisma\\.provider\\.upsert"
    - from: "public/assets/providers/manifest.json"
      to: "prisma/scripts/23-add-providers.ts"
      via: "manifest read for logoUrl"
      pattern: "manifest\\.json"
---

<objective>
Create and run a Prisma migration script to insert all 22 new Tier 1 and Tier 2 providers into the database, then verify they appear correctly on the site and build passes.

Purpose: This is the core data expansion -- adding the actual provider records to the database so they appear on listing pages and have detail pages.
Output: 22 new provider records in the database, migration script committed, build verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-market-expansion/23-CONTEXT.md
@.planning/phases/23-market-expansion/23-01-SUMMARY.md
@.planning/research/v3-market-gaps.md

<interfaces>
<!-- Provider model required fields (from prisma/schema.prisma): -->
```prisma
model Provider {
  name        String
  slug        String   @unique
  description String   @db.Text
  website     String
  category    CategoryType    // MEAL_KIT, PREPARED_MEAL, PROTEIN_BOX, PRODUCE_BOX, SPECIALTY
  status      ProviderStatus  // ACTIVE, HYBRID, UNCLEAR, DISCONTINUED
  // Optional fields we should populate:
  logoUrl     String?
  modelType   String?
  prepStyle   String?
  notes       String?  @db.Text
}
```

<!-- Existing migration script pattern (from prisma/scripts/22-status-cleanup.ts): -->
```typescript
import "dotenv/config";
import { PrismaClient } from "../../src/generated/prisma/client";
import { PrismaPg } from "@prisma/adapter-pg";

const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! });
const prisma = new PrismaClient({ adapter });

async function main() {
  // ... migration logic
  await prisma.$disconnect();
}

main().catch(async (e) => {
  console.error("Migration failed:", e);
  await prisma.$disconnect();
  process.exit(1);
});
```

<!-- Manifest.json entry structure: -->
```json
{ "slug": "provider-slug", "asset": "/assets/providers/provider-slug.png" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run migration script to insert 22 new providers</name>
  <files>prisma/scripts/23-add-providers.ts</files>
  <read_first>
    - prisma/scripts/22-status-cleanup.ts
    - prisma/schema.prisma
    - public/assets/providers/manifest.json
    - src/lib/categories.ts
  </read_first>
  <action>
Create `prisma/scripts/23-add-providers.ts` following the established pattern from `prisma/scripts/22-status-cleanup.ts`.

The script must use `prisma.provider.upsert` (upsert by slug) to insert each of the 22 providers. This ensures the script is idempotent -- safe to re-run.

Read `public/assets/providers/manifest.json` at the top of the script to look up logoUrl for each provider (same pattern as seed.ts).

**All 22 providers with their exact field values:**

**Tier 1 (10 must-adds):**

1. **Clean Eatz Kitchen**
   - slug: `clean-eatz-kitchen`
   - website: `https://cleaneatzkitchen.com`
   - category: `PREPARED_MEAL`
   - status: `ACTIVE`
   - modelType: `subscription-free`
   - prepStyle: `heat-and-eat`
   - description: `Clean Eatz Kitchen -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `No-subscription model (unique positioning), $7.50/meal, flash-frozen, GLP-1 meal plan, dietitian-designed. Appears in multiple 2026 comparison articles as Factor alternative.`

2. **Tempo**
   - slug: `tempo`
   - website: `https://tempomeals.com`
   - category: `PREPARED_MEAL`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `heat-and-eat`
   - parentCompany: `Kroger`
   - description: `Tempo -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Home Chef's prepared meal brand by Kroger. 4-minute microwave meals, $11-13/serving. Launched Oct 2023. Appears in NBC, Fortune, Taste of Home 2026 lists.`

3. **Rastelli's**
   - slug: `rastellis`
   - website: `https://rastellis.com`
   - category: `PROTEIN_BOX`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `raw-ingredients`
   - description: `Rastelli's -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Widely reviewed meat/seafood subscription. Grass-fed, blast-frozen at -40F. Flexible 2/4/6/8 week delivery. $12+/portion. Free shipping over $200.`

4. **Sea to Table**
   - slug: `sea-to-table`
   - website: `https://sea2table.com`
   - category: `PROTEIN_BOX`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `raw-ingredients`
   - description: `Sea to Table -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Wild-caught US seafood subscriptions. DNA-tested fish (only company doing this). 4/6/8 week delivery. Traceable to specific fisheries.`

5. **Cometeer**
   - slug: `cometeer`
   - website: `https://cometeer.com`
   - category: `SPECIALTY`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `ready-to-drink`
   - description: `Cometeer -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Flash-frozen coffee capsules. Innovative format. $2/cup. Strong media buzz. Multiple 2026 "best coffee subscription" lists.`

6. **TokyoTreat**
   - slug: `tokyotreat`
   - website: `https://tokyotreat.com`
   - category: `SPECIALTY`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `ready-to-eat`
   - description: `TokyoTreat -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Japanese snack box, 15-20 items, $32.50/mo. 24-page culture guide. Ships worldwide. Very popular.`

7. **Japan Crate**
   - slug: `japan-crate`
   - website: `https://japancrate.com`
   - category: `SPECIALTY`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `ready-to-eat`
   - description: `Japan Crate -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Japanese candy/snacks, 3 tiers ($12-35/mo). DIY kits, drinks included.`

8. **Munch Addict**
   - slug: `munch-addict`
   - website: `https://munchaddict.com`
   - category: `SPECIALTY`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `ready-to-eat`
   - description: `Munch Addict -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `International snacks, 5-72 snacks per order. Also Korean-specific box. LA-based.`

9. **Heatonist**
   - slug: `heatonist`
   - website: `https://heatonist.com`
   - category: `SPECIALTY`
   - status: `ACTIVE`
   - modelType: `subscription`
   - prepStyle: `condiment`
   - description: `Heatonist -- see research notes for positioning, pricing, and flexibility details.`
   - notes: `Official Hot Ones hot sauce subscription. 3 sauces/month. Strong brand recognition from the show.`

10. **Melissa's Produce**
    - slug: `melissas-produce`
    - website: `https://melissas.com`
    - category: `PRODUCE_BOX`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `raw-ingredients`
    - description: `Melissa's Produce -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Named "best overall produce delivery" by Taste of Home. Subscription clubs (HarvestClub Americana, Medley, Exotica, Organic). National.`

**Tier 2 (12 should-adds):**

11. **Sprinly**
    - slug: `sprinly`
    - website: `https://sprinly.com`
    - category: `PREPARED_MEAL`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `heat-and-eat`
    - description: `Sprinly -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `100% plant-based, organic, gluten-free prepared meals. Ships nationwide incl AK/HI. $18/meal. Cleveland-based.`

12. **ModifyHealth**
    - slug: `modifyhealth`
    - website: `https://modifyhealth.com`
    - category: `PREPARED_MEAL`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `heat-and-eat`
    - description: `ModifyHealth -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Gut health / Low FODMAP specialist. Monash University certified. Also GLP-1, Mediterranean, diabetes-friendly. Free shipping. Dietitian access included.`

13. **MealPro**
    - slug: `mealpro`
    - website: `https://mealpro.net`
    - category: `PREPARED_MEAL`
    - status: `ACTIVE`
    - modelType: `subscription-free`
    - prepStyle: `heat-and-eat`
    - description: `MealPro -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Fitness-focused, no subscription required, macro-customizable, registered dietitian designed. Nationwide delivery.`

14. **MegaFit Meals**
    - slug: `megafit-meals`
    - website: `https://megafitmeals.com`
    - category: `PREPARED_MEAL`
    - status: `ACTIVE`
    - modelType: `hybrid`
    - prepStyle: `heat-and-eat`
    - description: `MegaFit Meals -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Fitness/bodybuilding focused, $12.95-18/meal, 40+ rotating meals, macro-customizable, one-time or subscription.`

15. **Methodology**
    - slug: `methodology`
    - website: `https://gomethodology.com`
    - category: `PREPARED_MEAL`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `heat-and-eat`
    - description: `Methodology -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Premium prepared meals ($14-18/meal), zero food waste, gluten/dairy/refined sugar free. Expanded from SF Bay Area to nationwide.`

16. **Primal Pastures**
    - slug: `primal-pastures`
    - website: `https://primalpastures.com`
    - category: `PROTEIN_BOX`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `raw-ingredients`
    - description: `Primal Pastures -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Regenerative, pasture-raised, soy-free, sugar-free. Nationwide. 10-20lb boxes.`

17. **Alaskan Salmon Company**
    - slug: `alaskan-salmon-company`
    - website: `https://aksalmonco.com`
    - category: `PROTEIN_BOX`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `raw-ingredients`
    - description: `Alaskan Salmon Company -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Direct-from-Alaska wild salmon. Flash-frozen, customizable boxes.`

18. **Wild Tide Seafoods**
    - slug: `wild-tide-seafoods`
    - website: `https://wildtideseafoods.com`
    - category: `PROTEIN_BOX`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `raw-ingredients`
    - description: `Wild Tide Seafoods -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Wild-caught seafood subscription boxes. Captain's Choice, Omega, Grill boxes. 14% subscriber discount.`

19. **Frog Hollow Farm**
    - slug: `frog-hollow-farm`
    - website: `https://froghollow.com`
    - category: `PRODUCE_BOX`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `raw-ingredients`
    - description: `Frog Hollow Farm -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Premium organic fruit subscriptions. "Tree to table" from their own California farm. Bay Area CSA + nationwide shipping.`

20. **Seoulbox**
    - slug: `seoulbox`
    - website: `https://myseoulbox.com`
    - category: `SPECIALTY`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `ready-to-eat`
    - description: `Seoulbox -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Korean snacks + K-beauty + K-pop merch. Ships from Seoul. Vegetarian option available.`

21. **SnackFever**
    - slug: `snackfever`
    - website: `https://snackfever.com`
    - category: `SPECIALTY`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `ready-to-eat`
    - description: `SnackFever -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Korean snacks, $19.99-29.99/box. Ships from Korea.`

22. **Fuego Box**
    - slug: `fuego-box`
    - website: `https://fuegobox.com`
    - category: `SPECIALTY`
    - status: `ACTIVE`
    - modelType: `subscription`
    - prepStyle: `condiment`
    - description: `Fuego Box -- see research notes for positioning, pricing, and flexibility details.`
    - notes: `Curated hot sauce subscription. Flavor-forward, not gimmicky heat.`

**Script structure:**
1. Import dotenv, PrismaClient, PrismaPg, fs, path (same as 22-status-cleanup.ts)
2. Read manifest.json to build a slug-to-asset map
3. Define the 22 providers as a typed array
4. Loop through each provider and call `prisma.provider.upsert({ where: { slug }, update: {}, create: { ...allFields } })`
   - The `update: {}` means re-running the script won't overwrite existing data if the provider was later enriched
5. Log each provider created/skipped
6. Print summary with total count
7. Disconnect

Run: `npx tsx prisma/scripts/23-add-providers.ts`
  </action>
  <verify>
    <automated>npx tsx -e "
import 'dotenv/config';
import { PrismaClient } from './src/generated/prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! });
const prisma = new PrismaClient({ adapter });
async function check() {
  const slugs = ['clean-eatz-kitchen','tempo','rastellis','sea-to-table','cometeer','tokyotreat','japan-crate','munch-addict','heatonist','melissas-produce','sprinly','modifyhealth','mealpro','megafit-meals','methodology','primal-pastures','alaskan-salmon-company','wild-tide-seafoods','frog-hollow-farm','seoulbox','snackfever','fuego-box'];
  const found = await prisma.provider.findMany({ where: { slug: { in: slugs } }, select: { slug: true, status: true, category: true } });
  console.log('Found ' + found.length + ' of 22 new providers');
  const missing = slugs.filter(s => !found.find(f => f.slug === s));
  if (missing.length > 0) console.log('MISSING:', missing);
  else console.log('ALL 22 PRESENT');
  const total = await prisma.provider.count();
  console.log('Total providers in DB:', total);
  await prisma.\$disconnect();
}
check();
"</automated>
  </verify>
  <acceptance_criteria>
    - All 22 providers exist in the database (verified by slug lookup)
    - All 22 have status ACTIVE
    - All 22 have correct category assignments: 7 PREPARED_MEAL, 5 PROTEIN_BOX, 2 PRODUCE_BOX, 8 SPECIALTY
    - All 22 have logoUrl set from manifest.json
    - All 22 have modelType and prepStyle populated
    - Total provider count is approximately 117
    - Script file exists at prisma/scripts/23-add-providers.ts
  </acceptance_criteria>
  <done>22 new providers inserted into the database with all required fields populated</done>
</task>

<task type="auto">
  <name>Task 2: Verify site rendering and build</name>
  <files></files>
  <read_first>
    - src/app/providers/[slug]/page.tsx
    - src/lib/queries.ts
  </read_first>
  <action>
Run verification checks to confirm all 22 new providers work correctly:

1. **Build check:** Run `npx next build` (which runs `prisma generate && next build` via the build script). This must pass with zero errors. This also confirms:
   - All new provider slugs generate valid static pages
   - No TypeScript errors
   - No missing imports or broken references

2. **Database spot check:** Query the database to verify:
   - Total active provider count is ~116+ (95 original - 1 discontinued + 22 new)
   - All 22 new providers have: name, slug, website, category, status=ACTIVE, logoUrl, modelType, prepStyle
   - Category distribution of new providers matches expectations: 7 PREPARED_MEAL (clean-eatz-kitchen, tempo, sprinly, modifyhealth, mealpro, megafit-meals, methodology), 5 PROTEIN_BOX (rastellis, sea-to-table, primal-pastures, alaskan-salmon-company, wild-tide-seafoods), 2 PRODUCE_BOX (melissas-produce, frog-hollow-farm), 8 SPECIALTY (cometeer, tokyotreat, japan-crate, munch-addict, heatonist, seoulbox, snackfever, fuego-box)

3. **Slug uniqueness check:** Verify no duplicate slugs exist in the database.

If build fails, diagnose and fix the issue. Common issues: missing logo files referenced in manifest, TypeScript errors in new scripts, or Prisma client out of sync (fix with `npx prisma generate`).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output shows no errors
    - No TypeScript compilation errors
    - All provider detail pages can be statically generated (no notFound errors during build for new slugs)
  </acceptance_criteria>
  <done>next build passes, confirming all 22 new providers have valid detail pages, appear on listing pages, and the site is deployment-ready</done>
</task>

</tasks>

<verification>
- 22 new providers exist in the database with correct fields
- All new providers appear on /search page (ACTIVE status)
- All new provider detail pages render (build generates them)
- `next build` passes with zero errors
- Total provider count is approximately 117
</verification>

<success_criteria>
- 22 new providers in database with name, slug, website, category, status, modelType, prepStyle, logoUrl
- All 10 Tier 1 and 12 Tier 2 providers accounted for
- next build passes
- Migration script is idempotent (safe to re-run)
</success_criteria>

<output>
After completion, create `.planning/phases/23-market-expansion/23-02-SUMMARY.md`
</output>
