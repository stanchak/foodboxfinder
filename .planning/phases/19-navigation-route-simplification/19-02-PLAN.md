---
phase: 19-navigation-route-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/proxy.ts
  - src/app/sitemap.ts
autonomous: true
requirements:
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "Homepage category cards link to /search?category={slug} not /{slug}"
    - "Visiting /meal-kits returns 301 redirect to /search?category=meal-kits"
    - "Visiting /prepared-meals returns 301 redirect to /search?category=prepared-meals"
    - "Visiting /protein-boxes returns 301 redirect to /search?category=protein-boxes"
    - "Visiting /produce-boxes returns 301 redirect to /search?category=produce-boxes"
    - "Visiting /specialty returns 301 redirect to /search?category=specialty"
    - "Sitemap includes /search but not individual category page URLs"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with updated category card links"
      contains: "/search?category="
    - path: "src/proxy.ts"
      provides: "301 redirects for old category URLs"
      contains: "category"
    - path: "src/app/sitemap.ts"
      provides: "Updated sitemap without individual category pages"
  key_links:
    - from: "src/app/page.tsx"
      to: "/search?category="
      via: "Category card Link hrefs"
      pattern: '/search\\?category='
    - from: "src/proxy.ts"
      to: "/search?category="
      via: "301 redirect responses"
      pattern: "301"
---

<objective>
Update homepage category card links to point to /search?category={slug}, add 301 redirects in proxy.ts for the 5 old category URLs, and update the sitemap to reflect the new URL structure.

Purpose: SEO-safe transition from individual category pages to unified discovery. Old URLs redirect with 301 (permanent) to preserve link equity. Sitemap tells crawlers the correct URLs.
Output: Updated page.tsx, proxy.ts, sitemap.ts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-navigation-route-simplification/19-CONTEXT.md

@src/app/page.tsx
@src/proxy.ts
@src/app/sitemap.ts
@src/lib/categories.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/categories.ts:
```typescript
export const CATEGORY_MAP: Record<CategoryType, { slug: string; label: string; description: string }>;
export function getCategoryBySlug(slug: string): { key: CategoryType; slug: string; label: string; description: string } | undefined;
```

From src/proxy.ts (current):
```typescript
export function proxy(request: NextRequest): NextResponse;
export const config = { matcher: "/admin/:path*" };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage category links and add proxy.ts redirects</name>
  <files>src/app/page.tsx, src/proxy.ts</files>
  <read_first>src/app/page.tsx, src/proxy.ts, src/lib/categories.ts</read_first>
  <action>
**src/app/page.tsx:**
- In the hero "Category quick-links" nav section (around line 341), change:
  `href={`/${cat.slug}`}` to `href={`/search?category=${cat.slug}`}`
- In the "Browse by Category" section (around line 445), change:
  `href={`/${cat.slug}`}` to `href={`/search?category=${cat.slug}`}`
- All other links on the homepage already point to /search or /compare or /best -- leave them as-is
- Do NOT change anything else in page.tsx

**src/proxy.ts:**
- Add category route redirects BEFORE the admin route check
- Import `CATEGORY_MAP` from `@/lib/categories` to get the 5 category slugs
- Build a Set of category slugs from CATEGORY_MAP values for O(1) lookup
- Check if `pathname` matches any category slug pattern:
  - Strip leading slash: `const segment = pathname.slice(1)`
  - If `categorySlugs.has(segment)`, redirect to `/search?category=${segment}` with 301 status
  - Use `NextResponse.redirect(new URL(`/search?category=${segment}`, request.url), 301)` for permanent redirect
- The 5 slugs to redirect: `meal-kits`, `prepared-meals`, `protein-boxes`, `produce-boxes`, `specialty`
- Update the `config.matcher` to include both admin and category routes:
  ```typescript
  export const config = {
    matcher: ["/admin/:path*", "/meal-kits", "/prepared-meals", "/protein-boxes", "/produce-boxes", "/specialty"],
  };
  ```
- Keep all existing admin auth logic exactly as-is
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx does NOT contain `href={`/${cat.slug}`}` pattern (old direct category links)
    - page.tsx contains `href={`/search?category=${cat.slug}`}` pattern in both hero quick-links and Browse by Category sections
    - proxy.ts contains `301` for permanent redirect status
    - proxy.ts contains `/search?category=` redirect target
    - proxy.ts still contains the admin auth check logic (unchanged)
    - proxy.ts config.matcher includes "/meal-kits" and "/specialty" entries
    - Build passes without errors
  </acceptance_criteria>
  <done>Homepage category cards link to /search?category={slug}. Old category URLs (/meal-kits, /prepared-meals, /protein-boxes, /produce-boxes, /specialty) permanently redirect to /search?category={slug} via proxy.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Update sitemap to remove old category pages and add /search variants</name>
  <files>src/app/sitemap.ts</files>
  <read_first>src/app/sitemap.ts, src/lib/categories.ts</read_first>
  <action>
**src/app/sitemap.ts:**
- Remove the entire `categoryPages` block (lines 56-63) that generates `/${slug}` URLs from CATEGORY_MAP
- Replace it with `searchCategoryPages` that generates `/search?category=${slug}` URLs:
  ```typescript
  const searchCategoryPages: MetadataRoute.Sitemap = Object.values(CATEGORY_MAP).map(
    ({ slug }) => ({
      url: `${BASE_URL}/search?category=${slug}`,
      lastModified: now,
      changeFrequency: "weekly" as const,
      priority: 0.8,
    }),
  );
  ```
- Note: priority lowered from 0.9 to 0.8 since these are filtered views of /search, not standalone pages
- Update the /search static page entry priority from 0.5 to 0.9 since it is now the primary discovery page
- Update the return array to spread `searchCategoryPages` instead of `categoryPages`
- Keep all other entries (static pages, providers, collections, blog) exactly as-is
  </action>
  <verify>
    <automated>cd /Users/chrisstanchak/Code/foodboxfinder && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sitemap.ts does NOT contain `${BASE_URL}/${slug}` pattern for category pages
    - sitemap.ts contains `${BASE_URL}/search?category=${slug}` pattern for search category variants
    - sitemap.ts /search entry has priority 0.9
    - sitemap.ts still contains provider, collection, and blog page entries
    - Build passes without errors
  </acceptance_criteria>
  <done>Sitemap no longer includes old /{category} URLs. Instead includes /search (priority 0.9) and /search?category={slug} variants (priority 0.8). All other sitemap entries unchanged.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- page.tsx: grep for `/search?category=` finds category card links, no old `/{cat.slug}` links remain
- proxy.ts: grep for `301` confirms permanent redirect, grep for `/search?category=` confirms redirect target
- sitemap.ts: grep for `search?category=` finds new entries, no bare `/${slug}` category entries remain
</verification>

<success_criteria>
1. Homepage hero category quick-links and Browse by Category cards all link to /search?category={slug}
2. Visiting /meal-kits, /prepared-meals, /protein-boxes, /produce-boxes, /specialty returns 301 redirect to /search?category={slug}
3. Sitemap includes /search (priority 0.9) and 5 /search?category={slug} variants (priority 0.8)
4. Sitemap does NOT include old /{category} URLs
5. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-navigation-route-simplification/19-02-SUMMARY.md`
</output>
